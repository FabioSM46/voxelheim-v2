// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
#[deprecated(
    since = "2.0.0",
    note = "Use associated constants instead. This will no longer be generated in 2021."
)]
pub const ENUM_MIN_RESIDENT_ROLE: u8 = 0;
#[deprecated(
    since = "2.0.0",
    note = "Use associated constants instead. This will no longer be generated in 2021."
)]
pub const ENUM_MAX_RESIDENT_ROLE: u8 = 7;
#[deprecated(
    since = "2.0.0",
    note = "Use associated constants instead. This will no longer be generated in 2021."
)]
#[allow(non_camel_case_types)]
pub const ENUM_VALUES_RESIDENT_ROLE: [ResidentRole; 8] = [
    ResidentRole::Unknown,
    ResidentRole::Villager,
    ResidentRole::Smith,
    ResidentRole::Carpenter,
    ResidentRole::Cook,
    ResidentRole::Trader,
    ResidentRole::Guard,
    ResidentRole::Stablemaster,
];

/// What a resident does in their settlement. A vocabulary, not a description.
///
/// The same shape `MarkerKind` and `HairModel` are: the server stores the member and
/// draws nothing, and the client maps it to whatever it shows — a word under the name,
/// an icon, an apron. That is why every member is an occupation and none of them is a
/// sprite, a schedule or a dialogue tree.
///
/// **`Villager` is the member with no trade**, and it is 1 rather than last because it
/// is the ordinary case: most residents of a village are not smiths. It is distinct from
/// `Unknown`, which is the absent-field zero and means the message failed to say.
///
/// **A role is not a capability.** `Trader` does not mean this entity has a stall, and
/// `Smith` does not mean it will repair anything: whether a `VendorState` opens is the
/// server's answer to an `NpcInteractRequest` and is never inferred from this member.
/// A client that greyed out the interact prompt for every non-`Trader` would be deciding
/// a gameplay outcome, which is the one thing it may not do.
///
/// Zero member for the reason every enum in this contract has one, and it fails closed:
/// a `ResidentAppearance` whose role is `Unknown` is refused rather than drawn as a
/// generic villager, because the server always knows what a resident is.
#[derive(Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Default)]
#[repr(transparent)]
pub struct ResidentRole(pub u8);
#[allow(non_upper_case_globals)]
impl ResidentRole {
    pub const Unknown: Self = Self(0);
    pub const Villager: Self = Self(1);
    pub const Smith: Self = Self(2);
    pub const Carpenter: Self = Self(3);
    pub const Cook: Self = Self(4);
    pub const Trader: Self = Self(5);
    pub const Guard: Self = Self(6);
    pub const Stablemaster: Self = Self(7);

    pub const ENUM_MIN: u8 = 0;
    pub const ENUM_MAX: u8 = 7;
    pub const ENUM_VALUES: &'static [Self] = &[
        Self::Unknown,
        Self::Villager,
        Self::Smith,
        Self::Carpenter,
        Self::Cook,
        Self::Trader,
        Self::Guard,
        Self::Stablemaster,
    ];
    /// Returns the variant's name or "" if unknown.
    pub fn variant_name(self) -> Option<&'static str> {
        match self {
            Self::Unknown => Some("Unknown"),
            Self::Villager => Some("Villager"),
            Self::Smith => Some("Smith"),
            Self::Carpenter => Some("Carpenter"),
            Self::Cook => Some("Cook"),
            Self::Trader => Some("Trader"),
            Self::Guard => Some("Guard"),
            Self::Stablemaster => Some("Stablemaster"),
            _ => None,
        }
    }
}
impl ::core::fmt::Debug for ResidentRole {
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        if let Some(name) = self.variant_name() {
            f.write_str(name)
        } else {
            f.write_fmt(format_args!("<UNKNOWN {:?}>", self.0))
        }
    }
}
impl<'a> ::flatbuffers::Follow<'a> for ResidentRole {
    type Inner = Self;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        let b = unsafe { ::flatbuffers::read_scalar_at::<u8>(buf, loc) };
        Self(b)
    }
}

impl ::flatbuffers::Push for ResidentRole {
    type Output = ResidentRole;
    #[inline]
    unsafe fn push(&self, dst: &mut [u8], _written_len: usize) {
        unsafe { ::flatbuffers::emplace_scalar::<u8>(dst, self.0) };
    }
}

impl ::flatbuffers::EndianScalar for ResidentRole {
    type Scalar = u8;
    #[inline]
    fn to_little_endian(self) -> u8 {
        self.0.to_le()
    }
    #[inline]
    #[allow(clippy::wrong_self_convention)]
    fn from_little_endian(v: u8) -> Self {
        let b = u8::from_le(v);
        Self(b)
    }
}

impl<'a> ::flatbuffers::Verifiable for ResidentRole {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        u8::run_verifier(v, pos)
    }
}

impl ::flatbuffers::SimpleToVerifyInSlice for ResidentRole {}
