// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
pub enum ActionRefusedOffset {}
#[derive(Copy, Clone, PartialEq)]

/// The server's answer to an action it refused. Server -> client.
///
/// **The first message in this contract that says no to anything but a connection.**
/// Every refused action before it was silence plus a line in a server log: the reason
/// was computed, written down where no player could reach it, and thrown away. A
/// placement that vanishes with no answer is indistinguishable from a lost packet, from
/// a click that missed, and from a bug — and the one thing it is not indistinguishable
/// from is a placement that worked, which is the only feedback the player had.
///
/// **It is not an acknowledgement, and there is still no acceptance payload.** A
/// structure exists when a snapshot says it does; nothing here changes that, and a
/// client must never treat the *absence* of an `ActionRefused` as a yes. This message
/// carries one thing: the reason a thing that did not happen did not happen.
///
/// **Appending it is not a protocol break.** Union members are append-only, this is
/// tag 20, and a peer built against an earlier contract reads an unknown tag as a
/// payload it has no name for and drops it. So an older client loses the feedback and
/// nothing else, which is why `ProtocolVersion.Current` does not move for this.
///
/// Decoder invariants:
///   - `action` and `reason` are both read through their zero members: an absent or
///     unrecognised value is `Unknown`, and a receiver shows a player nothing rather
///     than inventing a sentence for a code it cannot read. It never fails the frame —
///     a refusal is the least important message on the wire, and a decode error would
///     end a session over one
///   - `anchor` is absent whenever the refused request named no voxel, which includes
///     every request that carried no anchor at all. Present or absent, never zero as a
///     stand-in for absent
pub struct ActionRefused<'a> {
    pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for ActionRefused<'a> {
    type Inner = ActionRefused<'a>;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        Self {
            _tab: unsafe { ::flatbuffers::Table::new(buf, loc) },
        }
    }
}

impl<'a> ActionRefused<'a> {
    pub const VT_ACTION: ::flatbuffers::VOffsetT = 4;
    pub const VT_REASON: ::flatbuffers::VOffsetT = 6;
    pub const VT_ANCHOR: ::flatbuffers::VOffsetT = 8;

    #[inline]
    pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
        ActionRefused { _tab: table }
    }
    #[allow(unused_mut)]
    pub fn create<
        'bldr: 'args,
        'args: 'mut_bldr,
        'mut_bldr,
        A: ::flatbuffers::Allocator + 'bldr,
    >(
        _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
        args: &'args ActionRefusedArgs<'args>,
    ) -> ::flatbuffers::WIPOffset<ActionRefused<'bldr>> {
        let mut builder = ActionRefusedBuilder::new(_fbb);
        if let Some(x) = args.anchor {
            builder.add_anchor(x);
        }
        builder.add_reason(args.reason);
        builder.add_action(args.action);
        builder.finish()
    }

    /// Which action was refused. Enough on its own to route the answer, because at most
    /// one of a player's actions is in flight per press.
    #[inline]
    pub fn action(&self) -> RefusedAction {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<RefusedAction>(ActionRefused::VT_ACTION, Some(RefusedAction::Unknown))
                .unwrap()
        }
    }
    /// Why. The whole point of the message.
    #[inline]
    pub fn reason(&self) -> RefusalReason {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<RefusalReason>(ActionRefused::VT_REASON, Some(RefusalReason::Unknown))
                .unwrap()
        }
    }
    /// The voxel the refused request named, when it named one — the anchor of a
    /// placement, the target of a mine or a block edit. Absent for an action that names
    /// no cell, and absent for a request whose anchor was the thing that was missing.
    ///
    /// Carried so a client can show the answer where the player was looking rather than
    /// only in a line of text, and so a refusal that raced ahead of the player's aim is
    /// recognisable as belonging to somewhere else.
    #[inline]
    pub fn anchor(&self) -> Option<&'a BlockCoord> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe { self._tab.get::<BlockCoord>(ActionRefused::VT_ANCHOR, None) }
    }
}

impl ::flatbuffers::Verifiable for ActionRefused<'_> {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        v.visit_table(pos)?
            .visit_field::<RefusedAction>("action", Self::VT_ACTION, false)?
            .visit_field::<RefusalReason>("reason", Self::VT_REASON, false)?
            .visit_field::<BlockCoord>("anchor", Self::VT_ANCHOR, false)?
            .finish();
        Ok(())
    }
}
pub struct ActionRefusedArgs<'a> {
    pub action: RefusedAction,
    pub reason: RefusalReason,
    pub anchor: Option<&'a BlockCoord>,
}
impl<'a> Default for ActionRefusedArgs<'a> {
    #[inline]
    fn default() -> Self {
        ActionRefusedArgs {
            action: RefusedAction::Unknown,
            reason: RefusalReason::Unknown,
            anchor: None,
        }
    }
}

pub struct ActionRefusedBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
    fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> ActionRefusedBuilder<'a, 'b, A> {
    #[inline]
    pub fn add_action(&mut self, action: RefusedAction) {
        self.fbb_.push_slot::<RefusedAction>(
            ActionRefused::VT_ACTION,
            action,
            RefusedAction::Unknown,
        );
    }
    #[inline]
    pub fn add_reason(&mut self, reason: RefusalReason) {
        self.fbb_.push_slot::<RefusalReason>(
            ActionRefused::VT_REASON,
            reason,
            RefusalReason::Unknown,
        );
    }
    #[inline]
    pub fn add_anchor(&mut self, anchor: &BlockCoord) {
        self.fbb_
            .push_slot_always::<&BlockCoord>(ActionRefused::VT_ANCHOR, anchor);
    }
    #[inline]
    pub fn new(
        _fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    ) -> ActionRefusedBuilder<'a, 'b, A> {
        let start = _fbb.start_table();
        ActionRefusedBuilder {
            fbb_: _fbb,
            start_: start,
        }
    }
    #[inline]
    pub fn finish(self) -> ::flatbuffers::WIPOffset<ActionRefused<'a>> {
        let o = self.fbb_.end_table(self.start_);
        ::flatbuffers::WIPOffset::new(o.value())
    }
}

impl ::core::fmt::Debug for ActionRefused<'_> {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        let mut ds = f.debug_struct("ActionRefused");
        ds.field("action", &self.action());
        ds.field("reason", &self.reason());
        ds.field("anchor", &self.anchor());
        ds.finish()
    }
}
