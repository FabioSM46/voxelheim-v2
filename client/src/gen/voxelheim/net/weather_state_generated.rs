// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
/// The weather at one point in the world, as the server holds it.
///
/// **A struct, and both halves of the rule apply.** It is hot — one per snapshot, once per
/// recipient per tick — and settled: a kind and a number is the whole of what weather is on
/// the wire, because everything a client draws from it (particle density, fog, wind, sound)
/// is presentation derived from those two. What is *not* settled — where a front is, how
/// long it lasts, what it is called — is a `StormWarning`, a table for that reason.
///
/// Decoder invariants:
///   - `kind` is a known non-zero member. `Unknown` is the absent-field zero and a
///     present struct that carries it is a protocol error, never "no weather"
///   - `intensity` is 0 for `Clear` and may be anything for the other kinds. 0 is none
///     and 255 is the most the sky can do; nothing divides the range into named bands,
///     so a client interpolates rather than switching on it
// struct WeatherState, aligned to 1
#[repr(transparent)]
#[derive(Clone, Copy, PartialEq)]
pub struct WeatherState(pub [u8; 2]);
impl Default for WeatherState {
    fn default() -> Self {
        Self([0; 2])
    }
}
impl ::core::fmt::Debug for WeatherState {
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        f.debug_struct("WeatherState")
            .field("kind", &self.kind())
            .field("intensity", &self.intensity())
            .finish()
    }
}

impl ::flatbuffers::SimpleToVerifyInSlice for WeatherState {}
impl<'a> ::flatbuffers::Follow<'a> for WeatherState {
    type Inner = &'a WeatherState;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        unsafe { <&'a WeatherState>::follow(buf, loc) }
    }
}
impl<'a> ::flatbuffers::Follow<'a> for &'a WeatherState {
    type Inner = &'a WeatherState;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        unsafe { ::flatbuffers::follow_cast_ref::<WeatherState>(buf, loc) }
    }
}
impl<'b> ::flatbuffers::Push for WeatherState {
    type Output = WeatherState;
    #[inline]
    unsafe fn push(&self, dst: &mut [u8], _written_len: usize) {
        let src = unsafe {
            ::core::slice::from_raw_parts(
                self as *const WeatherState as *const u8,
                <Self as ::flatbuffers::Push>::size(),
            )
        };
        dst.copy_from_slice(src);
    }
    #[inline]
    fn alignment() -> ::flatbuffers::PushAlignment {
        ::flatbuffers::PushAlignment::new(1)
    }
}

impl<'a> ::flatbuffers::Verifiable for WeatherState {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        v.in_buffer::<Self>(pos)
    }
}

impl<'a> WeatherState {
    #[allow(clippy::too_many_arguments)]
    pub fn new(kind: WeatherKind, intensity: u8) -> Self {
        let mut s = Self([0; 2]);
        s.set_kind(kind);
        s.set_intensity(intensity);
        s
    }

    pub fn kind(&self) -> WeatherKind {
        let mut mem = ::core::mem::MaybeUninit::<
            <WeatherKind as ::flatbuffers::EndianScalar>::Scalar,
        >::uninit();
        // Safety:
        // Created from a valid Table for this object
        // Which contains a valid value in this slot
        ::flatbuffers::EndianScalar::from_little_endian(unsafe {
            ::core::ptr::copy_nonoverlapping(
                self.0[0..].as_ptr(),
                mem.as_mut_ptr() as *mut u8,
                ::core::mem::size_of::<<WeatherKind as ::flatbuffers::EndianScalar>::Scalar>(),
            );
            mem.assume_init()
        })
    }

    pub fn set_kind(&mut self, x: WeatherKind) {
        let x_le = ::flatbuffers::EndianScalar::to_little_endian(x);
        // Safety:
        // Created from a valid Table for this object
        // Which contains a valid value in this slot
        unsafe {
            ::core::ptr::copy_nonoverlapping(
                &x_le as *const _ as *const u8,
                self.0[0..].as_mut_ptr(),
                ::core::mem::size_of::<<WeatherKind as ::flatbuffers::EndianScalar>::Scalar>(),
            );
        }
    }

    /// How hard it is coming down, 0 to 255. Not a percentage and not a rate: a scalar the
    /// server owns, which presentation reads and gameplay is already computed from.
    pub fn intensity(&self) -> u8 {
        let mut mem =
            ::core::mem::MaybeUninit::<<u8 as ::flatbuffers::EndianScalar>::Scalar>::uninit();
        // Safety:
        // Created from a valid Table for this object
        // Which contains a valid value in this slot
        ::flatbuffers::EndianScalar::from_little_endian(unsafe {
            ::core::ptr::copy_nonoverlapping(
                self.0[1..].as_ptr(),
                mem.as_mut_ptr() as *mut u8,
                ::core::mem::size_of::<<u8 as ::flatbuffers::EndianScalar>::Scalar>(),
            );
            mem.assume_init()
        })
    }

    pub fn set_intensity(&mut self, x: u8) {
        let x_le = ::flatbuffers::EndianScalar::to_little_endian(x);
        // Safety:
        // Created from a valid Table for this object
        // Which contains a valid value in this slot
        unsafe {
            ::core::ptr::copy_nonoverlapping(
                &x_le as *const _ as *const u8,
                self.0[1..].as_mut_ptr(),
                ::core::mem::size_of::<<u8 as ::flatbuffers::EndianScalar>::Scalar>(),
            );
        }
    }
}
