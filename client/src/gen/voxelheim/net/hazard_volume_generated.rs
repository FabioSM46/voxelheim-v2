// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
pub enum HazardVolumeOffset {}
#[derive(Copy, Clone, PartialEq)]

/// One bounded region an announced move endangers, in world space. Server -> client.
///
/// **It is the region, not the damage.** Whether a player standing in it is hit is the
/// server's answer and arrives as `BlowLanded`; nothing here lets a client decide it, and
/// a client that took a health point away because a body overlapped this volume would be
/// deciding a gameplay outcome. The volume exists so the danger can be *shown* — which is
/// the readability the whole encounter rests on — and so a receiver can tell the region
/// announced in `Telegraph` from the one that is live in `Release`.
///
/// Every region is bounded, and the bound is checked rather than assumed: an unbounded
/// or non-finite volume reaches a renderer as a shape covering the world, and a receiver
/// that clamped it would be inventing a danger boundary the server did not state.
///
/// Decoder invariants:
///   - `shape` is a known non-zero member
///   - `origin` is present and every component is finite and inside the +/-2^24 block
///     domain
///   - `direction` is present and every component is finite. `Cone` and `Line` require a
///     non-zero direction; `Disc` and `Ring` ignore it, and a server writes zero there
///   - `radius` is finite, strictly positive, and at most 128 blocks. No move in this
///     encounter reaches further, and an arena is smaller than that
///   - `height` is finite, strictly positive, and at most 128 blocks
///   - `inner_radius` is finite and in `0 .. radius`; `Ring` requires it strictly less
///     than `radius`, and the other three shapes ignore it
///   - `half_angle` is finite and in `0 .. pi` radians; `Cone` requires it strictly
///     positive, and the other three shapes ignore it. The bound is pi *as a float32
///     holds it* — the nearest float32 to pi sits fractionally above the real value, so
///     a receiver comparing in double precision would refuse the widest cone a sender
///     can encode
///   - `half_width` is finite and in `0 .. radius`; `Line` requires it strictly
///     positive, and the other three shapes ignore it
pub struct HazardVolume<'a> {
    pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for HazardVolume<'a> {
    type Inner = HazardVolume<'a>;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        Self {
            _tab: unsafe { ::flatbuffers::Table::new(buf, loc) },
        }
    }
}

impl<'a> HazardVolume<'a> {
    pub const VT_SHAPE: ::flatbuffers::VOffsetT = 4;
    pub const VT_ORIGIN: ::flatbuffers::VOffsetT = 6;
    pub const VT_DIRECTION: ::flatbuffers::VOffsetT = 8;
    pub const VT_RADIUS: ::flatbuffers::VOffsetT = 10;
    pub const VT_HEIGHT: ::flatbuffers::VOffsetT = 12;
    pub const VT_INNER_RADIUS: ::flatbuffers::VOffsetT = 14;
    pub const VT_HALF_ANGLE: ::flatbuffers::VOffsetT = 16;
    pub const VT_HALF_WIDTH: ::flatbuffers::VOffsetT = 18;

    #[inline]
    pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
        HazardVolume { _tab: table }
    }
    #[allow(unused_mut)]
    pub fn create<
        'bldr: 'args,
        'args: 'mut_bldr,
        'mut_bldr,
        A: ::flatbuffers::Allocator + 'bldr,
    >(
        _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
        args: &'args HazardVolumeArgs<'args>,
    ) -> ::flatbuffers::WIPOffset<HazardVolume<'bldr>> {
        let mut builder = HazardVolumeBuilder::new(_fbb);
        builder.add_half_width(args.half_width);
        builder.add_half_angle(args.half_angle);
        builder.add_inner_radius(args.inner_radius);
        builder.add_height(args.height);
        builder.add_radius(args.radius);
        if let Some(x) = args.direction {
            builder.add_direction(x);
        }
        if let Some(x) = args.origin {
            builder.add_origin(x);
        }
        builder.add_shape(args.shape);
        builder.finish()
    }

    #[inline]
    pub fn shape(&self) -> HazardShape {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<HazardShape>(HazardVolume::VT_SHAPE, Some(HazardShape::Unknown))
                .unwrap()
        }
    }
    /// The region's anchor in world space. For `Cone` and `Line` it is the apex or the
    /// start of the strip; for `Disc` and `Ring` it is the centre.
    #[inline]
    pub fn origin(&self) -> Option<&'a Vec3> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe { self._tab.get::<Vec3>(HazardVolume::VT_ORIGIN, None) }
    }
    /// Where the region points, as a unit vector the server has already normalised. It is
    /// **fixed by the server**, and for an aimed move it is fixed once — see
    /// `EncounterMove.aim`. A receiver never re-aims it at a player.
    #[inline]
    pub fn direction(&self) -> Option<&'a Vec3> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe { self._tab.get::<Vec3>(HazardVolume::VT_DIRECTION, None) }
    }
    /// Reach, in blocks: the cone's length, the strip's length, or the outer radius of a
    /// disc or ring.
    #[inline]
    pub fn radius(&self) -> f32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<f32>(HazardVolume::VT_RADIUS, Some(0.0))
                .unwrap()
        }
    }
    /// Vertical extent, in blocks, centred on `origin`. A ground wave is short and a
    /// leap's landing is not; a receiver that assumed a floor plane would draw a danger
    /// the wrong height.
    #[inline]
    pub fn height(&self) -> f32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<f32>(HazardVolume::VT_HEIGHT, Some(0.0))
                .unwrap()
        }
    }
    /// Inner radius, in blocks. `Ring` only.
    #[inline]
    pub fn inner_radius(&self) -> f32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<f32>(HazardVolume::VT_INNER_RADIUS, Some(0.0))
                .unwrap()
        }
    }
    /// Half the cone's opening angle, in radians. `Cone` only.
    #[inline]
    pub fn half_angle(&self) -> f32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<f32>(HazardVolume::VT_HALF_ANGLE, Some(0.0))
                .unwrap()
        }
    }
    /// Half the strip's width, in blocks. `Line` only.
    #[inline]
    pub fn half_width(&self) -> f32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<f32>(HazardVolume::VT_HALF_WIDTH, Some(0.0))
                .unwrap()
        }
    }
}

impl ::flatbuffers::Verifiable for HazardVolume<'_> {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        v.visit_table(pos)?
            .visit_field::<HazardShape>("shape", Self::VT_SHAPE, false)?
            .visit_field::<Vec3>("origin", Self::VT_ORIGIN, false)?
            .visit_field::<Vec3>("direction", Self::VT_DIRECTION, false)?
            .visit_field::<f32>("radius", Self::VT_RADIUS, false)?
            .visit_field::<f32>("height", Self::VT_HEIGHT, false)?
            .visit_field::<f32>("inner_radius", Self::VT_INNER_RADIUS, false)?
            .visit_field::<f32>("half_angle", Self::VT_HALF_ANGLE, false)?
            .visit_field::<f32>("half_width", Self::VT_HALF_WIDTH, false)?
            .finish();
        Ok(())
    }
}
pub struct HazardVolumeArgs<'a> {
    pub shape: HazardShape,
    pub origin: Option<&'a Vec3>,
    pub direction: Option<&'a Vec3>,
    pub radius: f32,
    pub height: f32,
    pub inner_radius: f32,
    pub half_angle: f32,
    pub half_width: f32,
}
impl<'a> Default for HazardVolumeArgs<'a> {
    #[inline]
    fn default() -> Self {
        HazardVolumeArgs {
            shape: HazardShape::Unknown,
            origin: None,
            direction: None,
            radius: 0.0,
            height: 0.0,
            inner_radius: 0.0,
            half_angle: 0.0,
            half_width: 0.0,
        }
    }
}

pub struct HazardVolumeBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
    fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> HazardVolumeBuilder<'a, 'b, A> {
    #[inline]
    pub fn add_shape(&mut self, shape: HazardShape) {
        self.fbb_
            .push_slot::<HazardShape>(HazardVolume::VT_SHAPE, shape, HazardShape::Unknown);
    }
    #[inline]
    pub fn add_origin(&mut self, origin: &Vec3) {
        self.fbb_
            .push_slot_always::<&Vec3>(HazardVolume::VT_ORIGIN, origin);
    }
    #[inline]
    pub fn add_direction(&mut self, direction: &Vec3) {
        self.fbb_
            .push_slot_always::<&Vec3>(HazardVolume::VT_DIRECTION, direction);
    }
    #[inline]
    pub fn add_radius(&mut self, radius: f32) {
        self.fbb_
            .push_slot::<f32>(HazardVolume::VT_RADIUS, radius, 0.0);
    }
    #[inline]
    pub fn add_height(&mut self, height: f32) {
        self.fbb_
            .push_slot::<f32>(HazardVolume::VT_HEIGHT, height, 0.0);
    }
    #[inline]
    pub fn add_inner_radius(&mut self, inner_radius: f32) {
        self.fbb_
            .push_slot::<f32>(HazardVolume::VT_INNER_RADIUS, inner_radius, 0.0);
    }
    #[inline]
    pub fn add_half_angle(&mut self, half_angle: f32) {
        self.fbb_
            .push_slot::<f32>(HazardVolume::VT_HALF_ANGLE, half_angle, 0.0);
    }
    #[inline]
    pub fn add_half_width(&mut self, half_width: f32) {
        self.fbb_
            .push_slot::<f32>(HazardVolume::VT_HALF_WIDTH, half_width, 0.0);
    }
    #[inline]
    pub fn new(
        _fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    ) -> HazardVolumeBuilder<'a, 'b, A> {
        let start = _fbb.start_table();
        HazardVolumeBuilder {
            fbb_: _fbb,
            start_: start,
        }
    }
    #[inline]
    pub fn finish(self) -> ::flatbuffers::WIPOffset<HazardVolume<'a>> {
        let o = self.fbb_.end_table(self.start_);
        ::flatbuffers::WIPOffset::new(o.value())
    }
}

impl ::core::fmt::Debug for HazardVolume<'_> {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        let mut ds = f.debug_struct("HazardVolume");
        ds.field("shape", &self.shape());
        ds.field("origin", &self.origin());
        ds.field("direction", &self.direction());
        ds.field("radius", &self.radius());
        ds.field("height", &self.height());
        ds.field("inner_radius", &self.inner_radius());
        ds.field("half_angle", &self.half_angle());
        ds.field("half_width", &self.half_width());
        ds.finish()
    }
}
