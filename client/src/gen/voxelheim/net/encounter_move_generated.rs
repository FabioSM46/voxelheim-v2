// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
pub enum EncounterMoveOffset {}
#[derive(Copy, Clone, PartialEq)]

/// One announced move of one boss, as the server holds it this tick. Server -> client.
///
/// **Superseding state, not an event.** Every frame carries the encounter's live moves
/// entire, so there is nothing to merge, nothing to replay and no ordering to preserve
/// across frames. A client that has been away — the frame was dropped, the player was
/// out of the arena, the session reconnected — is correct again on the next frame it
/// receives, which is the late-snapshot behaviour this contract requires everywhere else.
///
/// **A move is identified by `move_instance_id`, and that is what makes a late frame
/// safe.** The id is unique among the encounter's live instances and is never reused
/// inside one encounter instance, so a receiver holding a stale instance can tell it from
/// a new one that happens to be the same `kind`. Two bites in a row are two ids.
///
/// **Timing is in server ticks, and they are the same ticks the client already holds.**
/// `phase_started_tick` and `phase_ticks` are counted at `ServerWelcome.tick_rate` and
/// share `EntitySnapshot.tick`'s wrapping, so a receiver measures a telegraph against the
/// snapshot stream rather than against a local clock it started when a frame arrived.
/// **The damage window is these ticks**, not an animation's length: a presentation that
/// ran long or short must not move when the danger begins.
///
/// **Aimed or targeted, never both.** `target_entity_id` names a player the move is
/// tracking; `aim` states a direction fixed before release. A move that has locked its
/// direction sends `aim` and clears the target, which is exactly what makes a charge
/// dodgeable by stepping aside — the contract says the lane will not follow.
///
/// Decoder invariants:
///   - `move_instance_id` is non-zero, and no two live moves of one encounter share one
///   - `kind` and `phase` are known non-zero members
///   - `phase_ticks` is non-zero: a phase with no duration is not a phase a client can
///     present, and one that lasted no ticks would already be over
///   - `hazards` is present. Empty is legal and ordinary — a `Recovery` endangers
///     nothing — and at most 16 entries, which is more sectors than the widest ritual
///     announces. Every entry satisfies `HazardVolume`
///   - `aim` is present exactly when `target_entity_id` is zero, and when present every
///     component is finite and it is not the zero vector. A move that names neither a
///     target nor a direction points nowhere
///   - `pulse_total` is non-zero exactly when `phase` is `Channel`, and `pulse_index` is
///     strictly less than `pulse_total` there. Outside `Channel` both are zero
///   - `interruptible` is only ever true on a `Channel` phase. Nothing else in this
///     contract can be interrupted, and a true here on a swing would promise a player an
///     escape the server does not honour
///   - `ended` is read through its zero member and never fails a frame. A non-zero value
///     a receiver cannot name still ends the move
pub struct EncounterMove<'a> {
    pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for EncounterMove<'a> {
    type Inner = EncounterMove<'a>;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        Self {
            _tab: unsafe { ::flatbuffers::Table::new(buf, loc) },
        }
    }
}

impl<'a> EncounterMove<'a> {
    pub const VT_MOVE_INSTANCE_ID: ::flatbuffers::VOffsetT = 4;
    pub const VT_KIND: ::flatbuffers::VOffsetT = 6;
    pub const VT_PHASE: ::flatbuffers::VOffsetT = 8;
    pub const VT_PHASE_STARTED_TICK: ::flatbuffers::VOffsetT = 10;
    pub const VT_PHASE_TICKS: ::flatbuffers::VOffsetT = 12;
    pub const VT_TARGET_ENTITY_ID: ::flatbuffers::VOffsetT = 14;
    pub const VT_AIM: ::flatbuffers::VOffsetT = 16;
    pub const VT_HAZARDS: ::flatbuffers::VOffsetT = 18;
    pub const VT_PULSE_INDEX: ::flatbuffers::VOffsetT = 20;
    pub const VT_PULSE_TOTAL: ::flatbuffers::VOffsetT = 22;
    pub const VT_INTERRUPTIBLE: ::flatbuffers::VOffsetT = 24;
    pub const VT_ENDED: ::flatbuffers::VOffsetT = 26;
    pub const VT_COMBO_STEP: ::flatbuffers::VOffsetT = 28;
    pub const VT_COMBO_TOTAL: ::flatbuffers::VOffsetT = 30;

    #[inline]
    pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
        EncounterMove { _tab: table }
    }
    #[allow(unused_mut)]
    pub fn create<
        'bldr: 'args,
        'args: 'mut_bldr,
        'mut_bldr,
        A: ::flatbuffers::Allocator + 'bldr,
    >(
        _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
        args: &'args EncounterMoveArgs<'args>,
    ) -> ::flatbuffers::WIPOffset<EncounterMove<'bldr>> {
        let mut builder = EncounterMoveBuilder::new(_fbb);
        builder.add_target_entity_id(args.target_entity_id);
        builder.add_move_instance_id(args.move_instance_id);
        if let Some(x) = args.hazards {
            builder.add_hazards(x);
        }
        if let Some(x) = args.aim {
            builder.add_aim(x);
        }
        builder.add_phase_ticks(args.phase_ticks);
        builder.add_phase_started_tick(args.phase_started_tick);
        builder.add_combo_total(args.combo_total);
        builder.add_combo_step(args.combo_step);
        builder.add_ended(args.ended);
        builder.add_interruptible(args.interruptible);
        builder.add_pulse_total(args.pulse_total);
        builder.add_pulse_index(args.pulse_index);
        builder.add_phase(args.phase);
        builder.add_kind(args.kind);
        builder.finish()
    }

    #[inline]
    pub fn move_instance_id(&self) -> u64 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u64>(EncounterMove::VT_MOVE_INSTANCE_ID, Some(0))
                .unwrap()
        }
    }
    #[inline]
    pub fn kind(&self) -> EncounterMoveKind {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<EncounterMoveKind>(EncounterMove::VT_KIND, Some(EncounterMoveKind::Unknown))
                .unwrap()
        }
    }
    #[inline]
    pub fn phase(&self) -> MovePhase {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<MovePhase>(EncounterMove::VT_PHASE, Some(MovePhase::Unknown))
                .unwrap()
        }
    }
    /// The server tick this phase began at, at `ServerWelcome.tick_rate`. Wraps as
    /// `EntitySnapshot.tick` does.
    #[inline]
    pub fn phase_started_tick(&self) -> u32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u32>(EncounterMove::VT_PHASE_STARTED_TICK, Some(0))
                .unwrap()
        }
    }
    /// How many ticks this phase lasts. **A count, never a deadline** — the same rule
    /// `PlayerVitals.respawn_ticks` states: a client holds it unchanged while frames are
    /// absent rather than extrapolating an end it was not told about.
    #[inline]
    pub fn phase_ticks(&self) -> u32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u32>(EncounterMove::VT_PHASE_TICKS, Some(0))
                .unwrap()
        }
    }
    /// The player this move is tracking, or zero when it has fixed a direction instead.
    /// Presentation only, exactly as `MobState.target_entity_id` is.
    #[inline]
    pub fn target_entity_id(&self) -> u64 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u64>(EncounterMove::VT_TARGET_ENTITY_ID, Some(0))
                .unwrap()
        }
    }
    /// The direction the move has locked, present exactly when `target_entity_id` is zero.
    /// A unit vector the server normalised, and it does not move again for this instance.
    #[inline]
    pub fn aim(&self) -> Option<&'a Vec3> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe { self._tab.get::<Vec3>(EncounterMove::VT_AIM, None) }
    }
    /// Every region this move endangers in its current phase. Announced in `Telegraph`,
    /// live in `Release` and in each `Channel` pulse, and normally empty in `Recovery`.
    #[inline]
    pub fn hazards(
        &self,
    ) -> Option<::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<HazardVolume<'a>>>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab.get::<::flatbuffers::ForwardsUOffset<
                ::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<HazardVolume>>,
            >>(EncounterMove::VT_HAZARDS, None)
        }
    }
    /// Which pulse of a channel is next, counted from zero. Zero outside `Channel`.
    #[inline]
    pub fn pulse_index(&self) -> u8 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u8>(EncounterMove::VT_PULSE_INDEX, Some(0))
                .unwrap()
        }
    }
    /// How many pulses the channel has. Zero outside `Channel`.
    #[inline]
    pub fn pulse_total(&self) -> u8 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u8>(EncounterMove::VT_PULSE_TOTAL, Some(0))
                .unwrap()
        }
    }
    /// Whether the server will honour an interrupt against this instance. **It grants no
    /// ability**: what can interrupt is whatever the server already accepts, and a false
    /// here is the ordinary case rather than a failure.
    #[inline]
    pub fn interruptible(&self) -> bool {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<bool>(EncounterMove::VT_INTERRUPTIBLE, Some(false))
                .unwrap()
        }
    }
    /// Why this instance stopped, or `Unknown` while it is still running.
    #[inline]
    pub fn ended(&self) -> MoveEnd {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<MoveEnd>(EncounterMove::VT_ENDED, Some(MoveEnd::Unknown))
                .unwrap()
        }
    }
    /// V38: physical combination position, counted from one. 0/0 means an ordinary
    /// independent move. Otherwise 1 <= combo_step <= combo_total <= 3. BiteAndTear
    /// requires total 2; ThreeTolls requires total 3 (left cut, right cut, thrust).
    /// PrisonerClaws reserves totals 2 or 3 for alternating blows; all other kinds
    /// require 0/0. A combo never uses Channel, pulses or interruptibility.
    /// Each blow has a NEW move_instance_id, with this pair immutable for that instance
    /// across its telegraph, release, recovery and ending. A late snapshot is complete:
    /// receivers never count previous moves to reconstruct this position.
    #[inline]
    pub fn combo_step(&self) -> u8 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u8>(EncounterMove::VT_COMBO_STEP, Some(0))
                .unwrap()
        }
    }
    #[inline]
    pub fn combo_total(&self) -> u8 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u8>(EncounterMove::VT_COMBO_TOTAL, Some(0))
                .unwrap()
        }
    }
}

impl ::flatbuffers::Verifiable for EncounterMove<'_> {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        v.visit_table(pos)?
            .visit_field::<u64>("move_instance_id", Self::VT_MOVE_INSTANCE_ID, false)?
            .visit_field::<EncounterMoveKind>("kind", Self::VT_KIND, false)?
            .visit_field::<MovePhase>("phase", Self::VT_PHASE, false)?
            .visit_field::<u32>("phase_started_tick", Self::VT_PHASE_STARTED_TICK, false)?
            .visit_field::<u32>("phase_ticks", Self::VT_PHASE_TICKS, false)?
            .visit_field::<u64>("target_entity_id", Self::VT_TARGET_ENTITY_ID, false)?
            .visit_field::<Vec3>("aim", Self::VT_AIM, false)?
            .visit_field::<::flatbuffers::ForwardsUOffset<
                ::flatbuffers::Vector<'_, ::flatbuffers::ForwardsUOffset<HazardVolume>>,
            >>("hazards", Self::VT_HAZARDS, false)?
            .visit_field::<u8>("pulse_index", Self::VT_PULSE_INDEX, false)?
            .visit_field::<u8>("pulse_total", Self::VT_PULSE_TOTAL, false)?
            .visit_field::<bool>("interruptible", Self::VT_INTERRUPTIBLE, false)?
            .visit_field::<MoveEnd>("ended", Self::VT_ENDED, false)?
            .visit_field::<u8>("combo_step", Self::VT_COMBO_STEP, false)?
            .visit_field::<u8>("combo_total", Self::VT_COMBO_TOTAL, false)?
            .finish();
        Ok(())
    }
}
pub struct EncounterMoveArgs<'a> {
    pub move_instance_id: u64,
    pub kind: EncounterMoveKind,
    pub phase: MovePhase,
    pub phase_started_tick: u32,
    pub phase_ticks: u32,
    pub target_entity_id: u64,
    pub aim: Option<&'a Vec3>,
    pub hazards: Option<
        ::flatbuffers::WIPOffset<
            ::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<HazardVolume<'a>>>,
        >,
    >,
    pub pulse_index: u8,
    pub pulse_total: u8,
    pub interruptible: bool,
    pub ended: MoveEnd,
    pub combo_step: u8,
    pub combo_total: u8,
}
impl<'a> Default for EncounterMoveArgs<'a> {
    #[inline]
    fn default() -> Self {
        EncounterMoveArgs {
            move_instance_id: 0,
            kind: EncounterMoveKind::Unknown,
            phase: MovePhase::Unknown,
            phase_started_tick: 0,
            phase_ticks: 0,
            target_entity_id: 0,
            aim: None,
            hazards: None,
            pulse_index: 0,
            pulse_total: 0,
            interruptible: false,
            ended: MoveEnd::Unknown,
            combo_step: 0,
            combo_total: 0,
        }
    }
}

pub struct EncounterMoveBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
    fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> EncounterMoveBuilder<'a, 'b, A> {
    #[inline]
    pub fn add_move_instance_id(&mut self, move_instance_id: u64) {
        self.fbb_
            .push_slot::<u64>(EncounterMove::VT_MOVE_INSTANCE_ID, move_instance_id, 0);
    }
    #[inline]
    pub fn add_kind(&mut self, kind: EncounterMoveKind) {
        self.fbb_.push_slot::<EncounterMoveKind>(
            EncounterMove::VT_KIND,
            kind,
            EncounterMoveKind::Unknown,
        );
    }
    #[inline]
    pub fn add_phase(&mut self, phase: MovePhase) {
        self.fbb_
            .push_slot::<MovePhase>(EncounterMove::VT_PHASE, phase, MovePhase::Unknown);
    }
    #[inline]
    pub fn add_phase_started_tick(&mut self, phase_started_tick: u32) {
        self.fbb_
            .push_slot::<u32>(EncounterMove::VT_PHASE_STARTED_TICK, phase_started_tick, 0);
    }
    #[inline]
    pub fn add_phase_ticks(&mut self, phase_ticks: u32) {
        self.fbb_
            .push_slot::<u32>(EncounterMove::VT_PHASE_TICKS, phase_ticks, 0);
    }
    #[inline]
    pub fn add_target_entity_id(&mut self, target_entity_id: u64) {
        self.fbb_
            .push_slot::<u64>(EncounterMove::VT_TARGET_ENTITY_ID, target_entity_id, 0);
    }
    #[inline]
    pub fn add_aim(&mut self, aim: &Vec3) {
        self.fbb_
            .push_slot_always::<&Vec3>(EncounterMove::VT_AIM, aim);
    }
    #[inline]
    pub fn add_hazards(
        &mut self,
        hazards: ::flatbuffers::WIPOffset<
            ::flatbuffers::Vector<'b, ::flatbuffers::ForwardsUOffset<HazardVolume<'b>>>,
        >,
    ) {
        self.fbb_
            .push_slot_always::<::flatbuffers::WIPOffset<_>>(EncounterMove::VT_HAZARDS, hazards);
    }
    #[inline]
    pub fn add_pulse_index(&mut self, pulse_index: u8) {
        self.fbb_
            .push_slot::<u8>(EncounterMove::VT_PULSE_INDEX, pulse_index, 0);
    }
    #[inline]
    pub fn add_pulse_total(&mut self, pulse_total: u8) {
        self.fbb_
            .push_slot::<u8>(EncounterMove::VT_PULSE_TOTAL, pulse_total, 0);
    }
    #[inline]
    pub fn add_interruptible(&mut self, interruptible: bool) {
        self.fbb_
            .push_slot::<bool>(EncounterMove::VT_INTERRUPTIBLE, interruptible, false);
    }
    #[inline]
    pub fn add_ended(&mut self, ended: MoveEnd) {
        self.fbb_
            .push_slot::<MoveEnd>(EncounterMove::VT_ENDED, ended, MoveEnd::Unknown);
    }
    #[inline]
    pub fn add_combo_step(&mut self, combo_step: u8) {
        self.fbb_
            .push_slot::<u8>(EncounterMove::VT_COMBO_STEP, combo_step, 0);
    }
    #[inline]
    pub fn add_combo_total(&mut self, combo_total: u8) {
        self.fbb_
            .push_slot::<u8>(EncounterMove::VT_COMBO_TOTAL, combo_total, 0);
    }
    #[inline]
    pub fn new(
        _fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    ) -> EncounterMoveBuilder<'a, 'b, A> {
        let start = _fbb.start_table();
        EncounterMoveBuilder {
            fbb_: _fbb,
            start_: start,
        }
    }
    #[inline]
    pub fn finish(self) -> ::flatbuffers::WIPOffset<EncounterMove<'a>> {
        let o = self.fbb_.end_table(self.start_);
        ::flatbuffers::WIPOffset::new(o.value())
    }
}

impl ::core::fmt::Debug for EncounterMove<'_> {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        let mut ds = f.debug_struct("EncounterMove");
        ds.field("move_instance_id", &self.move_instance_id());
        ds.field("kind", &self.kind());
        ds.field("phase", &self.phase());
        ds.field("phase_started_tick", &self.phase_started_tick());
        ds.field("phase_ticks", &self.phase_ticks());
        ds.field("target_entity_id", &self.target_entity_id());
        ds.field("aim", &self.aim());
        ds.field("hazards", &self.hazards());
        ds.field("pulse_index", &self.pulse_index());
        ds.field("pulse_total", &self.pulse_total());
        ds.field("interruptible", &self.interruptible());
        ds.field("ended", &self.ended());
        ds.field("combo_step", &self.combo_step());
        ds.field("combo_total", &self.combo_total());
        ds.finish()
    }
}
