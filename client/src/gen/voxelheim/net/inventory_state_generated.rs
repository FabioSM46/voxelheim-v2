// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
pub enum InventoryStateOffset {}
#[derive(Copy, Clone, PartialEq)]

/// Everything the player is carrying, as the server holds it.
///
/// Sent whole whenever it changes: there is no delta form and no partial update,
/// so the client replaces its copy rather than merging into it. The client never
/// adds, removes, reorders or splits a stack on its own — what is in an
/// inventory is a gameplay outcome, and outcomes belong to the server. There is
/// no client→server counterpart to this message, for the same reason there is no
/// client-sent position.
///
/// **Encoding**: `stacks` is a slot-indexed flat vector of `(item_id, count)`
/// pairs — `stacks[2i]` is slot `i`'s item id and `stacks[2i+1]` is its count.
/// It contains exactly `ServerWelcome.inventory_slots` pairs (therefore exactly
/// twice that many `ushort` scalars). Flat rather than a vector of tables for the
/// reason `ChunkData.runs` is flat: a table per slot costs an offset each, and
/// this message is resent in full every time a single item moves.
///
/// Invariants a decoder must enforce:
///   - `stacks` has an even length
///   - its pair count is exactly `ServerWelcome.inventory_slots`; because that
///     value is non-zero, an absent or empty vector is a protocol error
///   - an empty slot is exactly `(0, 0)`: item id 0 is reserved for no item, and
///     neither `(0, nonzero)` nor `(nonzero, 0)` is legal
///   - an item id may appear in more than one slot; slot position, not item id,
///     identifies a stack
///
/// **Durability rides in two parallel vectors rather than inside `stacks`.** The pair
/// encoding above is append-only like every other field here, so it could not grow a
/// third scalar per slot without changing the meaning of every index already on the
/// wire. Two more slot-indexed vectors leave the existing pairs untouched, and they
/// let a client draw a durability ratio without owning an item-stat registry: the
/// maximum arrives beside the current value, so nothing has to be looked up.
///
/// Further invariants a decoder must enforce, for `durability[i]` and
/// `max_durability[i]` describing slot `i`:
///   - each vector holds exactly `ServerWelcome.inventory_slots` entries, the same
///     count as `stacks` holds pairs
///   - `(0, 0)` is a slot holding nothing, or holding something that does not wear
///     out; a resource stack is always exactly this
///   - otherwise `max_durability` is non-zero, `durability` is at most
///     `max_durability`, the slot's item id is non-zero, and its count is exactly 1 —
///     a durable item is a single whole item that never stacks or splits
///   - a `durability` of zero under a non-zero `max_durability` is **legal** and means
///     the item is worn out: unusable, still carried, still in its slot. Zero
///     durability is not an empty slot and never becomes one
pub struct InventoryState<'a> {
    pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for InventoryState<'a> {
    type Inner = InventoryState<'a>;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        Self {
            _tab: unsafe { ::flatbuffers::Table::new(buf, loc) },
        }
    }
}

impl<'a> InventoryState<'a> {
    pub const VT_STACKS: ::flatbuffers::VOffsetT = 4;
    pub const VT_DURABILITY: ::flatbuffers::VOffsetT = 6;
    pub const VT_MAX_DURABILITY: ::flatbuffers::VOffsetT = 8;
    pub const VT_SILVER: ::flatbuffers::VOffsetT = 10;

    #[inline]
    pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
        InventoryState { _tab: table }
    }
    #[allow(unused_mut)]
    pub fn create<
        'bldr: 'args,
        'args: 'mut_bldr,
        'mut_bldr,
        A: ::flatbuffers::Allocator + 'bldr,
    >(
        _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
        args: &'args InventoryStateArgs<'args>,
    ) -> ::flatbuffers::WIPOffset<InventoryState<'bldr>> {
        let mut builder = InventoryStateBuilder::new(_fbb);
        builder.add_silver(args.silver);
        if let Some(x) = args.max_durability {
            builder.add_max_durability(x);
        }
        if let Some(x) = args.durability {
            builder.add_durability(x);
        }
        if let Some(x) = args.stacks {
            builder.add_stacks(x);
        }
        builder.finish()
    }

    #[inline]
    pub fn stacks(&self) -> Option<::flatbuffers::Vector<'a, u16>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, u16>>>(
                    InventoryState::VT_STACKS,
                    None,
                )
        }
    }
    /// Current durability per slot, slot-indexed. See the invariants above.
    #[inline]
    pub fn durability(&self) -> Option<::flatbuffers::Vector<'a, u16>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, u16>>>(
                    InventoryState::VT_DURABILITY,
                    None,
                )
        }
    }
    /// Maximum durability per slot, slot-indexed. Zero means the slot's contents do not
    /// wear out at all, which is every resource and every empty slot.
    #[inline]
    pub fn max_durability(&self) -> Option<::flatbuffers::Vector<'a, u16>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, u16>>>(
                    InventoryState::VT_MAX_DURABILITY,
                    None,
                )
        }
    }
    /// Currency in this character's authoritative purse. It occupies no slot.
    #[inline]
    pub fn silver(&self) -> u32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u32>(InventoryState::VT_SILVER, Some(0))
                .unwrap()
        }
    }
}

impl ::flatbuffers::Verifiable for InventoryState<'_> {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        v.visit_table(pos)?
            .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, u16>>>(
                "stacks",
                Self::VT_STACKS,
                false,
            )?
            .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, u16>>>(
                "durability",
                Self::VT_DURABILITY,
                false,
            )?
            .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, u16>>>(
                "max_durability",
                Self::VT_MAX_DURABILITY,
                false,
            )?
            .visit_field::<u32>("silver", Self::VT_SILVER, false)?
            .finish();
        Ok(())
    }
}
pub struct InventoryStateArgs<'a> {
    pub stacks: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, u16>>>,
    pub durability: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, u16>>>,
    pub max_durability: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, u16>>>,
    pub silver: u32,
}
impl<'a> Default for InventoryStateArgs<'a> {
    #[inline]
    fn default() -> Self {
        InventoryStateArgs {
            stacks: None,
            durability: None,
            max_durability: None,
            silver: 0,
        }
    }
}

pub struct InventoryStateBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
    fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> InventoryStateBuilder<'a, 'b, A> {
    #[inline]
    pub fn add_stacks(&mut self, stacks: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b, u16>>) {
        self.fbb_
            .push_slot_always::<::flatbuffers::WIPOffset<_>>(InventoryState::VT_STACKS, stacks);
    }
    #[inline]
    pub fn add_durability(
        &mut self,
        durability: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b, u16>>,
    ) {
        self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(
            InventoryState::VT_DURABILITY,
            durability,
        );
    }
    #[inline]
    pub fn add_max_durability(
        &mut self,
        max_durability: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b, u16>>,
    ) {
        self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(
            InventoryState::VT_MAX_DURABILITY,
            max_durability,
        );
    }
    #[inline]
    pub fn add_silver(&mut self, silver: u32) {
        self.fbb_
            .push_slot::<u32>(InventoryState::VT_SILVER, silver, 0);
    }
    #[inline]
    pub fn new(
        _fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    ) -> InventoryStateBuilder<'a, 'b, A> {
        let start = _fbb.start_table();
        InventoryStateBuilder {
            fbb_: _fbb,
            start_: start,
        }
    }
    #[inline]
    pub fn finish(self) -> ::flatbuffers::WIPOffset<InventoryState<'a>> {
        let o = self.fbb_.end_table(self.start_);
        ::flatbuffers::WIPOffset::new(o.value())
    }
}

impl ::core::fmt::Debug for InventoryState<'_> {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        let mut ds = f.debug_struct("InventoryState");
        ds.field("stacks", &self.stacks());
        ds.field("durability", &self.durability());
        ds.field("max_durability", &self.max_durability());
        ds.field("silver", &self.silver());
        ds.finish()
    }
}
