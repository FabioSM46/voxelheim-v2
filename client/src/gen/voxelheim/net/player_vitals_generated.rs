// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
pub enum PlayerVitalsOffset {}
#[derive(Copy, Clone, PartialEq)]

/// The recipient's own health and life state, as the server holds them.
///
/// Per-recipient, and carried inside `EntitySnapshot` so that health arrives on the
/// same superseding stream as every other authoritative fact. A dropped snapshot is
/// harmless: the next one carries the complete answer, so there is nothing to merge
/// and nothing to replay. There is no client -> server counterpart, for the same
/// reason there is no client-sent position: what a player's health *is* is a gameplay
/// outcome, and outcomes belong to the server.
///
/// Invariants a decoder must enforce, because a receiver divides by one of these and
/// branches on another:
///   - `max_health` is non-zero — a health bar drawn as `health / max_health` divides
///     by it, and an honestly buggy server reaches that division as easily as a
///     hostile one
///   - `health` never exceeds `max_health`
///   - `life_state` is a known non-zero member; `Unknown` is the absent-field case
///   - an `Alive` player has positive health
///   - only a `Dead` player may carry a non-zero `respawn_ticks`
pub struct PlayerVitals<'a> {
    pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for PlayerVitals<'a> {
    type Inner = PlayerVitals<'a>;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        Self {
            _tab: unsafe { ::flatbuffers::Table::new(buf, loc) },
        }
    }
}

impl<'a> PlayerVitals<'a> {
    pub const VT_HEALTH: ::flatbuffers::VOffsetT = 4;
    pub const VT_MAX_HEALTH: ::flatbuffers::VOffsetT = 6;
    pub const VT_LIFE_STATE: ::flatbuffers::VOffsetT = 8;
    pub const VT_RESPAWN_TICKS: ::flatbuffers::VOffsetT = 10;
    pub const VT_INVULNERABLE: ::flatbuffers::VOffsetT = 12;

    #[inline]
    pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
        PlayerVitals { _tab: table }
    }
    #[allow(unused_mut)]
    pub fn create<
        'bldr: 'args,
        'args: 'mut_bldr,
        'mut_bldr,
        A: ::flatbuffers::Allocator + 'bldr,
    >(
        _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
        args: &'args PlayerVitalsArgs,
    ) -> ::flatbuffers::WIPOffset<PlayerVitals<'bldr>> {
        let mut builder = PlayerVitalsBuilder::new(_fbb);
        builder.add_respawn_ticks(args.respawn_ticks);
        builder.add_max_health(args.max_health);
        builder.add_health(args.health);
        builder.add_invulnerable(args.invulnerable);
        builder.add_life_state(args.life_state);
        builder.finish()
    }

    /// Current health, in the same units as `max_health`. Zero is legal and means the
    /// player is dead — the server's transition to `Dead` is what makes it so, not the
    /// number.
    #[inline]
    pub fn health(&self) -> u16 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u16>(PlayerVitals::VT_HEALTH, Some(0))
                .unwrap()
        }
    }
    /// Maximum health. Non-zero, always: it is the denominator of every health display.
    #[inline]
    pub fn max_health(&self) -> u16 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u16>(PlayerVitals::VT_MAX_HEALTH, Some(0))
                .unwrap()
        }
    }
    #[inline]
    pub fn life_state(&self) -> LifeState {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<LifeState>(PlayerVitals::VT_LIFE_STATE, Some(LifeState::Unknown))
                .unwrap()
        }
    }
    /// Server ticks remaining before the server respawns this player, counted at
    /// `ServerWelcome.tick_rate`. Zero for a living player. **A count, never a
    /// deadline**: the client converts it for display and holds the value unchanged
    /// while snapshots are absent, exactly as it holds an entity's last position rather
    /// than extrapolating one.
    #[inline]
    pub fn respawn_ticks(&self) -> u32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u32>(PlayerVitals::VT_RESPAWN_TICKS, Some(0))
                .unwrap()
        }
    }
    /// Whether the server is currently refusing damage to this player. The server owns
    /// the timer; this is the answer, not a duration to run down locally.
    #[inline]
    pub fn invulnerable(&self) -> bool {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<bool>(PlayerVitals::VT_INVULNERABLE, Some(false))
                .unwrap()
        }
    }
}

impl ::flatbuffers::Verifiable for PlayerVitals<'_> {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        v.visit_table(pos)?
            .visit_field::<u16>("health", Self::VT_HEALTH, false)?
            .visit_field::<u16>("max_health", Self::VT_MAX_HEALTH, false)?
            .visit_field::<LifeState>("life_state", Self::VT_LIFE_STATE, false)?
            .visit_field::<u32>("respawn_ticks", Self::VT_RESPAWN_TICKS, false)?
            .visit_field::<bool>("invulnerable", Self::VT_INVULNERABLE, false)?
            .finish();
        Ok(())
    }
}
pub struct PlayerVitalsArgs {
    pub health: u16,
    pub max_health: u16,
    pub life_state: LifeState,
    pub respawn_ticks: u32,
    pub invulnerable: bool,
}
impl<'a> Default for PlayerVitalsArgs {
    #[inline]
    fn default() -> Self {
        PlayerVitalsArgs {
            health: 0,
            max_health: 0,
            life_state: LifeState::Unknown,
            respawn_ticks: 0,
            invulnerable: false,
        }
    }
}

pub struct PlayerVitalsBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
    fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> PlayerVitalsBuilder<'a, 'b, A> {
    #[inline]
    pub fn add_health(&mut self, health: u16) {
        self.fbb_
            .push_slot::<u16>(PlayerVitals::VT_HEALTH, health, 0);
    }
    #[inline]
    pub fn add_max_health(&mut self, max_health: u16) {
        self.fbb_
            .push_slot::<u16>(PlayerVitals::VT_MAX_HEALTH, max_health, 0);
    }
    #[inline]
    pub fn add_life_state(&mut self, life_state: LifeState) {
        self.fbb_.push_slot::<LifeState>(
            PlayerVitals::VT_LIFE_STATE,
            life_state,
            LifeState::Unknown,
        );
    }
    #[inline]
    pub fn add_respawn_ticks(&mut self, respawn_ticks: u32) {
        self.fbb_
            .push_slot::<u32>(PlayerVitals::VT_RESPAWN_TICKS, respawn_ticks, 0);
    }
    #[inline]
    pub fn add_invulnerable(&mut self, invulnerable: bool) {
        self.fbb_
            .push_slot::<bool>(PlayerVitals::VT_INVULNERABLE, invulnerable, false);
    }
    #[inline]
    pub fn new(
        _fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    ) -> PlayerVitalsBuilder<'a, 'b, A> {
        let start = _fbb.start_table();
        PlayerVitalsBuilder {
            fbb_: _fbb,
            start_: start,
        }
    }
    #[inline]
    pub fn finish(self) -> ::flatbuffers::WIPOffset<PlayerVitals<'a>> {
        let o = self.fbb_.end_table(self.start_);
        ::flatbuffers::WIPOffset::new(o.value())
    }
}

impl ::core::fmt::Debug for PlayerVitals<'_> {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        let mut ds = f.debug_struct("PlayerVitals");
        ds.field("health", &self.health());
        ds.field("max_health", &self.max_health());
        ds.field("life_state", &self.life_state());
        ds.field("respawn_ticks", &self.respawn_ticks());
        ds.field("invulnerable", &self.invulnerable());
        ds.finish()
    }
}
