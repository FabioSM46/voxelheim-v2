// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
pub enum ServerWelcomeOffset {}
#[derive(Copy, Clone, PartialEq)]

/// The session is accepted. Every field here is authoritative and settles a
/// parameter the client would otherwise have to guess or hardcode.
///
/// **Decoder invariants.** The client sizes buffers with these values and
/// divides by some of them, so it validates them before use and treats a
/// violation as a protocol error. A zero `tick_rate` or a wrong `chunk_size` is
/// a division by zero or an allocation blowup, and an honestly buggy server
/// reaches that state as easily as a hostile one — trusting the server on
/// gameplay outcomes does not mean trusting it on array bounds:
///
///   - `spawn` components must be finite (no NaN, no infinity)
///   - `tick_rate` must be >= 1
///   - `chunk_size` must be in 1..=40, the ceiling imposed by the 16-bit RLE run
///     length documented in `world.fbs`
///   - `view_distance` must be in 0..=16: the streamed volume grows as
///     `(2 * view_distance + 1)^3` chunks, so the bound is what keeps a single
///     number from asking the client for gigabytes
///   - `inventory_slots` and `hotbar_slots` must be non-zero
///   - `equipment_slots` must be in 1..=8, bounding the trailing equipment layout
///   - `hotbar_slots + equipment_slots` must be <= `inventory_slots`, because the
///     hotbar is the leading subset, equipment is the trailing subset, and the pack
///     occupies the slots between them
///   - `player_token` must be present and exactly 32 bytes; an absent, empty or
///     differently sized vector is a protocol error, rejected the way a zero
///     `tick_rate` is
///   - the world clock is either absent or complete. `day_length_ticks` of zero
///     says this server keeps no clock and the two boundaries below it are not
///     read at all; any other value requires
///     `0 < night_start_ticks < night_end_ticks <= day_length_ticks`, and a
///     welcome that breaks that ordering is refused rather than repaired
pub struct ServerWelcome<'a> {
    pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for ServerWelcome<'a> {
    type Inner = ServerWelcome<'a>;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        Self {
            _tab: unsafe { ::flatbuffers::Table::new(buf, loc) },
        }
    }
}

impl<'a> ServerWelcome<'a> {
    pub const VT_ENTITY_ID: ::flatbuffers::VOffsetT = 4;
    pub const VT_SPAWN: ::flatbuffers::VOffsetT = 6;
    pub const VT_WORLD_SEED: ::flatbuffers::VOffsetT = 8;
    pub const VT_TICK_RATE: ::flatbuffers::VOffsetT = 10;
    pub const VT_CHUNK_SIZE: ::flatbuffers::VOffsetT = 12;
    pub const VT_VIEW_DISTANCE: ::flatbuffers::VOffsetT = 14;
    pub const VT_INVENTORY_SLOTS: ::flatbuffers::VOffsetT = 16;
    pub const VT_HOTBAR_SLOTS: ::flatbuffers::VOffsetT = 18;
    pub const VT_PLAYER_TOKEN: ::flatbuffers::VOffsetT = 20;
    pub const VT_DAY_LENGTH_TICKS: ::flatbuffers::VOffsetT = 22;
    pub const VT_NIGHT_START_TICKS: ::flatbuffers::VOffsetT = 24;
    pub const VT_NIGHT_END_TICKS: ::flatbuffers::VOffsetT = 26;
    pub const VT_EQUIPMENT_SLOTS: ::flatbuffers::VOffsetT = 28;

    #[inline]
    pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
        ServerWelcome { _tab: table }
    }
    #[allow(unused_mut)]
    pub fn create<
        'bldr: 'args,
        'args: 'mut_bldr,
        'mut_bldr,
        A: ::flatbuffers::Allocator + 'bldr,
    >(
        _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
        args: &'args ServerWelcomeArgs<'args>,
    ) -> ::flatbuffers::WIPOffset<ServerWelcome<'bldr>> {
        let mut builder = ServerWelcomeBuilder::new(_fbb);
        builder.add_world_seed(args.world_seed);
        builder.add_entity_id(args.entity_id);
        builder.add_night_end_ticks(args.night_end_ticks);
        builder.add_night_start_ticks(args.night_start_ticks);
        builder.add_day_length_ticks(args.day_length_ticks);
        if let Some(x) = args.player_token {
            builder.add_player_token(x);
        }
        if let Some(x) = args.spawn {
            builder.add_spawn(x);
        }
        builder.add_chunk_size(args.chunk_size);
        builder.add_equipment_slots(args.equipment_slots);
        builder.add_hotbar_slots(args.hotbar_slots);
        builder.add_inventory_slots(args.inventory_slots);
        builder.add_view_distance(args.view_distance);
        builder.add_tick_rate(args.tick_rate);
        builder.finish()
    }

    /// Server-assigned identity for this player's entity. Appears in
    /// `EntitySnapshot` so the client can tell itself apart from everyone else.
    #[inline]
    pub fn entity_id(&self) -> u64 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u64>(ServerWelcome::VT_ENTITY_ID, Some(0))
                .unwrap()
        }
    }
    /// Where the server has placed the player **this session**: the join spawn
    /// for an identity the server is seeing for the first time, the restored
    /// position for one that is returning. Which of the two happened is not
    /// something the client is told and not something it needs — the server
    /// decided either way. The client does not choose this and does not get to
    /// move itself there; it renders where the snapshots say.
    #[inline]
    pub fn spawn(&self) -> Option<&'a Vec3> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe { self._tab.get::<Vec3>(ServerWelcome::VT_SPAWN, None) }
    }
    /// Seed of the world's procedural generation. Sent for diagnostics and future
    /// client-side tooling only: the client never generates terrain from it,
    /// because the server's voxels are the only voxels that exist.
    #[inline]
    pub fn world_seed(&self) -> i64 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<i64>(ServerWelcome::VT_WORLD_SEED, Some(0))
                .unwrap()
        }
    }
    /// Authoritative simulation ticks per second. The client sends `PlayerInput`
    /// at this rate rather than at its frame rate.
    #[inline]
    pub fn tick_rate(&self) -> u8 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u8>(ServerWelcome::VT_TICK_RATE, Some(0))
                .unwrap()
        }
    }
    /// Edge length of a chunk in blocks. Chunks are cubic, so a chunk holds
    /// `chunk_size^3` voxels. Announced once here instead of repeated in every
    /// `ChunkData`.
    #[inline]
    pub fn chunk_size(&self) -> u16 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u16>(ServerWelcome::VT_CHUNK_SIZE, Some(0))
                .unwrap()
        }
    }
    /// Chunk-streaming radius in chunks, centred on the chunk the server believes
    /// the player is in.
    #[inline]
    pub fn view_distance(&self) -> u8 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u8>(ServerWelcome::VT_VIEW_DISTANCE, Some(0))
                .unwrap()
        }
    }
    /// Number of slot pairs in every `InventoryState.stacks` vector this session
    /// receives. The layout is the hotbar first, the pack in the middle, and equipment
    /// last. The server currently announces 40: 9 hotbar, 27 pack and 4 equipment.
    #[inline]
    pub fn inventory_slots(&self) -> u8 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u8>(ServerWelcome::VT_INVENTORY_SLOTS, Some(0))
                .unwrap()
        }
    }
    /// Leading inventory slots the player's hotbar can select. The server
    /// currently announces 9.
    #[inline]
    pub fn hotbar_slots(&self) -> u8 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u8>(ServerWelcome::VT_HOTBAR_SLOTS, Some(0))
                .unwrap()
        }
    }
    /// The identity token for this session. The client stores it and presents it
    /// as `ClientHello.player_token` on its next connection to **this** server.
    ///
    /// **Retired at V7 alongside its counterpart**, and for the same reason: a V7
    /// client's identity comes from the account service that signed its
    /// `ClientHello.session_ticket`, not from a value this server minted. The field
    /// stays where it is and every invariant below still holds for the value a server
    /// puts in it — present, 32 bytes, never the value the client sent unless the
    /// server chose it — because a V6 client on the far end still stores what arrives
    /// here and a V7 server has no reason to send a shorter one. What is retired is
    /// the *sentence above*: presenting this value on a later connection is the V6
    /// handshake, and a V7 server ignores it.
    ///
    /// Present and exactly 32 bytes on every accepted handshake; a decoder treats
    /// any other length as a protocol error, the way it treats a zero `tick_rate`.
    ///
    /// A client whose presented token the server did not recognise receives a
    /// **new** token here rather than the value it sent: the server never adopts a
    /// client-chosen value as an identity key, so every token in circulation is
    /// one the server minted. A client whose token *was* recognised stores
    /// whatever arrives here, which is not required to be the value it sent.
    ///
    /// The same bearer credential, carrying the same three exposures and closing
    /// them the same way. Two are the wire format's own and hold on every
    /// transport: never logged, never displayed, on either side. The third is the
    /// wire itself, and it is closed by the transport rather than by anything a
    /// schema can say — this server's only configuration is encrypted, so the
    /// field is not readable in transit. See `ClientHello.player_token`.
    #[inline]
    pub fn player_token(&self) -> Option<::flatbuffers::Vector<'a, u8>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, u8>>>(
                    ServerWelcome::VT_PLAYER_TOKEN,
                    None,
                )
        }
    }
    /// Length of one full day in server ticks, or **zero for a server that keeps no
    /// clock at all**.
    ///
    /// Zero is not a degenerate day. It is the world as it was before V6, in which
    /// time does not pass and a client draws whatever fixed sky it draws — a legal,
    /// expected value, and the only one that makes the two boundaries below
    /// meaningless. Every other value is a promise about
    /// `EntitySnapshot.tick_of_day`, which is then always less than it.
    ///
    /// Ticks rather than seconds, for the reason every other duration on this wire
    /// is in ticks: the simulation counts them, and integer arithmetic gives both
    /// sides the same answer without a rounding rule to agree on.
    #[inline]
    pub fn day_length_ticks(&self) -> u32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u32>(ServerWelcome::VT_DAY_LENGTH_TICKS, Some(0))
                .unwrap()
        }
    }
    /// The tick of the day at which night begins.
    ///
    /// Read only when `day_length_ticks` is non-zero, and then
    /// `0 < night_start_ticks < night_end_ticks <= day_length_ticks`.
    ///
    /// **Sent rather than left for the client to derive.** A client could compute a
    /// plausible dusk from constants of its own, and the night it drew would then be
    /// its night rather than the server's — which is the night the server's own rules
    /// are written against. There is one clock in this world and these are its
    /// boundaries; a second opinion about them is a disagreement waiting to be seen
    /// on screen.
    #[inline]
    pub fn night_start_ticks(&self) -> u32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u32>(ServerWelcome::VT_NIGHT_START_TICKS, Some(0))
                .unwrap()
        }
    }
    /// The tick of the day at which night ends. Read under the same condition and the
    /// same ordering as `night_start_ticks`, and equal to `day_length_ticks` for a
    /// night that runs to the end of the day.
    #[inline]
    pub fn night_end_ticks(&self) -> u32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u32>(ServerWelcome::VT_NIGHT_END_TICKS, Some(0))
                .unwrap()
        }
    }
    /// Trailing inventory slots reserved for worn equipment. These slots are part of
    /// `inventory_slots`, and the server currently announces four: head, chest, legs and
    /// off-hand, in that slot order at the inventory tail. The client uses this count to
    /// find where the pack ends; it never decides which item is legal in a worn slot.
    #[inline]
    pub fn equipment_slots(&self) -> u8 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u8>(ServerWelcome::VT_EQUIPMENT_SLOTS, Some(0))
                .unwrap()
        }
    }
}

impl ::flatbuffers::Verifiable for ServerWelcome<'_> {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        v.visit_table(pos)?
            .visit_field::<u64>("entity_id", Self::VT_ENTITY_ID, false)?
            .visit_field::<Vec3>("spawn", Self::VT_SPAWN, false)?
            .visit_field::<i64>("world_seed", Self::VT_WORLD_SEED, false)?
            .visit_field::<u8>("tick_rate", Self::VT_TICK_RATE, false)?
            .visit_field::<u16>("chunk_size", Self::VT_CHUNK_SIZE, false)?
            .visit_field::<u8>("view_distance", Self::VT_VIEW_DISTANCE, false)?
            .visit_field::<u8>("inventory_slots", Self::VT_INVENTORY_SLOTS, false)?
            .visit_field::<u8>("hotbar_slots", Self::VT_HOTBAR_SLOTS, false)?
            .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, u8>>>(
                "player_token",
                Self::VT_PLAYER_TOKEN,
                false,
            )?
            .visit_field::<u32>("day_length_ticks", Self::VT_DAY_LENGTH_TICKS, false)?
            .visit_field::<u32>("night_start_ticks", Self::VT_NIGHT_START_TICKS, false)?
            .visit_field::<u32>("night_end_ticks", Self::VT_NIGHT_END_TICKS, false)?
            .visit_field::<u8>("equipment_slots", Self::VT_EQUIPMENT_SLOTS, false)?
            .finish();
        Ok(())
    }
}
pub struct ServerWelcomeArgs<'a> {
    pub entity_id: u64,
    pub spawn: Option<&'a Vec3>,
    pub world_seed: i64,
    pub tick_rate: u8,
    pub chunk_size: u16,
    pub view_distance: u8,
    pub inventory_slots: u8,
    pub hotbar_slots: u8,
    pub player_token: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, u8>>>,
    pub day_length_ticks: u32,
    pub night_start_ticks: u32,
    pub night_end_ticks: u32,
    pub equipment_slots: u8,
}
impl<'a> Default for ServerWelcomeArgs<'a> {
    #[inline]
    fn default() -> Self {
        ServerWelcomeArgs {
            entity_id: 0,
            spawn: None,
            world_seed: 0,
            tick_rate: 0,
            chunk_size: 0,
            view_distance: 0,
            inventory_slots: 0,
            hotbar_slots: 0,
            player_token: None,
            day_length_ticks: 0,
            night_start_ticks: 0,
            night_end_ticks: 0,
            equipment_slots: 0,
        }
    }
}

pub struct ServerWelcomeBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
    fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> ServerWelcomeBuilder<'a, 'b, A> {
    #[inline]
    pub fn add_entity_id(&mut self, entity_id: u64) {
        self.fbb_
            .push_slot::<u64>(ServerWelcome::VT_ENTITY_ID, entity_id, 0);
    }
    #[inline]
    pub fn add_spawn(&mut self, spawn: &Vec3) {
        self.fbb_
            .push_slot_always::<&Vec3>(ServerWelcome::VT_SPAWN, spawn);
    }
    #[inline]
    pub fn add_world_seed(&mut self, world_seed: i64) {
        self.fbb_
            .push_slot::<i64>(ServerWelcome::VT_WORLD_SEED, world_seed, 0);
    }
    #[inline]
    pub fn add_tick_rate(&mut self, tick_rate: u8) {
        self.fbb_
            .push_slot::<u8>(ServerWelcome::VT_TICK_RATE, tick_rate, 0);
    }
    #[inline]
    pub fn add_chunk_size(&mut self, chunk_size: u16) {
        self.fbb_
            .push_slot::<u16>(ServerWelcome::VT_CHUNK_SIZE, chunk_size, 0);
    }
    #[inline]
    pub fn add_view_distance(&mut self, view_distance: u8) {
        self.fbb_
            .push_slot::<u8>(ServerWelcome::VT_VIEW_DISTANCE, view_distance, 0);
    }
    #[inline]
    pub fn add_inventory_slots(&mut self, inventory_slots: u8) {
        self.fbb_
            .push_slot::<u8>(ServerWelcome::VT_INVENTORY_SLOTS, inventory_slots, 0);
    }
    #[inline]
    pub fn add_hotbar_slots(&mut self, hotbar_slots: u8) {
        self.fbb_
            .push_slot::<u8>(ServerWelcome::VT_HOTBAR_SLOTS, hotbar_slots, 0);
    }
    #[inline]
    pub fn add_player_token(
        &mut self,
        player_token: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b, u8>>,
    ) {
        self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(
            ServerWelcome::VT_PLAYER_TOKEN,
            player_token,
        );
    }
    #[inline]
    pub fn add_day_length_ticks(&mut self, day_length_ticks: u32) {
        self.fbb_
            .push_slot::<u32>(ServerWelcome::VT_DAY_LENGTH_TICKS, day_length_ticks, 0);
    }
    #[inline]
    pub fn add_night_start_ticks(&mut self, night_start_ticks: u32) {
        self.fbb_
            .push_slot::<u32>(ServerWelcome::VT_NIGHT_START_TICKS, night_start_ticks, 0);
    }
    #[inline]
    pub fn add_night_end_ticks(&mut self, night_end_ticks: u32) {
        self.fbb_
            .push_slot::<u32>(ServerWelcome::VT_NIGHT_END_TICKS, night_end_ticks, 0);
    }
    #[inline]
    pub fn add_equipment_slots(&mut self, equipment_slots: u8) {
        self.fbb_
            .push_slot::<u8>(ServerWelcome::VT_EQUIPMENT_SLOTS, equipment_slots, 0);
    }
    #[inline]
    pub fn new(
        _fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    ) -> ServerWelcomeBuilder<'a, 'b, A> {
        let start = _fbb.start_table();
        ServerWelcomeBuilder {
            fbb_: _fbb,
            start_: start,
        }
    }
    #[inline]
    pub fn finish(self) -> ::flatbuffers::WIPOffset<ServerWelcome<'a>> {
        let o = self.fbb_.end_table(self.start_);
        ::flatbuffers::WIPOffset::new(o.value())
    }
}

impl ::core::fmt::Debug for ServerWelcome<'_> {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        let mut ds = f.debug_struct("ServerWelcome");
        ds.field("entity_id", &self.entity_id());
        ds.field("spawn", &self.spawn());
        ds.field("world_seed", &self.world_seed());
        ds.field("tick_rate", &self.tick_rate());
        ds.field("chunk_size", &self.chunk_size());
        ds.field("view_distance", &self.view_distance());
        ds.field("inventory_slots", &self.inventory_slots());
        ds.field("hotbar_slots", &self.hotbar_slots());
        ds.field("player_token", &self.player_token());
        ds.field("day_length_ticks", &self.day_length_ticks());
        ds.field("night_start_ticks", &self.night_start_ticks());
        ds.field("night_end_ticks", &self.night_end_ticks());
        ds.field("equipment_slots", &self.equipment_slots());
        ds.finish()
    }
}
