// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
/// One voxel's address in **world** coordinates, measured in blocks.
///
/// World coordinates rather than chunk-plus-local, deliberately. An edit is a
/// point in the world; turning that point into a chunk and an index inside it is
/// the server's job, and the server already owns that conversion. Sending a
/// chunk coordinate plus a local offset would put a second copy of the same
/// arithmetic on the untrusted side, and two copies can disagree — leaving the
/// server to defend against the difference rather than simply perform the
/// conversion once.
///
/// A struct, not a table: three ints, always present wherever it appears, and on
/// the path of every edit. That is the argument that made `ChunkCoord` a struct,
/// and the same price applies — a struct can never gain or reorder a field.
/// Signed, because the world extends in both directions on all three axes, the
/// vertical one included.
///
/// The width is the same `int` as `ChunkCoord`'s, and that is what keeps the two
/// consistent: dividing any `int` block coordinate by a legal `chunk_size`
/// (1..=40, per `ServerWelcome`) yields a value that still fits an `int`, so
/// every voxel this type can address lives in a chunk `ChunkCoord` can address.
// struct BlockCoord, aligned to 4
#[repr(transparent)]
#[derive(Clone, Copy, PartialEq)]
pub struct BlockCoord(pub [u8; 12]);
impl Default for BlockCoord {
    fn default() -> Self {
        Self([0; 12])
    }
}
impl ::core::fmt::Debug for BlockCoord {
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        f.debug_struct("BlockCoord")
            .field("x", &self.x())
            .field("y", &self.y())
            .field("z", &self.z())
            .finish()
    }
}

impl ::flatbuffers::SimpleToVerifyInSlice for BlockCoord {}
impl<'a> ::flatbuffers::Follow<'a> for BlockCoord {
    type Inner = &'a BlockCoord;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        unsafe { <&'a BlockCoord>::follow(buf, loc) }
    }
}
impl<'a> ::flatbuffers::Follow<'a> for &'a BlockCoord {
    type Inner = &'a BlockCoord;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        unsafe { ::flatbuffers::follow_cast_ref::<BlockCoord>(buf, loc) }
    }
}
impl<'b> ::flatbuffers::Push for BlockCoord {
    type Output = BlockCoord;
    #[inline]
    unsafe fn push(&self, dst: &mut [u8], _written_len: usize) {
        let src = unsafe {
            ::core::slice::from_raw_parts(
                self as *const BlockCoord as *const u8,
                <Self as ::flatbuffers::Push>::size(),
            )
        };
        dst.copy_from_slice(src);
    }
    #[inline]
    fn alignment() -> ::flatbuffers::PushAlignment {
        ::flatbuffers::PushAlignment::new(4)
    }
}

impl<'a> ::flatbuffers::Verifiable for BlockCoord {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        v.in_buffer::<Self>(pos)
    }
}

impl<'a> BlockCoord {
    #[allow(clippy::too_many_arguments)]
    pub fn new(x: i32, y: i32, z: i32) -> Self {
        let mut s = Self([0; 12]);
        s.set_x(x);
        s.set_y(y);
        s.set_z(z);
        s
    }

    pub fn x(&self) -> i32 {
        let mut mem =
            ::core::mem::MaybeUninit::<<i32 as ::flatbuffers::EndianScalar>::Scalar>::uninit();
        // Safety:
        // Created from a valid Table for this object
        // Which contains a valid value in this slot
        ::flatbuffers::EndianScalar::from_little_endian(unsafe {
            ::core::ptr::copy_nonoverlapping(
                self.0[0..].as_ptr(),
                mem.as_mut_ptr() as *mut u8,
                ::core::mem::size_of::<<i32 as ::flatbuffers::EndianScalar>::Scalar>(),
            );
            mem.assume_init()
        })
    }

    pub fn set_x(&mut self, x: i32) {
        let x_le = ::flatbuffers::EndianScalar::to_little_endian(x);
        // Safety:
        // Created from a valid Table for this object
        // Which contains a valid value in this slot
        unsafe {
            ::core::ptr::copy_nonoverlapping(
                &x_le as *const _ as *const u8,
                self.0[0..].as_mut_ptr(),
                ::core::mem::size_of::<<i32 as ::flatbuffers::EndianScalar>::Scalar>(),
            );
        }
    }

    pub fn y(&self) -> i32 {
        let mut mem =
            ::core::mem::MaybeUninit::<<i32 as ::flatbuffers::EndianScalar>::Scalar>::uninit();
        // Safety:
        // Created from a valid Table for this object
        // Which contains a valid value in this slot
        ::flatbuffers::EndianScalar::from_little_endian(unsafe {
            ::core::ptr::copy_nonoverlapping(
                self.0[4..].as_ptr(),
                mem.as_mut_ptr() as *mut u8,
                ::core::mem::size_of::<<i32 as ::flatbuffers::EndianScalar>::Scalar>(),
            );
            mem.assume_init()
        })
    }

    pub fn set_y(&mut self, x: i32) {
        let x_le = ::flatbuffers::EndianScalar::to_little_endian(x);
        // Safety:
        // Created from a valid Table for this object
        // Which contains a valid value in this slot
        unsafe {
            ::core::ptr::copy_nonoverlapping(
                &x_le as *const _ as *const u8,
                self.0[4..].as_mut_ptr(),
                ::core::mem::size_of::<<i32 as ::flatbuffers::EndianScalar>::Scalar>(),
            );
        }
    }

    pub fn z(&self) -> i32 {
        let mut mem =
            ::core::mem::MaybeUninit::<<i32 as ::flatbuffers::EndianScalar>::Scalar>::uninit();
        // Safety:
        // Created from a valid Table for this object
        // Which contains a valid value in this slot
        ::flatbuffers::EndianScalar::from_little_endian(unsafe {
            ::core::ptr::copy_nonoverlapping(
                self.0[8..].as_ptr(),
                mem.as_mut_ptr() as *mut u8,
                ::core::mem::size_of::<<i32 as ::flatbuffers::EndianScalar>::Scalar>(),
            );
            mem.assume_init()
        })
    }

    pub fn set_z(&mut self, x: i32) {
        let x_le = ::flatbuffers::EndianScalar::to_little_endian(x);
        // Safety:
        // Created from a valid Table for this object
        // Which contains a valid value in this slot
        unsafe {
            ::core::ptr::copy_nonoverlapping(
                &x_le as *const _ as *const u8,
                self.0[8..].as_mut_ptr(),
                ::core::mem::size_of::<<i32 as ::flatbuffers::EndianScalar>::Scalar>(),
            );
        }
    }
}
