// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
pub enum InstanceEntryOfferOffset {}
#[derive(Copy, Clone, PartialEq)]

/// The server offering one character a crossing that would bind them. Server -> client.
///
/// **An offer, not a question the client may answer on its own.** It is sent instead of
/// a `WorldChange` when a `PortalRequest` resolves to a run that is already saved and the
/// character owes nothing here yet: the crossing has not happened, nothing is bound, and
/// the character is still standing in the open world. A client that treated this frame as
/// entry would be rendering a world the server has not moved it to.
///
/// **`terms` is exactly what accepting would create.** It is the same shape the character
/// would then carry in their own bindings, so a client has nothing to compute and no
/// second vocabulary to keep in step. `bosses_defeated` is the disclosure this prompt
/// deliberately makes — walking into a half-cleared run is the thing the player is being
/// warned about — and it is the *only* thing disclosed about a run the character is not
/// in. No member, no party, no session id, no owner.
///
/// **The prose belongs to the client.** Nothing here is a sentence: `terms` is numbers
/// and a place, and the client turns them into whatever it shows. That is the same rule
/// `RefusalReason` states, and it is why the mismatch warning — which is prose, addressed
/// to a person, and not routable by a machine — travels as a `ChatMessage` instead of
/// being smuggled in here or into the refusal vocabulary.
///
/// Decoder invariants:
///   - `offer_id` is non-zero. It is minted by the server for this crossing, is the only
///     thing an `InstanceEntryAnswer` may name, and means nothing to any other character
///   - `terms` is present and satisfies `SessionBinding`
///   - an offer is answered once. A client that has answered must not keep the id: the
///     server has already forgotten it, and a second answer is refused with
///     `EntryOfferUnknown`
///   - an offer is not durable. A world change, a disconnect, a later crossing, and the
///     run's own reset each end it, and the server owes no frame saying so. A client
///     showing a prompt should drop it on any of those rather than wait
pub struct InstanceEntryOffer<'a> {
    pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for InstanceEntryOffer<'a> {
    type Inner = InstanceEntryOffer<'a>;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        Self {
            _tab: unsafe { ::flatbuffers::Table::new(buf, loc) },
        }
    }
}

impl<'a> InstanceEntryOffer<'a> {
    pub const VT_OFFER_ID: ::flatbuffers::VOffsetT = 4;
    pub const VT_TERMS: ::flatbuffers::VOffsetT = 6;

    #[inline]
    pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
        InstanceEntryOffer { _tab: table }
    }
    #[allow(unused_mut)]
    pub fn create<
        'bldr: 'args,
        'args: 'mut_bldr,
        'mut_bldr,
        A: ::flatbuffers::Allocator + 'bldr,
    >(
        _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
        args: &'args InstanceEntryOfferArgs<'args>,
    ) -> ::flatbuffers::WIPOffset<InstanceEntryOffer<'bldr>> {
        let mut builder = InstanceEntryOfferBuilder::new(_fbb);
        builder.add_offer_id(args.offer_id);
        if let Some(x) = args.terms {
            builder.add_terms(x);
        }
        builder.finish()
    }

    #[inline]
    pub fn offer_id(&self) -> u64 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u64>(InstanceEntryOffer::VT_OFFER_ID, Some(0))
                .unwrap()
        }
    }
    #[inline]
    pub fn terms(&self) -> Option<SessionBinding<'a>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<::flatbuffers::ForwardsUOffset<SessionBinding>>(
                    InstanceEntryOffer::VT_TERMS,
                    None,
                )
        }
    }
}

impl ::flatbuffers::Verifiable for InstanceEntryOffer<'_> {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        v.visit_table(pos)?
            .visit_field::<u64>("offer_id", Self::VT_OFFER_ID, false)?
            .visit_field::<::flatbuffers::ForwardsUOffset<SessionBinding>>(
                "terms",
                Self::VT_TERMS,
                false,
            )?
            .finish();
        Ok(())
    }
}
pub struct InstanceEntryOfferArgs<'a> {
    pub offer_id: u64,
    pub terms: Option<::flatbuffers::WIPOffset<SessionBinding<'a>>>,
}
impl<'a> Default for InstanceEntryOfferArgs<'a> {
    #[inline]
    fn default() -> Self {
        InstanceEntryOfferArgs {
            offer_id: 0,
            terms: None,
        }
    }
}

pub struct InstanceEntryOfferBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
    fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> InstanceEntryOfferBuilder<'a, 'b, A> {
    #[inline]
    pub fn add_offer_id(&mut self, offer_id: u64) {
        self.fbb_
            .push_slot::<u64>(InstanceEntryOffer::VT_OFFER_ID, offer_id, 0);
    }
    #[inline]
    pub fn add_terms(&mut self, terms: ::flatbuffers::WIPOffset<SessionBinding<'b>>) {
        self.fbb_
            .push_slot_always::<::flatbuffers::WIPOffset<SessionBinding>>(
                InstanceEntryOffer::VT_TERMS,
                terms,
            );
    }
    #[inline]
    pub fn new(
        _fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    ) -> InstanceEntryOfferBuilder<'a, 'b, A> {
        let start = _fbb.start_table();
        InstanceEntryOfferBuilder {
            fbb_: _fbb,
            start_: start,
        }
    }
    #[inline]
    pub fn finish(self) -> ::flatbuffers::WIPOffset<InstanceEntryOffer<'a>> {
        let o = self.fbb_.end_table(self.start_);
        ::flatbuffers::WIPOffset::new(o.value())
    }
}

impl ::core::fmt::Debug for InstanceEntryOffer<'_> {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        let mut ds = f.debug_struct("InstanceEntryOffer");
        ds.field("offer_id", &self.offer_id());
        ds.field("terms", &self.terms());
        ds.finish()
    }
}
