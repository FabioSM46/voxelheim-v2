// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
#[deprecated(
    since = "2.0.0",
    note = "Use associated constants instead. This will no longer be generated in 2021."
)]
pub const ENUM_MIN_PLAYER_TRADE_ACTION: u8 = 0;
#[deprecated(
    since = "2.0.0",
    note = "Use associated constants instead. This will no longer be generated in 2021."
)]
pub const ENUM_MAX_PLAYER_TRADE_ACTION: u8 = 6;
#[deprecated(
    since = "2.0.0",
    note = "Use associated constants instead. This will no longer be generated in 2021."
)]
#[allow(non_camel_case_types)]
pub const ENUM_VALUES_PLAYER_TRADE_ACTION: [PlayerTradeAction; 7] = [
    PlayerTradeAction::Unknown,
    PlayerTradeAction::Open,
    PlayerTradeAction::SetItem,
    PlayerTradeAction::ClearItem,
    PlayerTradeAction::SetSilver,
    PlayerTradeAction::Confirm,
    PlayerTradeAction::Cancel,
];

/// Which operation one `PlayerTradeRequest` asks the authoritative server to perform.
///
/// There is deliberately no invite, accept or decline. `Open` is the only way a trade
/// starts, and the addressed partner learns of it from the first `PlayerTradeState`, in
/// the same way a stall opens on its first `VendorState`. The confirmation prompt belongs
/// to the player who pressed the key, not to the player they addressed.
///
/// Every action also reads `client_tick` for authoritative ordering and staleness:
///   - `Open` reads `target_entity_id`. A target that is absent, self, not a player,
///     out of reach, dead or already trading is refused as `NoSuchPlayer`, so the sender
///     cannot probe another player's state. The sender's own state may instead produce
///     `PlayerIsDead` or `AlreadyTrading`; a recently closed pair uses `TradeCooldown`
///   - `SetItem` reads `trade_slot`, `pack_slot`, and `revision`
///   - `ClearItem` reads `trade_slot` and `revision`
///   - `SetSilver` reads `silver` and `revision`
///   - `Confirm` reads `revision`
///   - `Cancel` reads no action-specific field
#[derive(Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Default)]
#[repr(transparent)]
pub struct PlayerTradeAction(pub u8);
#[allow(non_upper_case_globals)]
impl PlayerTradeAction {
    pub const Unknown: Self = Self(0);
    pub const Open: Self = Self(1);
    pub const SetItem: Self = Self(2);
    pub const ClearItem: Self = Self(3);
    pub const SetSilver: Self = Self(4);
    pub const Confirm: Self = Self(5);
    pub const Cancel: Self = Self(6);

    pub const ENUM_MIN: u8 = 0;
    pub const ENUM_MAX: u8 = 6;
    pub const ENUM_VALUES: &'static [Self] = &[
        Self::Unknown,
        Self::Open,
        Self::SetItem,
        Self::ClearItem,
        Self::SetSilver,
        Self::Confirm,
        Self::Cancel,
    ];
    /// Returns the variant's name or "" if unknown.
    pub fn variant_name(self) -> Option<&'static str> {
        match self {
            Self::Unknown => Some("Unknown"),
            Self::Open => Some("Open"),
            Self::SetItem => Some("SetItem"),
            Self::ClearItem => Some("ClearItem"),
            Self::SetSilver => Some("SetSilver"),
            Self::Confirm => Some("Confirm"),
            Self::Cancel => Some("Cancel"),
            _ => None,
        }
    }
}
impl ::core::fmt::Debug for PlayerTradeAction {
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        if let Some(name) = self.variant_name() {
            f.write_str(name)
        } else {
            f.write_fmt(format_args!("<UNKNOWN {:?}>", self.0))
        }
    }
}
impl<'a> ::flatbuffers::Follow<'a> for PlayerTradeAction {
    type Inner = Self;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        let b = unsafe { ::flatbuffers::read_scalar_at::<u8>(buf, loc) };
        Self(b)
    }
}

impl ::flatbuffers::Push for PlayerTradeAction {
    type Output = PlayerTradeAction;
    #[inline]
    unsafe fn push(&self, dst: &mut [u8], _written_len: usize) {
        unsafe { ::flatbuffers::emplace_scalar::<u8>(dst, self.0) };
    }
}

impl ::flatbuffers::EndianScalar for PlayerTradeAction {
    type Scalar = u8;
    #[inline]
    fn to_little_endian(self) -> u8 {
        self.0.to_le()
    }
    #[inline]
    #[allow(clippy::wrong_self_convention)]
    fn from_little_endian(v: u8) -> Self {
        let b = u8::from_le(v);
        Self(b)
    }
}

impl<'a> ::flatbuffers::Verifiable for PlayerTradeAction {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        u8::run_verifier(v, pos)
    }
}

impl ::flatbuffers::SimpleToVerifyInSlice for PlayerTradeAction {}
