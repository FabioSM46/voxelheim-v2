// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
pub enum VoiceFrameOffset {}
#[derive(Copy, Clone, PartialEq)]

/// One encoded Opus frame a client is asking the server to relay. Client -> server,
/// and intent only.
///
/// **It carries no position and no recipient**, which is the whole of the authority
/// boundary here: the server already knows where this speaker is standing, because it
/// put them there, and it decides who is close enough to hear them. A field naming a
/// listener would be a client choosing an audience, and a field naming a position would
/// be a client choosing how far its voice carries.
///
/// `sequence` is the speaker's own monotonic frame counter, for a receiver's jitter
/// buffer to order and to notice gaps with. It is presentation, never authority: the
/// server does not read it as a clock, does not reject a frame for arriving out of
/// order, and does not renumber it.
///
/// Decoder invariants:
///   - `audience` is copied verbatim, unrecognised values included. It is deliberately
///     not a framing question: an audience this server cannot name is a filter it
///     cannot apply, so the *relay* drops the frame silently rather than the decoder
///     closing the connection. Widening it to `Everyone` is the one wrong answer, since
///     it would deliver to people the speaker asked to exclude
///   - `opus` is copied verbatim and never parsed. Whether it is a legal Opus frame is
///     the *listener's* decoder's question; this server forwards bytes it does not read
///   - an absent or empty `opus` is framing-valid and carries no audio. Whether such a
///     frame is worth relaying is a server decision, not a framing one
///   - a frame carrying more than `VoiceBound.MaxOpusBytes` of `opus` is dropped by the
///     relay. That ceiling is the contract's rather than one server's tuning, which is
///     why it is an enum both peers read
///
/// **Nothing may write these bytes down.** A voice frame is personal data: the server
/// relays it and never logs it, never persists it and never puts it in a diagnostic.
/// That is stated in the GDD's section 10 and it is a constraint on every consumer of
/// this table.
pub struct VoiceFrame<'a> {
    pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for VoiceFrame<'a> {
    type Inner = VoiceFrame<'a>;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        Self {
            _tab: unsafe { ::flatbuffers::Table::new(buf, loc) },
        }
    }
}

impl<'a> VoiceFrame<'a> {
    pub const VT_SEQUENCE: ::flatbuffers::VOffsetT = 4;
    pub const VT_AUDIENCE: ::flatbuffers::VOffsetT = 6;
    pub const VT_OPUS: ::flatbuffers::VOffsetT = 8;

    #[inline]
    pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
        VoiceFrame { _tab: table }
    }
    #[allow(unused_mut)]
    pub fn create<
        'bldr: 'args,
        'args: 'mut_bldr,
        'mut_bldr,
        A: ::flatbuffers::Allocator + 'bldr,
    >(
        _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
        args: &'args VoiceFrameArgs<'args>,
    ) -> ::flatbuffers::WIPOffset<VoiceFrame<'bldr>> {
        let mut builder = VoiceFrameBuilder::new(_fbb);
        if let Some(x) = args.opus {
            builder.add_opus(x);
        }
        builder.add_sequence(args.sequence);
        builder.add_audience(args.audience);
        builder.finish()
    }

    #[inline]
    pub fn sequence(&self) -> u32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u32>(VoiceFrame::VT_SEQUENCE, Some(0))
                .unwrap()
        }
    }
    #[inline]
    pub fn audience(&self) -> VoiceAudience {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<VoiceAudience>(VoiceFrame::VT_AUDIENCE, Some(VoiceAudience::Everyone))
                .unwrap()
        }
    }
    #[inline]
    pub fn opus(&self) -> Option<::flatbuffers::Vector<'a, u8>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, u8>>>(
                    VoiceFrame::VT_OPUS,
                    None,
                )
        }
    }
}

impl ::flatbuffers::Verifiable for VoiceFrame<'_> {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        v.visit_table(pos)?
            .visit_field::<u32>("sequence", Self::VT_SEQUENCE, false)?
            .visit_field::<VoiceAudience>("audience", Self::VT_AUDIENCE, false)?
            .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, u8>>>(
                "opus",
                Self::VT_OPUS,
                false,
            )?
            .finish();
        Ok(())
    }
}
pub struct VoiceFrameArgs<'a> {
    pub sequence: u32,
    pub audience: VoiceAudience,
    pub opus: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, u8>>>,
}
impl<'a> Default for VoiceFrameArgs<'a> {
    #[inline]
    fn default() -> Self {
        VoiceFrameArgs {
            sequence: 0,
            audience: VoiceAudience::Everyone,
            opus: None,
        }
    }
}

pub struct VoiceFrameBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
    fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> VoiceFrameBuilder<'a, 'b, A> {
    #[inline]
    pub fn add_sequence(&mut self, sequence: u32) {
        self.fbb_
            .push_slot::<u32>(VoiceFrame::VT_SEQUENCE, sequence, 0);
    }
    #[inline]
    pub fn add_audience(&mut self, audience: VoiceAudience) {
        self.fbb_.push_slot::<VoiceAudience>(
            VoiceFrame::VT_AUDIENCE,
            audience,
            VoiceAudience::Everyone,
        );
    }
    #[inline]
    pub fn add_opus(&mut self, opus: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b, u8>>) {
        self.fbb_
            .push_slot_always::<::flatbuffers::WIPOffset<_>>(VoiceFrame::VT_OPUS, opus);
    }
    #[inline]
    pub fn new(
        _fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    ) -> VoiceFrameBuilder<'a, 'b, A> {
        let start = _fbb.start_table();
        VoiceFrameBuilder {
            fbb_: _fbb,
            start_: start,
        }
    }
    #[inline]
    pub fn finish(self) -> ::flatbuffers::WIPOffset<VoiceFrame<'a>> {
        let o = self.fbb_.end_table(self.start_);
        ::flatbuffers::WIPOffset::new(o.value())
    }
}

impl ::core::fmt::Debug for VoiceFrame<'_> {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        let mut ds = f.debug_struct("VoiceFrame");
        ds.field("sequence", &self.sequence());
        ds.field("audience", &self.audience());
        ds.field("opus", &self.opus());
        ds.finish()
    }
}
