// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
/// One entity's authoritative state at a server tick.
///
/// A struct so that a snapshot's entity list is a flat, inlined array: this is
/// the most frequently sent payload in the game.
///
/// The finite-float invariant runs in this direction too: a non-finite `pos`
/// would poison the client's interpolation and, through the entity's transform,
/// its renderer. The server must never emit one, and the client validates rather
/// than assuming it never does.
///
/// **V7 added an appearance to every player and added nothing here.** What a
/// character looks like travels in `PlayerAppearance` at the bottom of this file;
/// the argument for keeping it out of this struct is written there, beside the
/// message that carries it instead. Four fields, unchanged since V1, and the
/// encoded size of this struct is pinned by a test on both sides — which is what
/// catches somebody quietly adding a fifth.
///
/// **V10 gave every player a death every viewer can see and added nothing here
/// either.** Whether a player is down rides beside this vector, in
/// `EntitySnapshot.dead_players`, where it costs nothing on the ticks nobody is
/// dead. A fifth field here would have charged every visible player those bytes on
/// every tick to say "no", and could never have been taken back.
///
/// Decoder invariant: `entity_id` is non-zero. Uniqueness belongs to the containing
/// snapshot, because only that complete vector can answer whether an id occurs twice.
// struct EntityState, aligned to 8
#[repr(transparent)]
#[derive(Clone, Copy, PartialEq)]
pub struct EntityState(pub [u8; 40]);
impl Default for EntityState {
    fn default() -> Self {
        Self([0; 40])
    }
}
impl ::core::fmt::Debug for EntityState {
    fn fmt(&self, f: &mut ::core::fmt::Formatter) -> ::core::fmt::Result {
        f.debug_struct("EntityState")
            .field("entity_id", &self.entity_id())
            .field("pos", &self.pos())
            .field("vel", &self.vel())
            .field("yaw", &self.yaw())
            .finish()
    }
}

impl ::flatbuffers::SimpleToVerifyInSlice for EntityState {}
impl<'a> ::flatbuffers::Follow<'a> for EntityState {
    type Inner = &'a EntityState;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        unsafe { <&'a EntityState>::follow(buf, loc) }
    }
}
impl<'a> ::flatbuffers::Follow<'a> for &'a EntityState {
    type Inner = &'a EntityState;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        unsafe { ::flatbuffers::follow_cast_ref::<EntityState>(buf, loc) }
    }
}
impl<'b> ::flatbuffers::Push for EntityState {
    type Output = EntityState;
    #[inline]
    unsafe fn push(&self, dst: &mut [u8], _written_len: usize) {
        let src = unsafe {
            ::core::slice::from_raw_parts(
                self as *const EntityState as *const u8,
                <Self as ::flatbuffers::Push>::size(),
            )
        };
        dst.copy_from_slice(src);
    }
    #[inline]
    fn alignment() -> ::flatbuffers::PushAlignment {
        ::flatbuffers::PushAlignment::new(8)
    }
}

impl<'a> ::flatbuffers::Verifiable for EntityState {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        v.in_buffer::<Self>(pos)
    }
}

impl<'a> EntityState {
    #[allow(clippy::too_many_arguments)]
    pub fn new(entity_id: u64, pos: &Vec3, vel: &Vec3, yaw: f32) -> Self {
        let mut s = Self([0; 40]);
        s.set_entity_id(entity_id);
        s.set_pos(pos);
        s.set_vel(vel);
        s.set_yaw(yaw);
        s
    }

    pub fn entity_id(&self) -> u64 {
        let mut mem =
            ::core::mem::MaybeUninit::<<u64 as ::flatbuffers::EndianScalar>::Scalar>::uninit();
        // Safety:
        // Created from a valid Table for this object
        // Which contains a valid value in this slot
        ::flatbuffers::EndianScalar::from_little_endian(unsafe {
            ::core::ptr::copy_nonoverlapping(
                self.0[0..].as_ptr(),
                mem.as_mut_ptr() as *mut u8,
                ::core::mem::size_of::<<u64 as ::flatbuffers::EndianScalar>::Scalar>(),
            );
            mem.assume_init()
        })
    }

    pub fn set_entity_id(&mut self, x: u64) {
        let x_le = ::flatbuffers::EndianScalar::to_little_endian(x);
        // Safety:
        // Created from a valid Table for this object
        // Which contains a valid value in this slot
        unsafe {
            ::core::ptr::copy_nonoverlapping(
                &x_le as *const _ as *const u8,
                self.0[0..].as_mut_ptr(),
                ::core::mem::size_of::<<u64 as ::flatbuffers::EndianScalar>::Scalar>(),
            );
        }
    }

    pub fn pos(&self) -> &Vec3 {
        // Safety:
        // Created from a valid Table for this object
        // Which contains a valid struct in this slot
        unsafe { &*(self.0[8..].as_ptr() as *const Vec3) }
    }

    #[allow(clippy::identity_op)]
    pub fn set_pos(&mut self, x: &Vec3) {
        self.0[8..8 + 12].copy_from_slice(&x.0)
    }

    pub fn vel(&self) -> &Vec3 {
        // Safety:
        // Created from a valid Table for this object
        // Which contains a valid struct in this slot
        unsafe { &*(self.0[20..].as_ptr() as *const Vec3) }
    }

    #[allow(clippy::identity_op)]
    pub fn set_vel(&mut self, x: &Vec3) {
        self.0[20..20 + 12].copy_from_slice(&x.0)
    }

    pub fn yaw(&self) -> f32 {
        let mut mem =
            ::core::mem::MaybeUninit::<<f32 as ::flatbuffers::EndianScalar>::Scalar>::uninit();
        // Safety:
        // Created from a valid Table for this object
        // Which contains a valid value in this slot
        ::flatbuffers::EndianScalar::from_little_endian(unsafe {
            ::core::ptr::copy_nonoverlapping(
                self.0[32..].as_ptr(),
                mem.as_mut_ptr() as *mut u8,
                ::core::mem::size_of::<<f32 as ::flatbuffers::EndianScalar>::Scalar>(),
            );
            mem.assume_init()
        })
    }

    pub fn set_yaw(&mut self, x: f32) {
        let x_le = ::flatbuffers::EndianScalar::to_little_endian(x);
        // Safety:
        // Created from a valid Table for this object
        // Which contains a valid value in this slot
        unsafe {
            ::core::ptr::copy_nonoverlapping(
                &x_le as *const _ as *const u8,
                self.0[32..].as_mut_ptr(),
                ::core::mem::size_of::<<f32 as ::flatbuffers::EndianScalar>::Scalar>(),
            );
        }
    }
}
