// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
pub enum SessionBindingOffset {}
#[derive(Copy, Clone, PartialEq)]

/// One saved run a character owes, as the server states it. Server -> client.
///
/// A table rather than a struct for `Appearance`'s reason: it is the least settled shape
/// this contract adds — a name for the ruin, a party, a difficulty are all things a later
/// issue may want — and it is not hot. One of these is sent when somebody stands at an
/// arch and when their bindings change, never per tick.
///
/// **It names a place, not a session.** `arch` is the ruin's own anchor in the open
/// world, which is the identity the client already uses to ask to cross
/// (`PortalRequest.arch`) and the one a map can draw. The session's `world_id` is
/// deliberately absent: `WorldChange` states that an instance id is scoped to a
/// connection, is never entry permission, and never announces another group's identity.
/// A binding is a fact about the character and the dungeon; it needs no session number
/// to be complete.
///
/// **`bosses_defeated` is counted per encounter species, not per corpse.** The server
/// records a defeat once per boss species so that it survives a restart, which means a
/// dungeon holding two bosses of one species would report one defeated encounter. That
/// limit is stated rather than designed around, and it is why `bosses_total` is a count
/// of the dungeon's boss-rank encounters at the same granularity.
///
/// Decoder invariants:
///   - `arch` is present and every component lies in the +/-2^24 block domain
///   - `bosses_total` is non-zero, and `bosses_defeated` is never greater than it. A
///     progress reading a client shows as "1 / 3" is these two numbers and nothing else;
///     the client never counts corpses of its own
///   - `resets_at_unix` is a non-zero Unix second strictly in the future when it is
///     sent. It is wall-clock time on the server's calendar, not an in-game clock: a
///     binding lasts until the server's next midnight, so a client that has been open
///     across that second must expect the list to be replaced rather than expire it
///     itself and act on the guess
pub struct SessionBinding<'a> {
    pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for SessionBinding<'a> {
    type Inner = SessionBinding<'a>;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        Self {
            _tab: unsafe { ::flatbuffers::Table::new(buf, loc) },
        }
    }
}

impl<'a> SessionBinding<'a> {
    pub const VT_ARCH: ::flatbuffers::VOffsetT = 4;
    pub const VT_BOSSES_DEFEATED: ::flatbuffers::VOffsetT = 6;
    pub const VT_BOSSES_TOTAL: ::flatbuffers::VOffsetT = 8;
    pub const VT_RESETS_AT_UNIX: ::flatbuffers::VOffsetT = 10;

    #[inline]
    pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
        SessionBinding { _tab: table }
    }
    #[allow(unused_mut)]
    pub fn create<
        'bldr: 'args,
        'args: 'mut_bldr,
        'mut_bldr,
        A: ::flatbuffers::Allocator + 'bldr,
    >(
        _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
        args: &'args SessionBindingArgs<'args>,
    ) -> ::flatbuffers::WIPOffset<SessionBinding<'bldr>> {
        let mut builder = SessionBindingBuilder::new(_fbb);
        builder.add_resets_at_unix(args.resets_at_unix);
        if let Some(x) = args.arch {
            builder.add_arch(x);
        }
        builder.add_bosses_total(args.bosses_total);
        builder.add_bosses_defeated(args.bosses_defeated);
        builder.finish()
    }

    #[inline]
    pub fn arch(&self) -> Option<&'a BlockCoord> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe { self._tab.get::<BlockCoord>(SessionBinding::VT_ARCH, None) }
    }
    #[inline]
    pub fn bosses_defeated(&self) -> u8 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u8>(SessionBinding::VT_BOSSES_DEFEATED, Some(0))
                .unwrap()
        }
    }
    #[inline]
    pub fn bosses_total(&self) -> u8 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u8>(SessionBinding::VT_BOSSES_TOTAL, Some(0))
                .unwrap()
        }
    }
    #[inline]
    pub fn resets_at_unix(&self) -> i64 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<i64>(SessionBinding::VT_RESETS_AT_UNIX, Some(0))
                .unwrap()
        }
    }
}

impl ::flatbuffers::Verifiable for SessionBinding<'_> {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        v.visit_table(pos)?
            .visit_field::<BlockCoord>("arch", Self::VT_ARCH, false)?
            .visit_field::<u8>("bosses_defeated", Self::VT_BOSSES_DEFEATED, false)?
            .visit_field::<u8>("bosses_total", Self::VT_BOSSES_TOTAL, false)?
            .visit_field::<i64>("resets_at_unix", Self::VT_RESETS_AT_UNIX, false)?
            .finish();
        Ok(())
    }
}
pub struct SessionBindingArgs<'a> {
    pub arch: Option<&'a BlockCoord>,
    pub bosses_defeated: u8,
    pub bosses_total: u8,
    pub resets_at_unix: i64,
}
impl<'a> Default for SessionBindingArgs<'a> {
    #[inline]
    fn default() -> Self {
        SessionBindingArgs {
            arch: None,
            bosses_defeated: 0,
            bosses_total: 0,
            resets_at_unix: 0,
        }
    }
}

pub struct SessionBindingBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
    fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> SessionBindingBuilder<'a, 'b, A> {
    #[inline]
    pub fn add_arch(&mut self, arch: &BlockCoord) {
        self.fbb_
            .push_slot_always::<&BlockCoord>(SessionBinding::VT_ARCH, arch);
    }
    #[inline]
    pub fn add_bosses_defeated(&mut self, bosses_defeated: u8) {
        self.fbb_
            .push_slot::<u8>(SessionBinding::VT_BOSSES_DEFEATED, bosses_defeated, 0);
    }
    #[inline]
    pub fn add_bosses_total(&mut self, bosses_total: u8) {
        self.fbb_
            .push_slot::<u8>(SessionBinding::VT_BOSSES_TOTAL, bosses_total, 0);
    }
    #[inline]
    pub fn add_resets_at_unix(&mut self, resets_at_unix: i64) {
        self.fbb_
            .push_slot::<i64>(SessionBinding::VT_RESETS_AT_UNIX, resets_at_unix, 0);
    }
    #[inline]
    pub fn new(
        _fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    ) -> SessionBindingBuilder<'a, 'b, A> {
        let start = _fbb.start_table();
        SessionBindingBuilder {
            fbb_: _fbb,
            start_: start,
        }
    }
    #[inline]
    pub fn finish(self) -> ::flatbuffers::WIPOffset<SessionBinding<'a>> {
        let o = self.fbb_.end_table(self.start_);
        ::flatbuffers::WIPOffset::new(o.value())
    }
}

impl ::core::fmt::Debug for SessionBinding<'_> {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        let mut ds = f.debug_struct("SessionBinding");
        ds.field("arch", &self.arch());
        ds.field("bosses_defeated", &self.bosses_defeated());
        ds.field("bosses_total", &self.bosses_total());
        ds.field("resets_at_unix", &self.resets_at_unix());
        ds.finish()
    }
}
