// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
pub enum PlayerInputOffset {}
#[derive(Copy, Clone, PartialEq)]

/// One tick's worth of intent from a client.
///
/// Sent at `ServerWelcome.tick_rate`, not at the client's frame rate. The
/// server clamps the axes and integrates them itself; a client that sends
/// out-of-range values gains nothing.
///
/// **Decoder invariant: every float here must be finite.** This is a different
/// requirement from the range clamp, and clamping cannot satisfy it. `NaN`
/// compares false against every bound, so the usual `if v > 1 { v = 1 }` lets it
/// through untouched, and it then propagates through the integrator into a
/// position that stays `NaN` forever. The server rejects or zeroes non-finite
/// components **before** any physics runs, treating them as the malformed input
/// they are rather than as an extreme value.
pub struct PlayerInput<'a> {
    pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for PlayerInput<'a> {
    type Inner = PlayerInput<'a>;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        Self {
            _tab: unsafe { ::flatbuffers::Table::new(buf, loc) },
        }
    }
}

impl<'a> PlayerInput<'a> {
    pub const VT_CLIENT_TICK: ::flatbuffers::VOffsetT = 4;
    pub const VT_MOVE_X: ::flatbuffers::VOffsetT = 6;
    pub const VT_MOVE_Z: ::flatbuffers::VOffsetT = 8;
    pub const VT_YAW: ::flatbuffers::VOffsetT = 10;
    pub const VT_PITCH: ::flatbuffers::VOffsetT = 12;
    pub const VT_JUMP: ::flatbuffers::VOffsetT = 14;

    #[inline]
    pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
        PlayerInput { _tab: table }
    }
    #[allow(unused_mut)]
    pub fn create<
        'bldr: 'args,
        'args: 'mut_bldr,
        'mut_bldr,
        A: ::flatbuffers::Allocator + 'bldr,
    >(
        _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
        args: &'args PlayerInputArgs,
    ) -> ::flatbuffers::WIPOffset<PlayerInput<'bldr>> {
        let mut builder = PlayerInputBuilder::new(_fbb);
        builder.add_pitch(args.pitch);
        builder.add_yaw(args.yaw);
        builder.add_move_z(args.move_z);
        builder.add_move_x(args.move_x);
        builder.add_client_tick(args.client_tick);
        builder.add_jump(args.jump);
        builder.finish()
    }

    /// The client's own tick counter. Used for ordering and staleness checks; the
    /// server never trusts it as a clock.
    #[inline]
    pub fn client_tick(&self) -> u32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u32>(PlayerInput::VT_CLIENT_TICK, Some(0))
                .unwrap()
        }
    }
    /// Strafe intent, -1 (left) to 1 (right). Clamped server-side.
    #[inline]
    pub fn move_x(&self) -> f32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<f32>(PlayerInput::VT_MOVE_X, Some(0.0))
                .unwrap()
        }
    }
    /// Forward intent, -1 (backward) to 1 (forward). Clamped server-side.
    #[inline]
    pub fn move_z(&self) -> f32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<f32>(PlayerInput::VT_MOVE_Z, Some(0.0))
                .unwrap()
        }
    }
    /// Facing, in radians. Affects the direction the movement axes are applied
    /// in, so the server needs it even though the camera is a client concern.
    #[inline]
    pub fn yaw(&self) -> f32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<f32>(PlayerInput::VT_YAW, Some(0.0))
                .unwrap()
        }
    }
    /// Pitch, in radians. Carried for aiming; movement ignores it.
    #[inline]
    pub fn pitch(&self) -> f32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<f32>(PlayerInput::VT_PITCH, Some(0.0))
                .unwrap()
        }
    }
    /// Whether the jump control is held this tick. Whether a jump *happens* is
    /// the server's decision (it owns ground contact).
    #[inline]
    pub fn jump(&self) -> bool {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<bool>(PlayerInput::VT_JUMP, Some(false))
                .unwrap()
        }
    }
}

impl ::flatbuffers::Verifiable for PlayerInput<'_> {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        v.visit_table(pos)?
            .visit_field::<u32>("client_tick", Self::VT_CLIENT_TICK, false)?
            .visit_field::<f32>("move_x", Self::VT_MOVE_X, false)?
            .visit_field::<f32>("move_z", Self::VT_MOVE_Z, false)?
            .visit_field::<f32>("yaw", Self::VT_YAW, false)?
            .visit_field::<f32>("pitch", Self::VT_PITCH, false)?
            .visit_field::<bool>("jump", Self::VT_JUMP, false)?
            .finish();
        Ok(())
    }
}
pub struct PlayerInputArgs {
    pub client_tick: u32,
    pub move_x: f32,
    pub move_z: f32,
    pub yaw: f32,
    pub pitch: f32,
    pub jump: bool,
}
impl<'a> Default for PlayerInputArgs {
    #[inline]
    fn default() -> Self {
        PlayerInputArgs {
            client_tick: 0,
            move_x: 0.0,
            move_z: 0.0,
            yaw: 0.0,
            pitch: 0.0,
            jump: false,
        }
    }
}

pub struct PlayerInputBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
    fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> PlayerInputBuilder<'a, 'b, A> {
    #[inline]
    pub fn add_client_tick(&mut self, client_tick: u32) {
        self.fbb_
            .push_slot::<u32>(PlayerInput::VT_CLIENT_TICK, client_tick, 0);
    }
    #[inline]
    pub fn add_move_x(&mut self, move_x: f32) {
        self.fbb_
            .push_slot::<f32>(PlayerInput::VT_MOVE_X, move_x, 0.0);
    }
    #[inline]
    pub fn add_move_z(&mut self, move_z: f32) {
        self.fbb_
            .push_slot::<f32>(PlayerInput::VT_MOVE_Z, move_z, 0.0);
    }
    #[inline]
    pub fn add_yaw(&mut self, yaw: f32) {
        self.fbb_.push_slot::<f32>(PlayerInput::VT_YAW, yaw, 0.0);
    }
    #[inline]
    pub fn add_pitch(&mut self, pitch: f32) {
        self.fbb_
            .push_slot::<f32>(PlayerInput::VT_PITCH, pitch, 0.0);
    }
    #[inline]
    pub fn add_jump(&mut self, jump: bool) {
        self.fbb_
            .push_slot::<bool>(PlayerInput::VT_JUMP, jump, false);
    }
    #[inline]
    pub fn new(
        _fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    ) -> PlayerInputBuilder<'a, 'b, A> {
        let start = _fbb.start_table();
        PlayerInputBuilder {
            fbb_: _fbb,
            start_: start,
        }
    }
    #[inline]
    pub fn finish(self) -> ::flatbuffers::WIPOffset<PlayerInput<'a>> {
        let o = self.fbb_.end_table(self.start_);
        ::flatbuffers::WIPOffset::new(o.value())
    }
}

impl ::core::fmt::Debug for PlayerInput<'_> {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        let mut ds = f.debug_struct("PlayerInput");
        ds.field("client_tick", &self.client_tick());
        ds.field("move_x", &self.move_x());
        ds.field("move_z", &self.move_z());
        ds.field("yaw", &self.yaw());
        ds.field("pitch", &self.pitch());
        ds.field("jump", &self.jump());
        ds.finish()
    }
}
