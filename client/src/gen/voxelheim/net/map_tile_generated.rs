// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
pub enum MapTileOffset {}
#[derive(Copy, Clone, PartialEq)]

/// One 64 x 64 square of the map, as the server draws it for one character.
/// Server -> client, and authoritative.
///
/// **Tables, not a table per pixel.** `height` and `surface` are flat `[ubyte]` of
/// exactly 4096 entries because 4096 tables would be a FlatBuffers pathology — an offset
/// each, and a vtable each — where two flat vectors are 8 KiB that decode to slices. It
/// is the argument `ChunkData.runs` makes, one dimension down. The message itself is a
/// table rather than a struct for the opposite reason: it is not settled, and a colour,
/// an icon or a biome name is exactly the sort of thing a later issue appends.
///
/// **Sampling.** Both arrays are row-major with z outer and x inner:
///
///     index = pz * 64 + px
///
/// and each entry samples the world column at that pixel's **centre**, at
/// `origin_x + px * scale + scale / 2` and `origin_z + pz * scale + scale / 2`. Sampling
/// one column rather than averaging `scale²` of them is what keeps a coarse tile as cheap
/// as a fine one; the map is a picture, not a measurement.
///
/// **`height` is `clamp(y + 64, 0, 255)`** — the surface height biased so that sea level
/// and the world's negative half both survive a single byte, and saturated at both ends
/// rather than wrapped. A client reads it as shading, never as a coordinate.
///
/// **`explored` is a bitmask over chunk columns, not over pixels.** The server records
/// exploration at the granularity it actually has one — a chunk column streamed to this
/// character — and a chunk column is 32 blocks square. A tile covers
/// `(64 * scale / 32)²` of them: 4 at scale 1, 64 at scale 4, 1024 at scale 16. The bits
/// are row-major in the same order the pixels are, z outer and x inner, **LSB first
/// within each byte**, so bit `i` lives in `explored[i / 8] >> (i % 8)`. Rounded up to
/// whole bytes that is exactly 1, 8 or 128 bytes — scale 1 is the one case that rounds,
/// and its four unused high bits are zero.
///
/// **The unexplored is blank, not shaped.** Every pixel inside a chunk column whose bit
/// is clear carries `0` in `height` and `Unknown` in `surface`. The server never sends
/// the real value with a "do not draw" flag beside it, because a client is not where a
/// secret is kept: a build that ignored the mask would otherwise hold the terrain of a
/// world nobody has walked.
///
/// Invariants a decoder must enforce:
///   - `scale` is 1, 4 or 16, and `origin_x` / `origin_z` are multiples of `64 * scale` —
///     the same grid the request is held to, checked again because this side is where a
///     tile is placed on a canvas
///   - `height` and `surface` are both present and both exactly 4096 entries. A short
///     array is a protocol error, never a partially drawn tile
///   - every `surface` entry is a member of `MapSurface`
///   - `explored` is present and exactly 1, 8 or 128 bytes for scale 1, 4 or 16. Bits
///     past the column count — only ever the four high bits of a scale-1 mask — are zero
pub struct MapTile<'a> {
    pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for MapTile<'a> {
    type Inner = MapTile<'a>;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        Self {
            _tab: unsafe { ::flatbuffers::Table::new(buf, loc) },
        }
    }
}

impl<'a> MapTile<'a> {
    pub const VT_ORIGIN_X: ::flatbuffers::VOffsetT = 4;
    pub const VT_ORIGIN_Z: ::flatbuffers::VOffsetT = 6;
    pub const VT_SCALE: ::flatbuffers::VOffsetT = 8;
    pub const VT_HEIGHT: ::flatbuffers::VOffsetT = 10;
    pub const VT_SURFACE: ::flatbuffers::VOffsetT = 12;
    pub const VT_EXPLORED: ::flatbuffers::VOffsetT = 14;

    #[inline]
    pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
        MapTile { _tab: table }
    }
    #[allow(unused_mut)]
    pub fn create<
        'bldr: 'args,
        'args: 'mut_bldr,
        'mut_bldr,
        A: ::flatbuffers::Allocator + 'bldr,
    >(
        _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
        args: &'args MapTileArgs<'args>,
    ) -> ::flatbuffers::WIPOffset<MapTile<'bldr>> {
        let mut builder = MapTileBuilder::new(_fbb);
        if let Some(x) = args.explored {
            builder.add_explored(x);
        }
        if let Some(x) = args.surface {
            builder.add_surface(x);
        }
        if let Some(x) = args.height {
            builder.add_height(x);
        }
        builder.add_origin_z(args.origin_z);
        builder.add_origin_x(args.origin_x);
        builder.add_scale(args.scale);
        builder.finish()
    }

    /// Block x of the tile's minimum corner, echoing the request that earned it.
    #[inline]
    pub fn origin_x(&self) -> i32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe { self._tab.get::<i32>(MapTile::VT_ORIGIN_X, Some(0)).unwrap() }
    }
    /// Block z of the tile's minimum corner.
    #[inline]
    pub fn origin_z(&self) -> i32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe { self._tab.get::<i32>(MapTile::VT_ORIGIN_Z, Some(0)).unwrap() }
    }
    /// Blocks per pixel: 1, 4 or 16.
    #[inline]
    pub fn scale(&self) -> u8 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe { self._tab.get::<u8>(MapTile::VT_SCALE, Some(0)).unwrap() }
    }
    /// 4096 biased heights, row-major, z outer and x inner. `clamp(y + 64, 0, 255)`.
    #[inline]
    pub fn height(&self) -> Option<::flatbuffers::Vector<'a, u8>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, u8>>>(
                    MapTile::VT_HEIGHT,
                    None,
                )
        }
    }
    /// 4096 `MapSurface` members, in the same order as `height`.
    #[inline]
    pub fn surface(&self) -> Option<::flatbuffers::Vector<'a, u8>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, u8>>>(
                    MapTile::VT_SURFACE,
                    None,
                )
        }
    }
    /// The chunk columns of this tile this character has explored, one bit each,
    /// row-major and LSB first. 1, 8 or 128 bytes by scale.
    #[inline]
    pub fn explored(&self) -> Option<::flatbuffers::Vector<'a, u8>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, u8>>>(
                    MapTile::VT_EXPLORED,
                    None,
                )
        }
    }
}

impl ::flatbuffers::Verifiable for MapTile<'_> {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        v.visit_table(pos)?
            .visit_field::<i32>("origin_x", Self::VT_ORIGIN_X, false)?
            .visit_field::<i32>("origin_z", Self::VT_ORIGIN_Z, false)?
            .visit_field::<u8>("scale", Self::VT_SCALE, false)?
            .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, u8>>>(
                "height",
                Self::VT_HEIGHT,
                false,
            )?
            .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, u8>>>(
                "surface",
                Self::VT_SURFACE,
                false,
            )?
            .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, u8>>>(
                "explored",
                Self::VT_EXPLORED,
                false,
            )?
            .finish();
        Ok(())
    }
}
pub struct MapTileArgs<'a> {
    pub origin_x: i32,
    pub origin_z: i32,
    pub scale: u8,
    pub height: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, u8>>>,
    pub surface: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, u8>>>,
    pub explored: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, u8>>>,
}
impl<'a> Default for MapTileArgs<'a> {
    #[inline]
    fn default() -> Self {
        MapTileArgs {
            origin_x: 0,
            origin_z: 0,
            scale: 0,
            height: None,
            surface: None,
            explored: None,
        }
    }
}

pub struct MapTileBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
    fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> MapTileBuilder<'a, 'b, A> {
    #[inline]
    pub fn add_origin_x(&mut self, origin_x: i32) {
        self.fbb_
            .push_slot::<i32>(MapTile::VT_ORIGIN_X, origin_x, 0);
    }
    #[inline]
    pub fn add_origin_z(&mut self, origin_z: i32) {
        self.fbb_
            .push_slot::<i32>(MapTile::VT_ORIGIN_Z, origin_z, 0);
    }
    #[inline]
    pub fn add_scale(&mut self, scale: u8) {
        self.fbb_.push_slot::<u8>(MapTile::VT_SCALE, scale, 0);
    }
    #[inline]
    pub fn add_height(&mut self, height: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b, u8>>) {
        self.fbb_
            .push_slot_always::<::flatbuffers::WIPOffset<_>>(MapTile::VT_HEIGHT, height);
    }
    #[inline]
    pub fn add_surface(
        &mut self,
        surface: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b, u8>>,
    ) {
        self.fbb_
            .push_slot_always::<::flatbuffers::WIPOffset<_>>(MapTile::VT_SURFACE, surface);
    }
    #[inline]
    pub fn add_explored(
        &mut self,
        explored: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b, u8>>,
    ) {
        self.fbb_
            .push_slot_always::<::flatbuffers::WIPOffset<_>>(MapTile::VT_EXPLORED, explored);
    }
    #[inline]
    pub fn new(_fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>) -> MapTileBuilder<'a, 'b, A> {
        let start = _fbb.start_table();
        MapTileBuilder {
            fbb_: _fbb,
            start_: start,
        }
    }
    #[inline]
    pub fn finish(self) -> ::flatbuffers::WIPOffset<MapTile<'a>> {
        let o = self.fbb_.end_table(self.start_);
        ::flatbuffers::WIPOffset::new(o.value())
    }
}

impl ::core::fmt::Debug for MapTile<'_> {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        let mut ds = f.debug_struct("MapTile");
        ds.field("origin_x", &self.origin_x());
        ds.field("origin_z", &self.origin_z());
        ds.field("scale", &self.scale());
        ds.field("height", &self.height());
        ds.field("surface", &self.surface());
        ds.field("explored", &self.explored());
        ds.finish()
    }
}
