// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
pub enum MapTileRequestOffset {}
#[derive(Copy, Clone, PartialEq)]

/// A client asking for one square of the map. **A request for data, never for an
/// outcome.**
///
/// The same shape of message `ChunkResendRequest` is, and the same argument licenses it:
/// asking is not stating. Nothing here says what the terrain holds, what the character
/// has explored, or which pixels may be answered. The server decides all three, and a
/// request it will not serve is refused rather than answered with a smaller tile.
///
/// **A tile is always 64 x 64 pixels**, whatever the scale. `scale` is how many blocks
/// one pixel covers, so a tile spans `64 * scale` blocks on each axis: 64, 256 or 1024.
/// Fixing the pixel count rather than the block span is what keeps `MapTile`'s arrays a
/// constant 4096 entries at every zoom level, so neither side ever sizes an allocation
/// from a number a peer chose.
///
/// Invariants on this untrusted input:
///   - `scale` is exactly 1, 4 or 16. The absent-field zero fails closed, and so does
///     every other value: a scale is a member of a fixed set here, not a range
///   - `origin_x` and `origin_z` are block coordinates of the tile's minimum corner and
///     are exact multiples of `64 * scale`. The grid is what makes a tile cacheable and
///     what lets two requests for the same ground be the same tile
///   - `client_tick` is ordering and staleness only, exactly as in `PlayerInput`. The
///     server never reads it as a clock
///
/// A request that breaks either of the first two is refused and never answered with a
/// tile. The refusal vocabulary is `RefusedAction.RequestMapTile` with
/// `RefusalReason.TileMisaligned`; a decoder that can see the violation from the frame
/// alone may close the session on it instead, which is the stricter of the two answers
/// and the one the Go server gives. Both are refusals, and neither produces a `MapTile`.
pub struct MapTileRequest<'a> {
    pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for MapTileRequest<'a> {
    type Inner = MapTileRequest<'a>;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        Self {
            _tab: unsafe { ::flatbuffers::Table::new(buf, loc) },
        }
    }
}

impl<'a> MapTileRequest<'a> {
    pub const VT_ORIGIN_X: ::flatbuffers::VOffsetT = 4;
    pub const VT_ORIGIN_Z: ::flatbuffers::VOffsetT = 6;
    pub const VT_SCALE: ::flatbuffers::VOffsetT = 8;
    pub const VT_CLIENT_TICK: ::flatbuffers::VOffsetT = 10;

    #[inline]
    pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
        MapTileRequest { _tab: table }
    }
    #[allow(unused_mut)]
    pub fn create<
        'bldr: 'args,
        'args: 'mut_bldr,
        'mut_bldr,
        A: ::flatbuffers::Allocator + 'bldr,
    >(
        _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
        args: &'args MapTileRequestArgs,
    ) -> ::flatbuffers::WIPOffset<MapTileRequest<'bldr>> {
        let mut builder = MapTileRequestBuilder::new(_fbb);
        builder.add_client_tick(args.client_tick);
        builder.add_origin_z(args.origin_z);
        builder.add_origin_x(args.origin_x);
        builder.add_scale(args.scale);
        builder.finish()
    }

    /// Block x of the tile's minimum corner. A multiple of `64 * scale`.
    #[inline]
    pub fn origin_x(&self) -> i32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<i32>(MapTileRequest::VT_ORIGIN_X, Some(0))
                .unwrap()
        }
    }
    /// Block z of the tile's minimum corner. A multiple of `64 * scale`.
    #[inline]
    pub fn origin_z(&self) -> i32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<i32>(MapTileRequest::VT_ORIGIN_Z, Some(0))
                .unwrap()
        }
    }
    /// Blocks per pixel: 1, 4 or 16.
    #[inline]
    pub fn scale(&self) -> u8 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u8>(MapTileRequest::VT_SCALE, Some(0))
                .unwrap()
        }
    }
    /// The client's own tick counter. Ordering and staleness only.
    #[inline]
    pub fn client_tick(&self) -> u32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u32>(MapTileRequest::VT_CLIENT_TICK, Some(0))
                .unwrap()
        }
    }
}

impl ::flatbuffers::Verifiable for MapTileRequest<'_> {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        v.visit_table(pos)?
            .visit_field::<i32>("origin_x", Self::VT_ORIGIN_X, false)?
            .visit_field::<i32>("origin_z", Self::VT_ORIGIN_Z, false)?
            .visit_field::<u8>("scale", Self::VT_SCALE, false)?
            .visit_field::<u32>("client_tick", Self::VT_CLIENT_TICK, false)?
            .finish();
        Ok(())
    }
}
pub struct MapTileRequestArgs {
    pub origin_x: i32,
    pub origin_z: i32,
    pub scale: u8,
    pub client_tick: u32,
}
impl<'a> Default for MapTileRequestArgs {
    #[inline]
    fn default() -> Self {
        MapTileRequestArgs {
            origin_x: 0,
            origin_z: 0,
            scale: 0,
            client_tick: 0,
        }
    }
}

pub struct MapTileRequestBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
    fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> MapTileRequestBuilder<'a, 'b, A> {
    #[inline]
    pub fn add_origin_x(&mut self, origin_x: i32) {
        self.fbb_
            .push_slot::<i32>(MapTileRequest::VT_ORIGIN_X, origin_x, 0);
    }
    #[inline]
    pub fn add_origin_z(&mut self, origin_z: i32) {
        self.fbb_
            .push_slot::<i32>(MapTileRequest::VT_ORIGIN_Z, origin_z, 0);
    }
    #[inline]
    pub fn add_scale(&mut self, scale: u8) {
        self.fbb_
            .push_slot::<u8>(MapTileRequest::VT_SCALE, scale, 0);
    }
    #[inline]
    pub fn add_client_tick(&mut self, client_tick: u32) {
        self.fbb_
            .push_slot::<u32>(MapTileRequest::VT_CLIENT_TICK, client_tick, 0);
    }
    #[inline]
    pub fn new(
        _fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    ) -> MapTileRequestBuilder<'a, 'b, A> {
        let start = _fbb.start_table();
        MapTileRequestBuilder {
            fbb_: _fbb,
            start_: start,
        }
    }
    #[inline]
    pub fn finish(self) -> ::flatbuffers::WIPOffset<MapTileRequest<'a>> {
        let o = self.fbb_.end_table(self.start_);
        ::flatbuffers::WIPOffset::new(o.value())
    }
}

impl ::core::fmt::Debug for MapTileRequest<'_> {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        let mut ds = f.debug_struct("MapTileRequest");
        ds.field("origin_x", &self.origin_x());
        ds.field("origin_z", &self.origin_z());
        ds.field("scale", &self.scale());
        ds.field("client_tick", &self.client_tick());
        ds.finish()
    }
}
