// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
pub enum EntitySnapshotOffset {}
#[derive(Copy, Clone, PartialEq)]

/// The authoritative state of every entity a session can see, for one tick.
///
/// The client renders from these, interpolating between the two most recent. It
/// does not correct them, rewind them, or override them with a local
/// simulation.
pub struct EntitySnapshot<'a> {
    pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for EntitySnapshot<'a> {
    type Inner = EntitySnapshot<'a>;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        Self {
            _tab: unsafe { ::flatbuffers::Table::new(buf, loc) },
        }
    }
}

impl<'a> EntitySnapshot<'a> {
    pub const VT_SERVER_TICK: ::flatbuffers::VOffsetT = 4;
    pub const VT_ENTITIES: ::flatbuffers::VOffsetT = 6;
    pub const VT_DROPS: ::flatbuffers::VOffsetT = 8;
    pub const VT_MOBS: ::flatbuffers::VOffsetT = 10;
    pub const VT_SELF_VITALS: ::flatbuffers::VOffsetT = 12;
    pub const VT_STRUCTURES: ::flatbuffers::VOffsetT = 14;
    pub const VT_TICK_OF_DAY: ::flatbuffers::VOffsetT = 16;
    pub const VT_DEAD_PLAYERS: ::flatbuffers::VOffsetT = 18;
    pub const VT_DROP_DURABILITIES: ::flatbuffers::VOffsetT = 20;
    pub const VT_PARTY_LEADER_ENTITY_ID: ::flatbuffers::VOffsetT = 22;
    pub const VT_PARTY_MEMBERS: ::flatbuffers::VOffsetT = 24;
    pub const VT_PARTY_ROSTER: ::flatbuffers::VOffsetT = 26;
    pub const VT_ACCESSIBLE_LOOT_CORPSES: ::flatbuffers::VOffsetT = 28;
    pub const VT_PROJECTILES: ::flatbuffers::VOffsetT = 30;
    pub const VT_BLOCKING_PLAYERS: ::flatbuffers::VOffsetT = 32;
    pub const VT_WEATHER: ::flatbuffers::VOffsetT = 34;
    pub const VT_MOUNTS: ::flatbuffers::VOffsetT = 36;
    pub const VT_SELF_CAST: ::flatbuffers::VOffsetT = 38;
    pub const VT_WORLD_TICK: ::flatbuffers::VOffsetT = 40;

    #[inline]
    pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
        EntitySnapshot { _tab: table }
    }
    #[allow(unused_mut)]
    pub fn create<
        'bldr: 'args,
        'args: 'mut_bldr,
        'mut_bldr,
        A: ::flatbuffers::Allocator + 'bldr,
    >(
        _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
        args: &'args EntitySnapshotArgs<'args>,
    ) -> ::flatbuffers::WIPOffset<EntitySnapshot<'bldr>> {
        let mut builder = EntitySnapshotBuilder::new(_fbb);
        builder.add_world_tick(args.world_tick);
        builder.add_party_leader_entity_id(args.party_leader_entity_id);
        if let Some(x) = args.self_cast {
            builder.add_self_cast(x);
        }
        if let Some(x) = args.mounts {
            builder.add_mounts(x);
        }
        if let Some(x) = args.weather {
            builder.add_weather(x);
        }
        if let Some(x) = args.blocking_players {
            builder.add_blocking_players(x);
        }
        if let Some(x) = args.projectiles {
            builder.add_projectiles(x);
        }
        if let Some(x) = args.accessible_loot_corpses {
            builder.add_accessible_loot_corpses(x);
        }
        if let Some(x) = args.party_roster {
            builder.add_party_roster(x);
        }
        if let Some(x) = args.party_members {
            builder.add_party_members(x);
        }
        if let Some(x) = args.drop_durabilities {
            builder.add_drop_durabilities(x);
        }
        if let Some(x) = args.dead_players {
            builder.add_dead_players(x);
        }
        builder.add_tick_of_day(args.tick_of_day);
        if let Some(x) = args.structures {
            builder.add_structures(x);
        }
        if let Some(x) = args.self_vitals {
            builder.add_self_vitals(x);
        }
        if let Some(x) = args.mobs {
            builder.add_mobs(x);
        }
        if let Some(x) = args.drops {
            builder.add_drops(x);
        }
        if let Some(x) = args.entities {
            builder.add_entities(x);
        }
        builder.add_server_tick(args.server_tick);
        builder.finish()
    }

    /// The server tick this snapshot describes. Monotonic per session.
    #[inline]
    pub fn server_tick(&self) -> u32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u32>(EntitySnapshot::VT_SERVER_TICK, Some(0))
                .unwrap()
        }
    }
    /// Every member has a non-zero `entity_id`, and no id occurs twice. Sparse vectors
    /// such as `mounts` key back to this one complete player set.
    #[inline]
    pub fn entities(&self) -> Option<::flatbuffers::Vector<'a, EntityState>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, EntityState>>>(
                    EntitySnapshot::VT_ENTITIES,
                    None,
                )
        }
    }
    /// Dropped items visible to this session. Empty when there are none; kept
    /// separate from `entities` so the hot player struct remains unchanged.
    #[inline]
    pub fn drops(&self) -> Option<::flatbuffers::Vector<'a, ItemDropState>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, ItemDropState>>>(
                    EntitySnapshot::VT_DROPS,
                    None,
                )
        }
    }
    /// Mobs visible to this session, under the same chunk-visibility rule the players
    /// and drops above obey. Empty when there are none, and the newest snapshot is the
    /// **complete** existence set: a mob the server stops sending has stopped existing
    /// for this session, and the client despawns it rather than inferring a reason.
    #[inline]
    pub fn mobs(
        &self,
    ) -> Option<::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<MobState<'a>>>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab.get::<::flatbuffers::ForwardsUOffset<
                ::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<MobState>>,
            >>(EntitySnapshot::VT_MOBS, None)
        }
    }
    /// This recipient's own health and life state.
    ///
    /// `(required)`, and it is the only required field in this contract. The rest of a
    /// snapshot describes what a session can *see*, which can legitimately be nothing;
    /// vitals describe the player the snapshot is addressed to, and there is no such
    /// thing as a session with no player.
    ///
    /// **The attribute is enforced on one side only, and it is worth knowing which.**
    /// flatc's Rust output visits the field with its required flag set, so the
    /// verifier `decode` runs refuses an absent one before any field of it is read.
    /// flatc's **Go** output emits no such assertion — `EntitySnapshotEnd` is a bare
    /// `EndObject` — so nothing stops a Go encoder from omitting it. What guarantees
    /// the server never does is that there is exactly one snapshot encoder and vitals
    /// are a plain field of the value it takes, so there is no call that can leave them
    /// out; `TestEntitySnapshotAlwaysCarriesTheRecipientsVitals` is what says so.
    #[inline]
    pub fn self_vitals(&self) -> PlayerVitals<'a> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<::flatbuffers::ForwardsUOffset<PlayerVitals>>(
                    EntitySnapshot::VT_SELF_VITALS,
                    None,
                )
                .unwrap()
        }
    }
    /// Structures visible to this session, under the same chunk-visibility rule the
    /// entities, drops and mobs above obey. Empty when there are none, and the newest
    /// snapshot is the **complete** existence set exactly as `mobs` is: a structure that
    /// stops being sent has stopped existing for this session, and the client despawns it
    /// rather than inferring a reason. Removed by its owner, collapsed under a broken
    /// block, or simply out of view are the same fact on the wire, and none of the three
    /// is a thing the client is entitled to distinguish.
    #[inline]
    pub fn structures(
        &self,
    ) -> Option<::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<StructureState<'a>>>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab.get::<::flatbuffers::ForwardsUOffset<
                ::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<StructureState>>,
            >>(EntitySnapshot::VT_STRUCTURES, None)
        }
    }
    /// The tick of the world's day this snapshot describes, when the server keeps a clock:
    /// always less than `ServerWelcome.day_length_ticks`, and not read at all when that
    /// value is zero.
    ///
    /// **The one field here that is not about an entity**, and it rides along rather than
    /// arriving as a message of its own because it changes every tick and is read by the
    /// same frame the entities are drawn from. A separate message would arrive on its own
    /// schedule and put the sky a tick away from the world underneath it.
    ///
    /// A value at or beyond a declared `day_length_ticks` is a protocol error, never
    /// wrapped and never clamped. The two numbers come from the same server in the same
    /// session, so a disagreement between them is a bug reporting itself, and a receiver
    /// that quietly repaired it would be hiding the only evidence.
    #[inline]
    pub fn tick_of_day(&self) -> u32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u32>(EntitySnapshot::VT_TICK_OF_DAY, Some(0))
                .unwrap()
        }
    }
    /// The entity ids of the players in `entities` the server currently holds dead.
    ///
    /// **A fact about the world, not an event.** It rides in every snapshot rather than in a
    /// message announcing a death, so a session that joined afterwards, or whose view has
    /// only just reached the body, is told exactly what the session that watched it happen
    /// was told. It says who is down *now*, and the newest snapshot is the complete answer in
    /// the same way `mobs` and `structures` are.
    ///
    /// **Why a sparse list of ids rather than a life state beside every player.**
    /// `EntityState` is a struct — inlined once per visible player per tick, and its field
    /// list can never be taken back — so the state cannot go in it; that argument is written
    /// above the struct itself. Of the two shapes that leaves, a table per player in the shape
    /// `MobState` took would charge every *living* player a vtable and an offset every tick to
    /// carry the answer "no", which is the answer almost every entry has almost all the time.
    /// This vector is empty on almost every tick of almost every session, and FlatBuffers
    /// writes nothing at all for an empty one, so an ordinary snapshot pays nothing for it and
    /// a death costs eight bytes for as long as it lasts.
    ///
    /// **The price is two structures that could disagree, and the invariants below are the
    /// answer.** They are enforced at the decode boundary rather than trusted, because both
    /// halves arrive in one frame from one server: a disagreement is a bug reporting itself,
    /// and a receiver that quietly repaired it would be hiding the only evidence — the
    /// reasoning `tick_of_day` records one field above, with the difference that this one
    /// *can* be checked where it lands, since nothing it names arrived in another message.
    ///
    /// **`self_vitals` stays the recipient's own authority and is not made redundant.** What
    /// this adds is the one bit of that state which is a property of a body standing in the
    /// world, so the recipient's own body and everybody else's are drawn from one statement
    /// instead of two that can drift apart.
    ///
    /// Decoder invariants:
    ///   - every id names a player in this snapshot's `entities`. An id for a player the
    ///     recipient cannot see is a protocol error, not a body to remember for later
    ///   - no id appears twice
    ///   - the recipient's own entity id is present **exactly when** `self_vitals.life_state`
    ///     is `Dead`. The two are the same fact stated once each way, and a frame in which
    ///     they disagree is refused
    ///   - absent and empty are the same thing, and both are ordinary: nobody in view is dead
    ///
    /// Nothing about how a body goes down is here. How long a fall takes, which way it tips,
    /// and what a receiver draws for a player it first sees already dead are presentation —
    /// the same division `MobAction.Dying` states for a creature.
    #[inline]
    pub fn dead_players(&self) -> Option<::flatbuffers::Vector<'a, u64>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, u64>>>(
                    EntitySnapshot::VT_DEAD_PLAYERS,
                    None,
                )
        }
    }
    /// Wear for the sparse subset of `drops` that came from durable inventory slots.
    /// Block yields, loot rolls and structure bundles carry no entry and remain wearless.
    /// See `ItemDropDurability` for the invariants tying the two vectors together.
    ///
    /// **V11 appends this field and changes no existing drop's fixed layout.** A V11
    /// receiver interprets absence as wearless, so a V10 server would silently turn a
    /// worn ground item pristine in presentation while the server later returned it worn.
    /// `ProtocolVersion.Current` therefore moves rather than allowing two peers to assign
    /// different state to the same drop after a clean handshake.
    #[inline]
    pub fn drop_durabilities(&self) -> Option<::flatbuffers::Vector<'a, ItemDropDurability>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab.get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, ItemDropDurability>>>(EntitySnapshot::VT_DROP_DURABILITIES, None)
        }
    }
    /// The current party leader's live entity id, or `0` when there is no party **or the
    /// stable leader at `party_roster[0]` is offline**.
    ///
    /// A non-zero leader may be the recipient itself or one of `party_members`. The
    /// decoder that sees only this frame compares it with the roster's first entry: an
    /// online leader must carry that same non-zero entity id, and an offline leader must
    /// carry zero. When the roster is empty this field and `party_members` are both empty.
    /// A non-zero leader may be the recipient and therefore absent from `party_members`.
    #[inline]
    pub fn party_leader_entity_id(&self) -> u64 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u64>(EntitySnapshot::VT_PARTY_LEADER_ENTITY_ID, Some(0))
                .unwrap()
        }
    }
    /// Every **other** member of the recipient's party, in authoritative party order.
    /// Empty when the recipient is in no party.
    ///
    /// **Why this widens a snapshot beyond ordinary visibility.** `dead_players` is sparse
    /// state only for bodies already present in `entities`, and `PlayerVitals` exposes one
    /// player's health only to that player. Party membership is explicit consent to share
    /// position and health with the other members, including outside the streamed view, so
    /// this vector is the narrow exception rather than a field on `EntityState` paid by every
    /// visible player. For everyone outside a party it is absent and costs zero bytes.
    ///
    /// Decoder invariants:
    ///   - no `entity_id` appears twice
    ///   - every member has finite `pos`, non-zero `max_health`, and
    ///     `health <= max_health`
    ///   - the recipient's own entity id never appears; this must be checked by a layer that
    ///     also holds `ServerWelcome.entity_id`, not by a frame-only decoder
    ///   - every member maps to an online non-zero entity in `party_roster`
    ///   - when the roster is empty this vector is empty
    ///   - a non-zero leader is either the recipient or one of the members; the
    ///     recipient-aware consumer checks which when it has the welcome and snapshot together
    #[inline]
    pub fn party_members(&self) -> Option<::flatbuffers::Vector<'a, PartyMemberState>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, PartyMemberState>>>(
                    EntitySnapshot::VT_PARTY_MEMBERS,
                    None,
                )
        }
    }
    /// The complete ordered party roster, including the recipient. Empty means no party;
    /// otherwise the first entry is the leader. Unlike `party_members`, this remains complete
    /// when a member disconnects and uses stable character identity for every position.
    ///
    /// `party_leader_entity_id` is the first entry's live entity id while the leader is
    /// online and zero while that leader is offline. Zero therefore no longer implies that
    /// `party_members` or this roster is empty; it never names an online entity.
    #[inline]
    pub fn party_roster(
        &self,
    ) -> Option<::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<PartyRosterMember<'a>>>>
    {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab.get::<::flatbuffers::ForwardsUOffset<
                ::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<PartyRosterMember>>,
            >>(EntitySnapshot::VT_PARTY_ROSTER, None)
        }
    }
    /// Corpse ids that still hold loot this recipient may access. Each id is exactly the
    /// `entity_id` of one `mobs` entry whose action is `Corpse`; a receiver rejects an id
    /// that names no such body. Complete per snapshot:
    /// a later vector replaces the former one, absence and empty both mean none, and ids
    /// are non-zero and unique. It carries access, never contents; opening one asks the
    /// server for a per-recipient `LootState`.
    #[inline]
    pub fn accessible_loot_corpses(&self) -> Option<::flatbuffers::Vector<'a, u64>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, u64>>>(
                    EntitySnapshot::VT_ACCESSIBLE_LOOT_CORPSES,
                    None,
                )
        }
    }
    /// Projectiles visible to this session. Absence and empty both mean that no projectile
    /// is in view. An arrow resting in terrain remains a projectile with zero velocity until
    /// the authoritative server drops it from this complete per-snapshot set.
    #[inline]
    pub fn projectiles(&self) -> Option<::flatbuffers::Vector<'a, ProjectileState>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, ProjectileState>>>(
                    EntitySnapshot::VT_PROJECTILES,
                    None,
                )
        }
    }
    /// The entity ids of players in `entities` whose off-hand the server currently holds
    /// raised. Sparse like `dead_players`: absence and empty both mean nobody in view is
    /// blocking, and the newest snapshot is the complete answer.
    ///
    /// Decoder invariants:
    ///   - every id names a player in this snapshot's `entities`
    ///   - no id appears twice
    ///   - the recipient's own entity id is present exactly when `self_vitals.blocking`
    ///     is true; a frame where the two statements disagree is refused
    ///
    /// A consumer may leave both append-only blocking fields unread. Once it reads either
    /// for presentation, it validates this complete set and the cross-field agreement first.
    #[inline]
    pub fn blocking_players(&self) -> Option<::flatbuffers::Vector<'a, u64>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'a, u64>>>(
                    EntitySnapshot::VT_BLOCKING_PLAYERS,
                    None,
                )
        }
    }
    /// What the sky is doing at **the recipient's own position**, this tick. Absent from a
    /// server that keeps no weather at all.
    ///
    /// **The second field here that is not about an entity**, and it rides along for the
    /// reason `tick_of_day` does: it changes every tick and is read by the same frame the
    /// world is drawn from, so a message of its own would arrive on its own schedule and
    /// put the rain a tick away from the ground it is falling on. One value rather than a
    /// field, a front or a map, because the only weather this session has to draw is the
    /// weather it is standing in.
    ///
    /// **Authoritative, not presentation, and the split runs through the middle of it.**
    /// The server has already applied everything this state *does* — the cold, the slowed
    /// step, the doused fire, what a blizzard hides — and those outcomes arrive as vitals,
    /// as position and as `StructureState.lit`. What is left for the client is everything
    /// it *draws*: particles, fog, wind, sound. A client that re-derived an effect from
    /// this field would be deciding a gameplay outcome from presentation data.
    ///
    /// Absence says this server keeps no weather, which a test world and an older server
    /// both legitimately are; a *present* struct carrying `WeatherKind.Unknown` is a
    /// protocol error. `WeatherState` sets both cases out.
    #[inline]
    pub fn weather(&self) -> Option<&'a WeatherState> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<WeatherState>(EntitySnapshot::VT_WEATHER, None)
        }
    }
    /// Mounted players in `entities`, keyed by `MountState.entity_id`. Sparse and
    /// complete: absence and empty both mean nobody in this snapshot is mounted.
    #[inline]
    pub fn mounts(
        &self,
    ) -> Option<::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<MountState<'a>>>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab.get::<::flatbuffers::ForwardsUOffset<
                ::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<MountState>>,
            >>(EntitySnapshot::VT_MOUNTS, None)
        }
    }
    /// This recipient's own cast, when one is running. A later snapshot supersedes it.
    #[inline]
    pub fn self_cast(&self) -> Option<CastState<'a>> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab.get::<::flatbuffers::ForwardsUOffset<CastState>>(
                EntitySnapshot::VT_SELF_CAST,
                None,
            )
        }
    }
    /// How many authoritative ticks this world has lived through, across every restart.
    /// This is the absolute half of `tick_of_day`: when the welcome declares a clock,
    /// `world_tick % day_length_ticks == tick_of_day`. A receiver refuses the whole frame
    /// when the pair disagrees rather than deriving or wrapping either half, because the
    /// disagreement is the only evidence that the server emitted a non-atomic clock.
    ///
    /// A clockless server carries zero. Unlike `server_tick`, this value is persisted world
    /// time rather than process uptime, so every client connected to the same world derives
    /// the same multi-day presentation and a restart resumes it instead of beginning again.
    ///
    /// Appended in V29. An older server's absent scalar would read as a fresh world's zero,
    /// which is a plausible but false lunar phase; the protocol version therefore moves.
    #[inline]
    pub fn world_tick(&self) -> u64 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u64>(EntitySnapshot::VT_WORLD_TICK, Some(0))
                .unwrap()
        }
    }
}

impl ::flatbuffers::Verifiable for EntitySnapshot<'_> {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        v.visit_table(pos)?
     .visit_field::<u32>("server_tick", Self::VT_SERVER_TICK, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, EntityState>>>("entities", Self::VT_ENTITIES, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, ItemDropState>>>("drops", Self::VT_DROPS, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, ::flatbuffers::ForwardsUOffset<MobState>>>>("mobs", Self::VT_MOBS, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<PlayerVitals>>("self_vitals", Self::VT_SELF_VITALS, true)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, ::flatbuffers::ForwardsUOffset<StructureState>>>>("structures", Self::VT_STRUCTURES, false)?
     .visit_field::<u32>("tick_of_day", Self::VT_TICK_OF_DAY, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, u64>>>("dead_players", Self::VT_DEAD_PLAYERS, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, ItemDropDurability>>>("drop_durabilities", Self::VT_DROP_DURABILITIES, false)?
     .visit_field::<u64>("party_leader_entity_id", Self::VT_PARTY_LEADER_ENTITY_ID, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, PartyMemberState>>>("party_members", Self::VT_PARTY_MEMBERS, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, ::flatbuffers::ForwardsUOffset<PartyRosterMember>>>>("party_roster", Self::VT_PARTY_ROSTER, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, u64>>>("accessible_loot_corpses", Self::VT_ACCESSIBLE_LOOT_CORPSES, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, ProjectileState>>>("projectiles", Self::VT_PROJECTILES, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, u64>>>("blocking_players", Self::VT_BLOCKING_PLAYERS, false)?
     .visit_field::<WeatherState>("weather", Self::VT_WEATHER, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<::flatbuffers::Vector<'_, ::flatbuffers::ForwardsUOffset<MountState>>>>("mounts", Self::VT_MOUNTS, false)?
     .visit_field::<::flatbuffers::ForwardsUOffset<CastState>>("self_cast", Self::VT_SELF_CAST, false)?
     .visit_field::<u64>("world_tick", Self::VT_WORLD_TICK, false)?
     .finish();
        Ok(())
    }
}
pub struct EntitySnapshotArgs<'a> {
    pub server_tick: u32,
    pub entities: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, EntityState>>>,
    pub drops: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, ItemDropState>>>,
    pub mobs: Option<
        ::flatbuffers::WIPOffset<
            ::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<MobState<'a>>>,
        >,
    >,
    pub self_vitals: Option<::flatbuffers::WIPOffset<PlayerVitals<'a>>>,
    pub structures: Option<
        ::flatbuffers::WIPOffset<
            ::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<StructureState<'a>>>,
        >,
    >,
    pub tick_of_day: u32,
    pub dead_players: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, u64>>>,
    pub drop_durabilities:
        Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, ItemDropDurability>>>,
    pub party_leader_entity_id: u64,
    pub party_members:
        Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, PartyMemberState>>>,
    pub party_roster: Option<
        ::flatbuffers::WIPOffset<
            ::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<PartyRosterMember<'a>>>,
        >,
    >,
    pub accessible_loot_corpses: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, u64>>>,
    pub projectiles: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, ProjectileState>>>,
    pub blocking_players: Option<::flatbuffers::WIPOffset<::flatbuffers::Vector<'a, u64>>>,
    pub weather: Option<&'a WeatherState>,
    pub mounts: Option<
        ::flatbuffers::WIPOffset<
            ::flatbuffers::Vector<'a, ::flatbuffers::ForwardsUOffset<MountState<'a>>>,
        >,
    >,
    pub self_cast: Option<::flatbuffers::WIPOffset<CastState<'a>>>,
    pub world_tick: u64,
}
impl<'a> Default for EntitySnapshotArgs<'a> {
    #[inline]
    fn default() -> Self {
        EntitySnapshotArgs {
            server_tick: 0,
            entities: None,
            drops: None,
            mobs: None,
            self_vitals: None, // required field
            structures: None,
            tick_of_day: 0,
            dead_players: None,
            drop_durabilities: None,
            party_leader_entity_id: 0,
            party_members: None,
            party_roster: None,
            accessible_loot_corpses: None,
            projectiles: None,
            blocking_players: None,
            weather: None,
            mounts: None,
            self_cast: None,
            world_tick: 0,
        }
    }
}

pub struct EntitySnapshotBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
    fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> EntitySnapshotBuilder<'a, 'b, A> {
    #[inline]
    pub fn add_server_tick(&mut self, server_tick: u32) {
        self.fbb_
            .push_slot::<u32>(EntitySnapshot::VT_SERVER_TICK, server_tick, 0);
    }
    #[inline]
    pub fn add_entities(
        &mut self,
        entities: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b, EntityState>>,
    ) {
        self.fbb_
            .push_slot_always::<::flatbuffers::WIPOffset<_>>(EntitySnapshot::VT_ENTITIES, entities);
    }
    #[inline]
    pub fn add_drops(
        &mut self,
        drops: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b, ItemDropState>>,
    ) {
        self.fbb_
            .push_slot_always::<::flatbuffers::WIPOffset<_>>(EntitySnapshot::VT_DROPS, drops);
    }
    #[inline]
    pub fn add_mobs(
        &mut self,
        mobs: ::flatbuffers::WIPOffset<
            ::flatbuffers::Vector<'b, ::flatbuffers::ForwardsUOffset<MobState<'b>>>,
        >,
    ) {
        self.fbb_
            .push_slot_always::<::flatbuffers::WIPOffset<_>>(EntitySnapshot::VT_MOBS, mobs);
    }
    #[inline]
    pub fn add_self_vitals(&mut self, self_vitals: ::flatbuffers::WIPOffset<PlayerVitals<'b>>) {
        self.fbb_
            .push_slot_always::<::flatbuffers::WIPOffset<PlayerVitals>>(
                EntitySnapshot::VT_SELF_VITALS,
                self_vitals,
            );
    }
    #[inline]
    pub fn add_structures(
        &mut self,
        structures: ::flatbuffers::WIPOffset<
            ::flatbuffers::Vector<'b, ::flatbuffers::ForwardsUOffset<StructureState<'b>>>,
        >,
    ) {
        self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(
            EntitySnapshot::VT_STRUCTURES,
            structures,
        );
    }
    #[inline]
    pub fn add_tick_of_day(&mut self, tick_of_day: u32) {
        self.fbb_
            .push_slot::<u32>(EntitySnapshot::VT_TICK_OF_DAY, tick_of_day, 0);
    }
    #[inline]
    pub fn add_dead_players(
        &mut self,
        dead_players: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b, u64>>,
    ) {
        self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(
            EntitySnapshot::VT_DEAD_PLAYERS,
            dead_players,
        );
    }
    #[inline]
    pub fn add_drop_durabilities(
        &mut self,
        drop_durabilities: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b, ItemDropDurability>>,
    ) {
        self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(
            EntitySnapshot::VT_DROP_DURABILITIES,
            drop_durabilities,
        );
    }
    #[inline]
    pub fn add_party_leader_entity_id(&mut self, party_leader_entity_id: u64) {
        self.fbb_.push_slot::<u64>(
            EntitySnapshot::VT_PARTY_LEADER_ENTITY_ID,
            party_leader_entity_id,
            0,
        );
    }
    #[inline]
    pub fn add_party_members(
        &mut self,
        party_members: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b, PartyMemberState>>,
    ) {
        self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(
            EntitySnapshot::VT_PARTY_MEMBERS,
            party_members,
        );
    }
    #[inline]
    pub fn add_party_roster(
        &mut self,
        party_roster: ::flatbuffers::WIPOffset<
            ::flatbuffers::Vector<'b, ::flatbuffers::ForwardsUOffset<PartyRosterMember<'b>>>,
        >,
    ) {
        self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(
            EntitySnapshot::VT_PARTY_ROSTER,
            party_roster,
        );
    }
    #[inline]
    pub fn add_accessible_loot_corpses(
        &mut self,
        accessible_loot_corpses: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b, u64>>,
    ) {
        self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(
            EntitySnapshot::VT_ACCESSIBLE_LOOT_CORPSES,
            accessible_loot_corpses,
        );
    }
    #[inline]
    pub fn add_projectiles(
        &mut self,
        projectiles: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b, ProjectileState>>,
    ) {
        self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(
            EntitySnapshot::VT_PROJECTILES,
            projectiles,
        );
    }
    #[inline]
    pub fn add_blocking_players(
        &mut self,
        blocking_players: ::flatbuffers::WIPOffset<::flatbuffers::Vector<'b, u64>>,
    ) {
        self.fbb_.push_slot_always::<::flatbuffers::WIPOffset<_>>(
            EntitySnapshot::VT_BLOCKING_PLAYERS,
            blocking_players,
        );
    }
    #[inline]
    pub fn add_weather(&mut self, weather: &WeatherState) {
        self.fbb_
            .push_slot_always::<&WeatherState>(EntitySnapshot::VT_WEATHER, weather);
    }
    #[inline]
    pub fn add_mounts(
        &mut self,
        mounts: ::flatbuffers::WIPOffset<
            ::flatbuffers::Vector<'b, ::flatbuffers::ForwardsUOffset<MountState<'b>>>,
        >,
    ) {
        self.fbb_
            .push_slot_always::<::flatbuffers::WIPOffset<_>>(EntitySnapshot::VT_MOUNTS, mounts);
    }
    #[inline]
    pub fn add_self_cast(&mut self, self_cast: ::flatbuffers::WIPOffset<CastState<'b>>) {
        self.fbb_
            .push_slot_always::<::flatbuffers::WIPOffset<CastState>>(
                EntitySnapshot::VT_SELF_CAST,
                self_cast,
            );
    }
    #[inline]
    pub fn add_world_tick(&mut self, world_tick: u64) {
        self.fbb_
            .push_slot::<u64>(EntitySnapshot::VT_WORLD_TICK, world_tick, 0);
    }
    #[inline]
    pub fn new(
        _fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    ) -> EntitySnapshotBuilder<'a, 'b, A> {
        let start = _fbb.start_table();
        EntitySnapshotBuilder {
            fbb_: _fbb,
            start_: start,
        }
    }
    #[inline]
    pub fn finish(self) -> ::flatbuffers::WIPOffset<EntitySnapshot<'a>> {
        let o = self.fbb_.end_table(self.start_);
        self.fbb_
            .required(o, EntitySnapshot::VT_SELF_VITALS, "self_vitals");
        ::flatbuffers::WIPOffset::new(o.value())
    }
}

impl ::core::fmt::Debug for EntitySnapshot<'_> {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        let mut ds = f.debug_struct("EntitySnapshot");
        ds.field("server_tick", &self.server_tick());
        ds.field("entities", &self.entities());
        ds.field("drops", &self.drops());
        ds.field("mobs", &self.mobs());
        ds.field("self_vitals", &self.self_vitals());
        ds.field("structures", &self.structures());
        ds.field("tick_of_day", &self.tick_of_day());
        ds.field("dead_players", &self.dead_players());
        ds.field("drop_durabilities", &self.drop_durabilities());
        ds.field("party_leader_entity_id", &self.party_leader_entity_id());
        ds.field("party_members", &self.party_members());
        ds.field("party_roster", &self.party_roster());
        ds.field("accessible_loot_corpses", &self.accessible_loot_corpses());
        ds.field("projectiles", &self.projectiles());
        ds.field("blocking_players", &self.blocking_players());
        ds.field("weather", &self.weather());
        ds.field("mounts", &self.mounts());
        ds.field("self_cast", &self.self_cast());
        ds.field("world_tick", &self.world_tick());
        ds.finish()
    }
}
