// automatically generated by the FlatBuffers compiler, do not modify
// @generated
extern crate alloc;
use super::*;
pub enum BlockEditRequestOffset {}
#[derive(Copy, Clone, PartialEq)]

/// A client asking for one voxel to change. **Intent, never outcome.**
///
/// The world's counterpart to `PlayerInput`: it says what the player is trying
/// to do, and the server decides what actually happens. Reach, line of sight,
/// whether the player is holding what they are placing, whether the chunk is
/// protected — all of it is resolved server-side, and none of it is stated
/// here. Breaking uses `MineRequest`, because the server must accumulate the
/// target's hardness on its own tick. A field the client fills in is a field the
/// server would only have to disbelieve.
///
/// **There is no rejection message, and that is the design rather than a gap in
/// it.** A request the server refuses produces no `BlockUpdate`, no error
/// payload, and no acknowledgement of any kind: the absence is the answer. So a
/// client must never treat its own request as applied. It may predict the edit
/// locally for feel, but the world it is entitled to believe in is the one the
/// server has described, and a request that goes unanswered means the world did
/// not change. There is deliberately no request id in this table, because there
/// is no reply to correlate one with.
///
/// Invariants the server enforces on this untrusted input:
///   - `pos` is a struct field, so an absent one decodes as null (Go) / `None`
///     (Rust). Reject that. The origin is a real location, so treating a missing
///     coordinate as (0, 0, 0) would silently edit the world at a point nobody
///     named
///   - `action` must be `Place`; both `Unknown` and the retired `Break` value are
///     protocol errors
///   - `slot` must be < `ServerWelcome.inventory_slots`. On `Place`, the server
///     reads that slot from its authoritative inventory and the item there must
///     name a block the server permits a player to place
///   - `pos` is unbounded on the wire and bounded by the server. An edit beyond
///     the player's reach, or inside a chunk this session was never sent, is
///     refused — silently, like every other refusal
pub struct BlockEditRequest<'a> {
    pub _tab: ::flatbuffers::Table<'a>,
}

impl<'a> ::flatbuffers::Follow<'a> for BlockEditRequest<'a> {
    type Inner = BlockEditRequest<'a>;
    #[inline]
    unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
        Self {
            _tab: unsafe { ::flatbuffers::Table::new(buf, loc) },
        }
    }
}

impl<'a> BlockEditRequest<'a> {
    pub const VT_POS: ::flatbuffers::VOffsetT = 4;
    pub const VT_ACTION: ::flatbuffers::VOffsetT = 6;
    pub const VT_SLOT: ::flatbuffers::VOffsetT = 8;
    pub const VT_CLIENT_TICK: ::flatbuffers::VOffsetT = 10;

    #[inline]
    pub unsafe fn init_from_table(table: ::flatbuffers::Table<'a>) -> Self {
        BlockEditRequest { _tab: table }
    }
    #[allow(unused_mut)]
    pub fn create<
        'bldr: 'args,
        'args: 'mut_bldr,
        'mut_bldr,
        A: ::flatbuffers::Allocator + 'bldr,
    >(
        _fbb: &'mut_bldr mut ::flatbuffers::FlatBufferBuilder<'bldr, A>,
        args: &'args BlockEditRequestArgs<'args>,
    ) -> ::flatbuffers::WIPOffset<BlockEditRequest<'bldr>> {
        let mut builder = BlockEditRequestBuilder::new(_fbb);
        builder.add_client_tick(args.client_tick);
        if let Some(x) = args.pos {
            builder.add_pos(x);
        }
        builder.add_slot(args.slot);
        builder.add_action(args.action);
        builder.finish()
    }

    /// The voxel the player is aiming at, in world coordinates.
    #[inline]
    pub fn pos(&self) -> Option<&'a BlockCoord> {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe { self._tab.get::<BlockCoord>(BlockEditRequest::VT_POS, None) }
    }
    /// Place only. `Unknown` and the reserved `Break` value fail closed; see the
    /// invariants above.
    #[inline]
    pub fn action(&self) -> EditAction {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<EditAction>(BlockEditRequest::VT_ACTION, Some(EditAction::Unknown))
                .unwrap()
        }
    }
    /// The authoritative inventory slot to spend on a placement.
    #[inline]
    pub fn slot(&self) -> u8 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u8>(BlockEditRequest::VT_SLOT, Some(0))
                .unwrap()
        }
    }
    /// The client's own tick counter, exactly as in `PlayerInput`: ordering and
    /// staleness only. The server never trusts it as a clock, and never lets it
    /// decide whether the edit was legal at the moment the client claims to have
    /// sent it.
    #[inline]
    pub fn client_tick(&self) -> u32 {
        // Safety:
        // Created from valid Table for this object
        // which contains a valid value in this slot
        unsafe {
            self._tab
                .get::<u32>(BlockEditRequest::VT_CLIENT_TICK, Some(0))
                .unwrap()
        }
    }
}

impl ::flatbuffers::Verifiable for BlockEditRequest<'_> {
    #[inline]
    fn run_verifier(
        v: &mut ::flatbuffers::Verifier,
        pos: usize,
    ) -> Result<(), ::flatbuffers::InvalidFlatbuffer> {
        v.visit_table(pos)?
            .visit_field::<BlockCoord>("pos", Self::VT_POS, false)?
            .visit_field::<EditAction>("action", Self::VT_ACTION, false)?
            .visit_field::<u8>("slot", Self::VT_SLOT, false)?
            .visit_field::<u32>("client_tick", Self::VT_CLIENT_TICK, false)?
            .finish();
        Ok(())
    }
}
pub struct BlockEditRequestArgs<'a> {
    pub pos: Option<&'a BlockCoord>,
    pub action: EditAction,
    pub slot: u8,
    pub client_tick: u32,
}
impl<'a> Default for BlockEditRequestArgs<'a> {
    #[inline]
    fn default() -> Self {
        BlockEditRequestArgs {
            pos: None,
            action: EditAction::Unknown,
            slot: 0,
            client_tick: 0,
        }
    }
}

pub struct BlockEditRequestBuilder<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> {
    fbb_: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    start_: ::flatbuffers::WIPOffset<::flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b, A: ::flatbuffers::Allocator + 'a> BlockEditRequestBuilder<'a, 'b, A> {
    #[inline]
    pub fn add_pos(&mut self, pos: &BlockCoord) {
        self.fbb_
            .push_slot_always::<&BlockCoord>(BlockEditRequest::VT_POS, pos);
    }
    #[inline]
    pub fn add_action(&mut self, action: EditAction) {
        self.fbb_
            .push_slot::<EditAction>(BlockEditRequest::VT_ACTION, action, EditAction::Unknown);
    }
    #[inline]
    pub fn add_slot(&mut self, slot: u8) {
        self.fbb_
            .push_slot::<u8>(BlockEditRequest::VT_SLOT, slot, 0);
    }
    #[inline]
    pub fn add_client_tick(&mut self, client_tick: u32) {
        self.fbb_
            .push_slot::<u32>(BlockEditRequest::VT_CLIENT_TICK, client_tick, 0);
    }
    #[inline]
    pub fn new(
        _fbb: &'b mut ::flatbuffers::FlatBufferBuilder<'a, A>,
    ) -> BlockEditRequestBuilder<'a, 'b, A> {
        let start = _fbb.start_table();
        BlockEditRequestBuilder {
            fbb_: _fbb,
            start_: start,
        }
    }
    #[inline]
    pub fn finish(self) -> ::flatbuffers::WIPOffset<BlockEditRequest<'a>> {
        let o = self.fbb_.end_table(self.start_);
        ::flatbuffers::WIPOffset::new(o.value())
    }
}

impl ::core::fmt::Debug for BlockEditRequest<'_> {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        let mut ds = f.debug_struct("BlockEditRequest");
        ds.field("pos", &self.pos());
        ds.field("action", &self.action());
        ds.field("slot", &self.slot());
        ds.field("client_tick", &self.client_tick());
        ds.finish()
    }
}
