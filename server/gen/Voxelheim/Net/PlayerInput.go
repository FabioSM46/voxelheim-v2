// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package Net

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

// / One tick's worth of intent from a client.
// /
// / Sent at `ServerWelcome.tick_rate`, not at the client's frame rate. The
// / server clamps the axes and integrates them itself; a client that sends
// / out-of-range values gains nothing.
// /
// / **Decoder invariant: every float here must be finite.** This is a different
// / requirement from the range clamp, and clamping cannot satisfy it. `NaN`
// / compares false against every bound, so the usual `if v > 1 { v = 1 }` lets it
// / through untouched, and it then propagates through the integrator into a
// / position that stays `NaN` forever. The server rejects or zeroes non-finite
// / components **before** any physics runs, treating them as the malformed input
// / they are rather than as an extreme value.
type PlayerInput struct {
	_tab flatbuffers.Table
}

func GetRootAsPlayerInput(buf []byte, offset flatbuffers.UOffsetT) *PlayerInput {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &PlayerInput{}
	x.Init(buf, n+offset)
	return x
}

func FinishPlayerInputBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.Finish(offset)
}

func GetSizePrefixedRootAsPlayerInput(buf []byte, offset flatbuffers.UOffsetT) *PlayerInput {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &PlayerInput{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func FinishSizePrefixedPlayerInputBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.FinishSizePrefixed(offset)
}

func (rcv *PlayerInput) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *PlayerInput) Table() flatbuffers.Table {
	return rcv._tab
}

// / The client's own tick counter. Used for ordering and staleness checks; the
// / server never trusts it as a clock.
func (rcv *PlayerInput) ClientTick() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

// / The client's own tick counter. Used for ordering and staleness checks; the
// / server never trusts it as a clock.
func (rcv *PlayerInput) MutateClientTick(n uint32) bool {
	return rcv._tab.MutateUint32Slot(4, n)
}

// / Strafe intent, -1 (left) to 1 (right). Clamped server-side.
func (rcv *PlayerInput) MoveX() float32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		return rcv._tab.GetFloat32(o + rcv._tab.Pos)
	}
	return 0.0
}

// / Strafe intent, -1 (left) to 1 (right). Clamped server-side.
func (rcv *PlayerInput) MutateMoveX(n float32) bool {
	return rcv._tab.MutateFloat32Slot(6, n)
}

// / Forward intent, -1 (backward) to 1 (forward). Clamped server-side.
func (rcv *PlayerInput) MoveZ() float32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		return rcv._tab.GetFloat32(o + rcv._tab.Pos)
	}
	return 0.0
}

// / Forward intent, -1 (backward) to 1 (forward). Clamped server-side.
func (rcv *PlayerInput) MutateMoveZ(n float32) bool {
	return rcv._tab.MutateFloat32Slot(8, n)
}

// / Facing, in radians. Affects the direction the movement axes are applied
// / in, so the server needs it even though the camera is a client concern.
func (rcv *PlayerInput) Yaw() float32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(10))
	if o != 0 {
		return rcv._tab.GetFloat32(o + rcv._tab.Pos)
	}
	return 0.0
}

// / Facing, in radians. Affects the direction the movement axes are applied
// / in, so the server needs it even though the camera is a client concern.
func (rcv *PlayerInput) MutateYaw(n float32) bool {
	return rcv._tab.MutateFloat32Slot(10, n)
}

// / Pitch, in radians. Carried for aiming; movement ignores it.
func (rcv *PlayerInput) Pitch() float32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(12))
	if o != 0 {
		return rcv._tab.GetFloat32(o + rcv._tab.Pos)
	}
	return 0.0
}

// / Pitch, in radians. Carried for aiming; movement ignores it.
func (rcv *PlayerInput) MutatePitch(n float32) bool {
	return rcv._tab.MutateFloat32Slot(12, n)
}

// / Whether the jump control is held this tick. Whether a jump *happens* is
// / the server's decision (it owns ground contact).
func (rcv *PlayerInput) Jump() bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(14))
	if o != 0 {
		return rcv._tab.GetBool(o + rcv._tab.Pos)
	}
	return false
}

// / Whether the jump control is held this tick. Whether a jump *happens* is
// / the server's decision (it owns ground contact).
func (rcv *PlayerInput) MutateJump(n bool) bool {
	return rcv._tab.MutateBoolSlot(14, n)
}

func PlayerInputStart(builder *flatbuffers.Builder) {
	builder.StartObject(6)
}
func PlayerInputAddClientTick(builder *flatbuffers.Builder, clientTick uint32) {
	builder.PrependUint32Slot(0, clientTick, 0)
}
func PlayerInputAddMoveX(builder *flatbuffers.Builder, moveX float32) {
	builder.PrependFloat32Slot(1, moveX, 0.0)
}
func PlayerInputAddMoveZ(builder *flatbuffers.Builder, moveZ float32) {
	builder.PrependFloat32Slot(2, moveZ, 0.0)
}
func PlayerInputAddYaw(builder *flatbuffers.Builder, yaw float32) {
	builder.PrependFloat32Slot(3, yaw, 0.0)
}
func PlayerInputAddPitch(builder *flatbuffers.Builder, pitch float32) {
	builder.PrependFloat32Slot(4, pitch, 0.0)
}
func PlayerInputAddJump(builder *flatbuffers.Builder, jump bool) {
	builder.PrependBoolSlot(5, jump, false)
}
func PlayerInputEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
