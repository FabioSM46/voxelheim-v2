// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package Net

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

// / One cubic chunk of voxels, run-length encoded.
// /
// / **Voxel order** is x fastest, then z, then y:
// /
// /     index = (y * chunk_size + z) * chunk_size + x
// /
// / This ordering is part of the wire contract, not an implementation detail:
// / the server encodes in it and the client's mesher indexes in it. Changing it
// / is a protocol version bump.
// /
// / **Encoding**: `runs` is a flat vector of (block_id, run_length) pairs —
// / `runs[2i]` is the block id and `runs[2i+1]` is how many consecutive voxels
// / carry it, in the index order above. The pairs are flat rather than a vector
// / of tables because a table per run would cost an offset each, and a terrain
// / chunk holds thousands of runs.
// /
// / Invariants a decoder must enforce on untrusted input:
// /   - `runs` has an even length
// /   - no run length is 0
// /   - the run lengths sum to exactly `chunk_size^3`
// /
// / A single run can span an entire chunk while `chunk_size^3` fits in a
// / `ushort` (32^3 = 32768 ≤ 65535). Raising `chunk_size` above 40 breaks that
// / and requires widening the run length — and therefore a version bump.
type ChunkData struct {
	_tab flatbuffers.Table
}

func GetRootAsChunkData(buf []byte, offset flatbuffers.UOffsetT) *ChunkData {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &ChunkData{}
	x.Init(buf, n+offset)
	return x
}

func FinishChunkDataBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.Finish(offset)
}

func GetSizePrefixedRootAsChunkData(buf []byte, offset flatbuffers.UOffsetT) *ChunkData {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &ChunkData{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func FinishSizePrefixedChunkDataBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.FinishSizePrefixed(offset)
}

func (rcv *ChunkData) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *ChunkData) Table() flatbuffers.Table {
	return rcv._tab
}

func (rcv *ChunkData) Coord(obj *ChunkCoord) *ChunkCoord {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		x := o + rcv._tab.Pos
		if obj == nil {
			obj = new(ChunkCoord)
		}
		obj.Init(rcv._tab.Bytes, x)
		return obj
	}
	return nil
}

func (rcv *ChunkData) Runs(j int) uint16 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return rcv._tab.GetUint16(a + flatbuffers.UOffsetT(j*2))
	}
	return 0
}

func (rcv *ChunkData) RunsLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *ChunkData) MutateRuns(j int, n uint16) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return rcv._tab.MutateUint16(a+flatbuffers.UOffsetT(j*2), n)
	}
	return false
}

func ChunkDataStart(builder *flatbuffers.Builder) {
	builder.StartObject(2)
}
func ChunkDataAddCoord(builder *flatbuffers.Builder, coord flatbuffers.UOffsetT) {
	builder.PrependStructSlot(0, flatbuffers.UOffsetT(coord), 0)
}
func ChunkDataAddRuns(builder *flatbuffers.Builder, runs flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(1, flatbuffers.UOffsetT(runs), 0)
}
func ChunkDataStartRunsVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(2, numElems, 2)
}
func ChunkDataEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
