// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package Net

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

// / Chunk columns this character has now explored. Server -> client.
// /
// / **Additive, and that is the whole of the protocol.** A column named once is explored
// / for the rest of that character's life; there is no message that takes one back, and
// / a client that receives the same column twice has learned nothing new rather than
// / something contradictory. So the client's ledger is a union of every `MapExplored` it
// / has ever received, and no revision number is needed to order two of them.
// /
// / Sent in pages after the welcome — a character with a long history has more columns
// / than one frame should carry — and then again as columns are newly revealed. The page
// / size is the server's to choose under the bound below; a client must never assume that
// / one message is the whole ledger, and must never treat the last page as an end marker,
// / because there is no such thing: the stream simply continues for as long as the
// / character keeps walking.
// /
// / A `MapExplored` is what tells a client which tiles are worth re-requesting. A tile it
// / already holds was drawn against the mask of the moment it arrived, and a column that
// / has since been revealed changes that tile's picture without any `MapTile` being sent.
// /
// / Invariants a decoder must enforce:
// /   - `columns` is present and non-empty. An empty page states nothing, and reading one
// /     as "the ledger is empty" would erase a client's map
// /   - at most 4096 entries. The bound is on the wire so the receiver sizes its work from
// /     the contract rather than from a number the peer chose
type MapExplored struct {
	_tab flatbuffers.Table
}

func GetRootAsMapExplored(buf []byte, offset flatbuffers.UOffsetT) *MapExplored {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &MapExplored{}
	x.Init(buf, n+offset)
	return x
}

func FinishMapExploredBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.Finish(offset)
}

func GetSizePrefixedRootAsMapExplored(buf []byte, offset flatbuffers.UOffsetT) *MapExplored {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &MapExplored{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func FinishSizePrefixedMapExploredBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.FinishSizePrefixed(offset)
}

func (rcv *MapExplored) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *MapExplored) Table() flatbuffers.Table {
	return rcv._tab
}

func (rcv *MapExplored) Columns(obj *MapColumn, j int) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		x := rcv._tab.Vector(o)
		x += flatbuffers.UOffsetT(j) * 8
		obj.Init(rcv._tab.Bytes, x)
		return true
	}
	return false
}

func (rcv *MapExplored) ColumnsLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func MapExploredStart(builder *flatbuffers.Builder) {
	builder.StartObject(1)
}
func MapExploredAddColumns(builder *flatbuffers.Builder, columns flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(0, flatbuffers.UOffsetT(columns), 0)
}
func MapExploredStartColumnsVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(8, numElems, 4)
}
func MapExploredEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
