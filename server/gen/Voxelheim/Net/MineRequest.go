// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package Net

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

// / Starts, continues or cancels mining one voxel. Client -> server, and intent
// / only: the server owns hardness, elapsed progress, completion and the item
// / that breaking eventually yields.
// /
// / There is no acknowledgement or rejection payload. A request the server
// / refuses is silence, and an accepted request becomes visible only through the
// / authoritative `MineProgress`, inventory and world state that follow.
// /
// / Decoder invariants the server enforces on this untrusted input:
// /   - `pos` absent decodes as null / `None`: a protocol error, never the origin
// /   - `slot` must be < `ServerWelcome.inventory_slots`
// /   - `client_tick` is ordering and staleness only, never a trusted clock
type MineRequest struct {
	_tab flatbuffers.Table
}

func GetRootAsMineRequest(buf []byte, offset flatbuffers.UOffsetT) *MineRequest {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &MineRequest{}
	x.Init(buf, n+offset)
	return x
}

func FinishMineRequestBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.Finish(offset)
}

func GetSizePrefixedRootAsMineRequest(buf []byte, offset flatbuffers.UOffsetT) *MineRequest {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &MineRequest{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func FinishSizePrefixedMineRequestBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.FinishSizePrefixed(offset)
}

func (rcv *MineRequest) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *MineRequest) Table() flatbuffers.Table {
	return rcv._tab
}

// / The voxel whose mining control changed, in world coordinates.
func (rcv *MineRequest) Pos(obj *BlockCoord) *BlockCoord {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		x := o + rcv._tab.Pos
		if obj == nil {
			obj = new(BlockCoord)
		}
		obj.Init(rcv._tab.Bytes, x)
		return obj
	}
	return nil
}

// / The voxel whose mining control changed, in world coordinates.
// / True while mining is active; false cancels mining at `pos`.
func (rcv *MineRequest) Active() bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		return rcv._tab.GetBool(o + rcv._tab.Pos)
	}
	return false
}

// / True while mining is active; false cancels mining at `pos`.
func (rcv *MineRequest) MutateActive(n bool) bool {
	return rcv._tab.MutateBoolSlot(6, n)
}

// / The client's own tick counter, for ordering and staleness only.
func (rcv *MineRequest) ClientTick() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

// / The client's own tick counter, for ordering and staleness only.
func (rcv *MineRequest) MutateClientTick(n uint32) bool {
	return rcv._tab.MutateUint32Slot(8, n)
}

// / The authoritative inventory slot the player is mining with.
// /
// / **Which slot, never what is in it.** The server reads its own inventory to find
// / the item and its own table to find what that item is worth against this block; a
// / client that named a tool would be naming its own mining speed.
// /
// / Appended in V8, and this was the one action that named no slot — an attack, a
// / placement and a repair all already do. Absent decodes as `0`, which is a real
// / hotbar slot rather than a sentinel, and that is deliberate: a client too old to
// / send one mines with slot zero rather than being refused, and mining with the wrong
// / implement is exactly bare hands. There is nothing a stale client can gain here.
func (rcv *MineRequest) Slot() byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(10))
	if o != 0 {
		return rcv._tab.GetByte(o + rcv._tab.Pos)
	}
	return 0
}

// / The authoritative inventory slot the player is mining with.
// /
// / **Which slot, never what is in it.** The server reads its own inventory to find
// / the item and its own table to find what that item is worth against this block; a
// / client that named a tool would be naming its own mining speed.
// /
// / Appended in V8, and this was the one action that named no slot — an attack, a
// / placement and a repair all already do. Absent decodes as `0`, which is a real
// / hotbar slot rather than a sentinel, and that is deliberate: a client too old to
// / send one mines with slot zero rather than being refused, and mining with the wrong
// / implement is exactly bare hands. There is nothing a stale client can gain here.
func (rcv *MineRequest) MutateSlot(n byte) bool {
	return rcv._tab.MutateByteSlot(10, n)
}

func MineRequestStart(builder *flatbuffers.Builder) {
	builder.StartObject(4)
}
func MineRequestAddPos(builder *flatbuffers.Builder, pos flatbuffers.UOffsetT) {
	builder.PrependStructSlot(0, flatbuffers.UOffsetT(pos), 0)
}
func MineRequestAddActive(builder *flatbuffers.Builder, active bool) {
	builder.PrependBoolSlot(1, active, false)
}
func MineRequestAddClientTick(builder *flatbuffers.Builder, clientTick uint32) {
	builder.PrependUint32Slot(2, clientTick, 0)
}
func MineRequestAddSlot(builder *flatbuffers.Builder, slot byte) {
	builder.PrependByteSlot(3, slot, 0)
}
func MineRequestEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
