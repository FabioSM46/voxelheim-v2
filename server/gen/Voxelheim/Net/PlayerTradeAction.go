// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package Net

import "strconv"

// / Which operation one `PlayerTradeRequest` asks the authoritative server to perform.
// /
// / There is deliberately no invite, accept or decline. `Open` is the only way a trade
// / starts, and the addressed partner learns of it from the first `PlayerTradeState`, in
// / the same way a stall opens on its first `VendorState`. The confirmation prompt belongs
// / to the player who pressed the key, not to the player they addressed.
// /
// / Every action also reads `client_tick` for authoritative ordering and staleness:
// /   - `Open` reads `target_entity_id`
// /   - `SetItem` reads `trade_slot`, `pack_slot`, and `revision`
// /   - `ClearItem` reads `trade_slot` and `revision`
// /   - `SetSilver` reads `silver` and `revision`
// /   - `Confirm` reads `revision`
// /   - `Cancel` reads no action-specific field
type PlayerTradeAction byte

const (
	PlayerTradeActionUnknown   PlayerTradeAction = 0
	PlayerTradeActionOpen      PlayerTradeAction = 1
	PlayerTradeActionSetItem   PlayerTradeAction = 2
	PlayerTradeActionClearItem PlayerTradeAction = 3
	PlayerTradeActionSetSilver PlayerTradeAction = 4
	PlayerTradeActionConfirm   PlayerTradeAction = 5
	PlayerTradeActionCancel    PlayerTradeAction = 6
)

var EnumNamesPlayerTradeAction = map[PlayerTradeAction]string{
	PlayerTradeActionUnknown:   "Unknown",
	PlayerTradeActionOpen:      "Open",
	PlayerTradeActionSetItem:   "SetItem",
	PlayerTradeActionClearItem: "ClearItem",
	PlayerTradeActionSetSilver: "SetSilver",
	PlayerTradeActionConfirm:   "Confirm",
	PlayerTradeActionCancel:    "Cancel",
}

var EnumValuesPlayerTradeAction = map[string]PlayerTradeAction{
	"Unknown":   PlayerTradeActionUnknown,
	"Open":      PlayerTradeActionOpen,
	"SetItem":   PlayerTradeActionSetItem,
	"ClearItem": PlayerTradeActionClearItem,
	"SetSilver": PlayerTradeActionSetSilver,
	"Confirm":   PlayerTradeActionConfirm,
	"Cancel":    PlayerTradeActionCancel,
}

func (v PlayerTradeAction) String() string {
	if s, ok := EnumNamesPlayerTradeAction[v]; ok {
		return s
	}
	return "PlayerTradeAction(" + strconv.FormatInt(int64(v), 10) + ")"
}
