// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package Net

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

// / The recipient's own health and life state, as the server holds them.
// /
// / Per-recipient, and carried inside `EntitySnapshot` so that health arrives on the
// / same superseding stream as every other authoritative fact. A dropped snapshot is
// / harmless: the next one carries the complete answer, so there is nothing to merge
// / and nothing to replay. There is no client -> server counterpart, for the same
// / reason there is no client-sent position: what a player's health *is* is a gameplay
// / outcome, and outcomes belong to the server.
// /
// / Invariants a decoder must enforce, because a receiver divides by one of these and
// / branches on another:
// /   - `max_health` is non-zero — a health bar drawn as `health / max_health` divides
// /     by it, and an honestly buggy server reaches that division as easily as a
// /     hostile one
// /   - `health` never exceeds `max_health`
// /   - `life_state` is a known non-zero member; `Unknown` is the absent-field case
// /   - an `Alive` player has positive health
// /   - only a `Dead` player may carry a non-zero `respawn_ticks`
// /   - `life_state` agrees with `EntitySnapshot.dead_players`: the recipient's own entity
// /     id is in that vector exactly when this says `Dead` — see that field, where the
// /     reason one fact is stated twice is argued
type PlayerVitals struct {
	_tab flatbuffers.Table
}

func GetRootAsPlayerVitals(buf []byte, offset flatbuffers.UOffsetT) *PlayerVitals {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &PlayerVitals{}
	x.Init(buf, n+offset)
	return x
}

func FinishPlayerVitalsBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.Finish(offset)
}

func GetSizePrefixedRootAsPlayerVitals(buf []byte, offset flatbuffers.UOffsetT) *PlayerVitals {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &PlayerVitals{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func FinishSizePrefixedPlayerVitalsBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.FinishSizePrefixed(offset)
}

func (rcv *PlayerVitals) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *PlayerVitals) Table() flatbuffers.Table {
	return rcv._tab
}

// / Current health, in the same units as `max_health`. Zero is legal and means the
// / player is dead — the server's transition to `Dead` is what makes it so, not the
// / number.
func (rcv *PlayerVitals) Health() uint16 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		return rcv._tab.GetUint16(o + rcv._tab.Pos)
	}
	return 0
}

// / Current health, in the same units as `max_health`. Zero is legal and means the
// / player is dead — the server's transition to `Dead` is what makes it so, not the
// / number.
func (rcv *PlayerVitals) MutateHealth(n uint16) bool {
	return rcv._tab.MutateUint16Slot(4, n)
}

// / Maximum health. Non-zero, always: it is the denominator of every health display.
func (rcv *PlayerVitals) MaxHealth() uint16 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		return rcv._tab.GetUint16(o + rcv._tab.Pos)
	}
	return 0
}

// / Maximum health. Non-zero, always: it is the denominator of every health display.
func (rcv *PlayerVitals) MutateMaxHealth(n uint16) bool {
	return rcv._tab.MutateUint16Slot(6, n)
}

func (rcv *PlayerVitals) LifeState() LifeState {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		return LifeState(rcv._tab.GetByte(o + rcv._tab.Pos))
	}
	return 0
}

func (rcv *PlayerVitals) MutateLifeState(n LifeState) bool {
	return rcv._tab.MutateByteSlot(8, byte(n))
}

// / Server ticks remaining before the server respawns this player, counted at
// / `ServerWelcome.tick_rate`. Zero for a living player. **A count, never a
// / deadline**: the client converts it for display and holds the value unchanged
// / while snapshots are absent, exactly as it holds an entity's last position rather
// / than extrapolating one.
func (rcv *PlayerVitals) RespawnTicks() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(10))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

// / Server ticks remaining before the server respawns this player, counted at
// / `ServerWelcome.tick_rate`. Zero for a living player. **A count, never a
// / deadline**: the client converts it for display and holds the value unchanged
// / while snapshots are absent, exactly as it holds an entity's last position rather
// / than extrapolating one.
func (rcv *PlayerVitals) MutateRespawnTicks(n uint32) bool {
	return rcv._tab.MutateUint32Slot(10, n)
}

// / Whether the server is currently refusing damage to this player. The server owns
// / the timer; this is the answer, not a duration to run down locally.
func (rcv *PlayerVitals) Invulnerable() bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(12))
	if o != 0 {
		return rcv._tab.GetBool(o + rcv._tab.Pos)
	}
	return false
}

// / Whether the server is currently refusing damage to this player. The server owns
// / the timer; this is the answer, not a duration to run down locally.
func (rcv *PlayerVitals) MutateInvulnerable(n bool) bool {
	return rcv._tab.MutateBoolSlot(12, n)
}

// / Current hunger reserve, in the same units as `max_hunger`. Zero is legal: it
// / prevents health regeneration but does not itself damage the player.
func (rcv *PlayerVitals) Hunger() uint16 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(14))
	if o != 0 {
		return rcv._tab.GetUint16(o + rcv._tab.Pos)
	}
	return 0
}

// / Current hunger reserve, in the same units as `max_hunger`. Zero is legal: it
// / prevents health regeneration but does not itself damage the player.
func (rcv *PlayerVitals) MutateHunger(n uint16) bool {
	return rcv._tab.MutateUint16Slot(14, n)
}

// / Maximum hunger. Non-zero, always: it is the denominator of every hunger display.
func (rcv *PlayerVitals) MaxHunger() uint16 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(16))
	if o != 0 {
		return rcv._tab.GetUint16(o + rcv._tab.Pos)
	}
	return 0
}

// / Maximum hunger. Non-zero, always: it is the denominator of every hunger display.
func (rcv *PlayerVitals) MutateMaxHunger(n uint16) bool {
	return rcv._tab.MutateUint16Slot(16, n)
}

// / The recipient's current level. Never zero: the first level is level 1.
func (rcv *PlayerVitals) Level() uint16 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(18))
	if o != 0 {
		return rcv._tab.GetUint16(o + rcv._tab.Pos)
	}
	return 0
}

// / The recipient's current level. Never zero: the first level is level 1.
func (rcv *PlayerVitals) MutateLevel(n uint16) bool {
	return rcv._tab.MutateUint16Slot(18, n)
}

// / Experience earned into the current level. Never exceeds `experience_to_next`.
// / At the level cap the two values are equal.
func (rcv *PlayerVitals) Experience() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(20))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

// / Experience earned into the current level. Never exceeds `experience_to_next`.
// / At the level cap the two values are equal.
func (rcv *PlayerVitals) MutateExperience(n uint32) bool {
	return rcv._tab.MutateUint32Slot(20, n)
}

// / Experience required to complete the current level. Never zero: it is the
// / denominator of every experience display.
func (rcv *PlayerVitals) ExperienceToNext() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(22))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

// / Experience required to complete the current level. Never zero: it is the
// / denominator of every experience display.
func (rcv *PlayerVitals) MutateExperienceToNext(n uint32) bool {
	return rcv._tab.MutateUint32Slot(22, n)
}

func PlayerVitalsStart(builder *flatbuffers.Builder) {
	builder.StartObject(10)
}
func PlayerVitalsAddHealth(builder *flatbuffers.Builder, health uint16) {
	builder.PrependUint16Slot(0, health, 0)
}
func PlayerVitalsAddMaxHealth(builder *flatbuffers.Builder, maxHealth uint16) {
	builder.PrependUint16Slot(1, maxHealth, 0)
}
func PlayerVitalsAddLifeState(builder *flatbuffers.Builder, lifeState LifeState) {
	builder.PrependByteSlot(2, byte(lifeState), 0)
}
func PlayerVitalsAddRespawnTicks(builder *flatbuffers.Builder, respawnTicks uint32) {
	builder.PrependUint32Slot(3, respawnTicks, 0)
}
func PlayerVitalsAddInvulnerable(builder *flatbuffers.Builder, invulnerable bool) {
	builder.PrependBoolSlot(4, invulnerable, false)
}
func PlayerVitalsAddHunger(builder *flatbuffers.Builder, hunger uint16) {
	builder.PrependUint16Slot(5, hunger, 0)
}
func PlayerVitalsAddMaxHunger(builder *flatbuffers.Builder, maxHunger uint16) {
	builder.PrependUint16Slot(6, maxHunger, 0)
}
func PlayerVitalsAddLevel(builder *flatbuffers.Builder, level uint16) {
	builder.PrependUint16Slot(7, level, 0)
}
func PlayerVitalsAddExperience(builder *flatbuffers.Builder, experience uint32) {
	builder.PrependUint32Slot(8, experience, 0)
}
func PlayerVitalsAddExperienceToNext(builder *flatbuffers.Builder, experienceToNext uint32) {
	builder.PrependUint32Slot(9, experienceToNext, 0)
}
func PlayerVitalsEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
