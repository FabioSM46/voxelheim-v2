// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package Net

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

// / One chunk column under a ward, in a `WardsNearby`.
// /
// / A struct: fixed, small, and there are up to `MaxWardedColumns` of them in one message,
// / so a table apiece would cost an offset each for two ints and two bytes. It is settled
// / for the same reason `ChunkCoord` is — a column address plus who claims it is the whole
// / of what a ward is on the wire, and the runestone that *causes* it is already a
// / `StructureState` in the snapshot.
// /
// / Only the horizontal axes, deliberately: a ward claims a column of the world from
// / bedrock to sky, not a cube of it, so there is no `cy` to send and none to ignore.
type WardedColumn struct {
	_tab flatbuffers.Struct
}

func (rcv *WardedColumn) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *WardedColumn) Table() flatbuffers.Table {
	return rcv._tab.Table
}

func (rcv *WardedColumn) Cx() int32 {
	return rcv._tab.GetInt32(rcv._tab.Pos + flatbuffers.UOffsetT(0))
}
func (rcv *WardedColumn) MutateCx(n int32) bool {
	return rcv._tab.MutateInt32(rcv._tab.Pos+flatbuffers.UOffsetT(0), n)
}

func (rcv *WardedColumn) Cz() int32 {
	return rcv._tab.GetInt32(rcv._tab.Pos + flatbuffers.UOffsetT(4))
}
func (rcv *WardedColumn) MutateCz(n int32) bool {
	return rcv._tab.MutateInt32(rcv._tab.Pos+flatbuffers.UOffsetT(4), n)
}

func (rcv *WardedColumn) Kind() WardKind {
	return WardKind(rcv._tab.GetByte(rcv._tab.Pos + flatbuffers.UOffsetT(8)))
}
func (rcv *WardedColumn) MutateKind(n WardKind) bool {
	return rcv._tab.MutateByte(rcv._tab.Pos+flatbuffers.UOffsetT(8), byte(n))
}

// / True when the recipient is the one whose ward this is. Presentation only — the
// / server refuses the edit either way, and a client that let a `true` here authorise
// / anything would be deciding a gameplay outcome.
func (rcv *WardedColumn) Mine() bool {
	return rcv._tab.GetBool(rcv._tab.Pos + flatbuffers.UOffsetT(9))
}

// / True when the recipient is the one whose ward this is. Presentation only — the
// / server refuses the edit either way, and a client that let a `true` here authorise
// / anything would be deciding a gameplay outcome.
func (rcv *WardedColumn) MutateMine(n bool) bool {
	return rcv._tab.MutateBool(rcv._tab.Pos+flatbuffers.UOffsetT(9), n)
}

func CreateWardedColumn(builder *flatbuffers.Builder, cx int32, cz int32, kind WardKind, mine bool) flatbuffers.UOffsetT {
	builder.Prep(4, 12)
	builder.Pad(2)
	builder.PrependBool(mine)
	builder.PrependByte(byte(kind))
	builder.PrependInt32(cz)
	builder.PrependInt32(cx)
	return builder.Offset()
}
