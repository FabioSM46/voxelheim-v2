// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package Net

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

// / What a character looks like: four worn colours, a hair model, and its colour.
// /
// / **Authoritative, and the client never invents a default.** Every value here was
// / chosen by the player and stored by the server; a client that substituted its own
// / colour for a missing one would draw somebody the server never described. An
// / absent `Appearance`, an absent colour and an `Unknown` hair model are therefore
// / all protocol errors at the decode boundary, not opportunities to guess. The one
// / documented placeholder in this contract is a different case entirely — an entity
// / whose `PlayerAppearance` has not arrived *yet*; see `player.fbs`.
// /
// / **A table rather than a struct**, deliberately, and against the rule of thumb
// / that fixed-size shapes are inlined. A struct can never gain, lose or reorder a
// / field, and this shape is the least settled in the contract: beards, faces, body
// / types and worn equipment are all things a later issue adds. It is also not hot —
// / see `PlayerAppearance` in `player.fbs`, which is why appearance is a message of
// / its own rather than a field of `EntityState`.
// /
// / **Colour encoding — stated once, here, and authoritative for every colour on
// / this wire.** Each colour is one `uint` holding `0x00RRGGBB`: eight bits per
// / channel, sRGB, non-linear, with the most significant eight bits **reserved and
// / required to be zero**. There is no alpha — a character is opaque — and there is
// / no second encoding anywhere in this contract, so neither side needs a conversion
// / rule to agree on. A client whose renderer wants linear values converts on the way
// / in; that is a rendering detail and not a property of the wire.
// /
// / Decoder invariants, in both directions:
// /   - every colour's top eight bits are zero. Refused rather than masked: a set
// /     high byte means the peer is encoding something this build does not know
// /     about, and masking it would show a colour nobody chose while hiding the
// /     disagreement — the reasoning `ServerWelcome`'s world clock records, where a
// /     repair a decoder invents is a different answer from the server's
// /   - `hair_model` is a known member and never `Unknown`
// /   - the table itself is present wherever it is referenced
type Appearance struct {
	_tab flatbuffers.Table
}

func GetRootAsAppearance(buf []byte, offset flatbuffers.UOffsetT) *Appearance {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &Appearance{}
	x.Init(buf, n+offset)
	return x
}

func FinishAppearanceBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.Finish(offset)
}

func GetSizePrefixedRootAsAppearance(buf []byte, offset flatbuffers.UOffsetT) *Appearance {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &Appearance{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func FinishSizePrefixedAppearanceBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.FinishSizePrefixed(offset)
}

func (rcv *Appearance) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *Appearance) Table() flatbuffers.Table {
	return rcv._tab
}

// / Skin.
func (rcv *Appearance) SkinColor() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

// / Skin.
func (rcv *Appearance) MutateSkinColor(n uint32) bool {
	return rcv._tab.MutateUint32Slot(4, n)
}

// / The shirt, tunic or coat covering the torso.
func (rcv *Appearance) ShirtColor() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

// / The shirt, tunic or coat covering the torso.
func (rcv *Appearance) MutateShirtColor(n uint32) bool {
	return rcv._tab.MutateUint32Slot(6, n)
}

// / The trousers, breeches or leggings covering the legs.
func (rcv *Appearance) TrousersColor() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

// / The trousers, breeches or leggings covering the legs.
func (rcv *Appearance) MutateTrousersColor(n uint32) bool {
	return rcv._tab.MutateUint32Slot(8, n)
}

// / Footwear.
func (rcv *Appearance) ShoesColor() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(10))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

// / Footwear.
func (rcv *Appearance) MutateShoesColor(n uint32) bool {
	return rcv._tab.MutateUint32Slot(10, n)
}

// / Which hair model the character wears. Never `Unknown` in a value the server
// / emits or accepts.
func (rcv *Appearance) HairModel() HairModel {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(12))
	if o != 0 {
		return HairModel(rcv._tab.GetByte(o + rcv._tab.Pos))
	}
	return 0
}

// / Which hair model the character wears. Never `Unknown` in a value the server
// / emits or accepts.
func (rcv *Appearance) MutateHairModel(n HairModel) bool {
	return rcv._tab.MutateByteSlot(12, byte(n))
}

// / The hair's colour, in the same encoding as the four above. Read whatever the
// / model is: a shaved head still has stubble, and the client decides how much of
// / the colour a given model shows.
func (rcv *Appearance) HairColor() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(14))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

// / The hair's colour, in the same encoding as the four above. Read whatever the
// / model is: a shaved head still has stubble, and the client decides how much of
// / the colour a given model shows.
func (rcv *Appearance) MutateHairColor(n uint32) bool {
	return rcv._tab.MutateUint32Slot(14, n)
}

func AppearanceStart(builder *flatbuffers.Builder) {
	builder.StartObject(6)
}
func AppearanceAddSkinColor(builder *flatbuffers.Builder, skinColor uint32) {
	builder.PrependUint32Slot(0, skinColor, 0)
}
func AppearanceAddShirtColor(builder *flatbuffers.Builder, shirtColor uint32) {
	builder.PrependUint32Slot(1, shirtColor, 0)
}
func AppearanceAddTrousersColor(builder *flatbuffers.Builder, trousersColor uint32) {
	builder.PrependUint32Slot(2, trousersColor, 0)
}
func AppearanceAddShoesColor(builder *flatbuffers.Builder, shoesColor uint32) {
	builder.PrependUint32Slot(3, shoesColor, 0)
}
func AppearanceAddHairModel(builder *flatbuffers.Builder, hairModel HairModel) {
	builder.PrependByteSlot(4, byte(hairModel), 0)
}
func AppearanceAddHairColor(builder *flatbuffers.Builder, hairColor uint32) {
	builder.PrependUint32Slot(5, hairColor, 0)
}
func AppearanceEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
