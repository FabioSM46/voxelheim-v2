// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package Net

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

// / One Opus frame the authoritative server decided this recipient may hear.
// / Server -> client.
// /
// / Receiving one *is* the audibility decision. A client never asks whether it should be
// / hearing this speaker and never filters the frame by its own idea of distance or
// / party: the server sent it, so it is audible, and a client that decided otherwise
// / would be second-guessing the only authority there is.
// /
// / `sequence` is the speaker's counter, copied through unchanged, so a recipient can
// / order frames and hear a gap as a gap. It is per speaker: two speakers' sequences say
// / nothing about each other.
// /
// / Decoder invariants:
// /   - `speaker_entity_id` is non-zero and names the speaker's live entity, the same id
// /     an `EntitySnapshot` addresses them by
// /   - `opus` is present, non-empty and at most `VoiceBound.MaxOpusBytes` — the same
// /     ceiling `VoiceFrame` states, because this frame is the one that got past it. A
// /     frame with no audio in it is one the server should not have relayed, and one over
// /     the ceiling is one it should not have accepted
// /   - `opus` is copied verbatim and decoded by the client's Opus decoder, which is the
// /     only thing in this system that reads it
// /
// / The same rule as its counterpart: **these bytes are never logged, never persisted
// / and never quoted in a diagnostic**, on either side of the wire.
type VoiceHeard struct {
	_tab flatbuffers.Table
}

func GetRootAsVoiceHeard(buf []byte, offset flatbuffers.UOffsetT) *VoiceHeard {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &VoiceHeard{}
	x.Init(buf, n+offset)
	return x
}

func FinishVoiceHeardBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.Finish(offset)
}

func GetSizePrefixedRootAsVoiceHeard(buf []byte, offset flatbuffers.UOffsetT) *VoiceHeard {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &VoiceHeard{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func FinishSizePrefixedVoiceHeardBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.FinishSizePrefixed(offset)
}

func (rcv *VoiceHeard) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *VoiceHeard) Table() flatbuffers.Table {
	return rcv._tab
}

func (rcv *VoiceHeard) SpeakerEntityId() uint64 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		return rcv._tab.GetUint64(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *VoiceHeard) MutateSpeakerEntityId(n uint64) bool {
	return rcv._tab.MutateUint64Slot(4, n)
}

func (rcv *VoiceHeard) Sequence() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *VoiceHeard) MutateSequence(n uint32) bool {
	return rcv._tab.MutateUint32Slot(6, n)
}

func (rcv *VoiceHeard) Opus(j int) byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return rcv._tab.GetByte(a + flatbuffers.UOffsetT(j*1))
	}
	return 0
}

func (rcv *VoiceHeard) OpusLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func (rcv *VoiceHeard) OpusBytes() []byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		return rcv._tab.ByteVector(o + rcv._tab.Pos)
	}
	return nil
}

func (rcv *VoiceHeard) MutateOpus(j int, n byte) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		a := rcv._tab.Vector(o)
		return rcv._tab.MutateByte(a+flatbuffers.UOffsetT(j*1), n)
	}
	return false
}

func VoiceHeardStart(builder *flatbuffers.Builder) {
	builder.StartObject(3)
}
func VoiceHeardAddSpeakerEntityId(builder *flatbuffers.Builder, speakerEntityId uint64) {
	builder.PrependUint64Slot(0, speakerEntityId, 0)
}
func VoiceHeardAddSequence(builder *flatbuffers.Builder, sequence uint32) {
	builder.PrependUint32Slot(1, sequence, 0)
}
func VoiceHeardAddOpus(builder *flatbuffers.Builder, opus flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(2, flatbuffers.UOffsetT(opus), 0)
}
func VoiceHeardStartOpusVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(1, numElems, 1)
}
func VoiceHeardEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
