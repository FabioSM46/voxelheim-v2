// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package Net

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

// / One bounded region an announced move endangers, in world space. Server -> client.
// /
// / **It is the region, not the damage.** Whether a player standing in it is hit is the
// / server's answer and arrives as `BlowLanded`; nothing here lets a client decide it, and
// / a client that took a health point away because a body overlapped this volume would be
// / deciding a gameplay outcome. The volume exists so the danger can be *shown* — which is
// / the readability the whole encounter rests on — and so a receiver can tell the region
// / announced in `Telegraph` from the one that is live in `Release`.
// /
// / Every region is bounded, and the bound is checked rather than assumed: an unbounded
// / or non-finite volume reaches a renderer as a shape covering the world, and a receiver
// / that clamped it would be inventing a danger boundary the server did not state.
// /
// / Decoder invariants:
// /   - `shape` is a known non-zero member
// /   - `origin` is present and every component is finite and inside the +/-2^24 block
// /     domain
// /   - `direction` is present and every component is finite. `Cone` and `Line` require a
// /     non-zero direction; `Disc` and `Ring` ignore it, and a server writes zero there
// /   - `radius` is finite, strictly positive, and at most 128 blocks. No move in this
// /     encounter reaches further, and an arena is smaller than that
// /   - `height` is finite, strictly positive, and at most 128 blocks
// /   - `inner_radius` is finite and in `0 .. radius`; `Ring` requires it strictly less
// /     than `radius`, and the other three shapes ignore it
// /   - `half_angle` is finite and in `0 .. pi` radians; `Cone` requires it strictly
// /     positive, and the other three shapes ignore it. The bound is pi *as a float32
// /     holds it* — the nearest float32 to pi sits fractionally above the real value, so
// /     a receiver comparing in double precision would refuse the widest cone a sender
// /     can encode
// /   - `half_width` is finite and in `0 .. radius`; `Line` requires it strictly
// /     positive, and the other three shapes ignore it
type HazardVolume struct {
	_tab flatbuffers.Table
}

func GetRootAsHazardVolume(buf []byte, offset flatbuffers.UOffsetT) *HazardVolume {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &HazardVolume{}
	x.Init(buf, n+offset)
	return x
}

func FinishHazardVolumeBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.Finish(offset)
}

func GetSizePrefixedRootAsHazardVolume(buf []byte, offset flatbuffers.UOffsetT) *HazardVolume {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &HazardVolume{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func FinishSizePrefixedHazardVolumeBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.FinishSizePrefixed(offset)
}

func (rcv *HazardVolume) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *HazardVolume) Table() flatbuffers.Table {
	return rcv._tab
}

func (rcv *HazardVolume) Shape() HazardShape {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		return HazardShape(rcv._tab.GetByte(o + rcv._tab.Pos))
	}
	return 0
}

func (rcv *HazardVolume) MutateShape(n HazardShape) bool {
	return rcv._tab.MutateByteSlot(4, byte(n))
}

// / The region's anchor in world space. For `Cone` and `Line` it is the apex or the
// / start of the strip; for `Disc` and `Ring` it is the centre.
func (rcv *HazardVolume) Origin(obj *Vec3) *Vec3 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		x := o + rcv._tab.Pos
		if obj == nil {
			obj = new(Vec3)
		}
		obj.Init(rcv._tab.Bytes, x)
		return obj
	}
	return nil
}

// / The region's anchor in world space. For `Cone` and `Line` it is the apex or the
// / start of the strip; for `Disc` and `Ring` it is the centre.
// / Where the region points, as a unit vector the server has already normalised. It is
// / **fixed by the server**, and for an aimed move it is fixed once — see
// / `EncounterMove.aim`. A receiver never re-aims it at a player.
func (rcv *HazardVolume) Direction(obj *Vec3) *Vec3 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		x := o + rcv._tab.Pos
		if obj == nil {
			obj = new(Vec3)
		}
		obj.Init(rcv._tab.Bytes, x)
		return obj
	}
	return nil
}

// / Where the region points, as a unit vector the server has already normalised. It is
// / **fixed by the server**, and for an aimed move it is fixed once — see
// / `EncounterMove.aim`. A receiver never re-aims it at a player.
// / Reach, in blocks: the cone's length, the strip's length, or the outer radius of a
// / disc or ring.
func (rcv *HazardVolume) Radius() float32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(10))
	if o != 0 {
		return rcv._tab.GetFloat32(o + rcv._tab.Pos)
	}
	return 0.0
}

// / Reach, in blocks: the cone's length, the strip's length, or the outer radius of a
// / disc or ring.
func (rcv *HazardVolume) MutateRadius(n float32) bool {
	return rcv._tab.MutateFloat32Slot(10, n)
}

// / Vertical extent, in blocks, centred on `origin`. A ground wave is short and a
// / leap's landing is not; a receiver that assumed a floor plane would draw a danger
// / the wrong height.
func (rcv *HazardVolume) Height() float32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(12))
	if o != 0 {
		return rcv._tab.GetFloat32(o + rcv._tab.Pos)
	}
	return 0.0
}

// / Vertical extent, in blocks, centred on `origin`. A ground wave is short and a
// / leap's landing is not; a receiver that assumed a floor plane would draw a danger
// / the wrong height.
func (rcv *HazardVolume) MutateHeight(n float32) bool {
	return rcv._tab.MutateFloat32Slot(12, n)
}

// / Inner radius, in blocks. `Ring` only.
func (rcv *HazardVolume) InnerRadius() float32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(14))
	if o != 0 {
		return rcv._tab.GetFloat32(o + rcv._tab.Pos)
	}
	return 0.0
}

// / Inner radius, in blocks. `Ring` only.
func (rcv *HazardVolume) MutateInnerRadius(n float32) bool {
	return rcv._tab.MutateFloat32Slot(14, n)
}

// / Half the cone's opening angle, in radians. `Cone` only.
func (rcv *HazardVolume) HalfAngle() float32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(16))
	if o != 0 {
		return rcv._tab.GetFloat32(o + rcv._tab.Pos)
	}
	return 0.0
}

// / Half the cone's opening angle, in radians. `Cone` only.
func (rcv *HazardVolume) MutateHalfAngle(n float32) bool {
	return rcv._tab.MutateFloat32Slot(16, n)
}

// / Half the strip's width, in blocks. `Line` only.
func (rcv *HazardVolume) HalfWidth() float32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(18))
	if o != 0 {
		return rcv._tab.GetFloat32(o + rcv._tab.Pos)
	}
	return 0.0
}

// / Half the strip's width, in blocks. `Line` only.
func (rcv *HazardVolume) MutateHalfWidth(n float32) bool {
	return rcv._tab.MutateFloat32Slot(18, n)
}

func HazardVolumeStart(builder *flatbuffers.Builder) {
	builder.StartObject(8)
}
func HazardVolumeAddShape(builder *flatbuffers.Builder, shape HazardShape) {
	builder.PrependByteSlot(0, byte(shape), 0)
}
func HazardVolumeAddOrigin(builder *flatbuffers.Builder, origin flatbuffers.UOffsetT) {
	builder.PrependStructSlot(1, flatbuffers.UOffsetT(origin), 0)
}
func HazardVolumeAddDirection(builder *flatbuffers.Builder, direction flatbuffers.UOffsetT) {
	builder.PrependStructSlot(2, flatbuffers.UOffsetT(direction), 0)
}
func HazardVolumeAddRadius(builder *flatbuffers.Builder, radius float32) {
	builder.PrependFloat32Slot(3, radius, 0.0)
}
func HazardVolumeAddHeight(builder *flatbuffers.Builder, height float32) {
	builder.PrependFloat32Slot(4, height, 0.0)
}
func HazardVolumeAddInnerRadius(builder *flatbuffers.Builder, innerRadius float32) {
	builder.PrependFloat32Slot(5, innerRadius, 0.0)
}
func HazardVolumeAddHalfAngle(builder *flatbuffers.Builder, halfAngle float32) {
	builder.PrependFloat32Slot(6, halfAngle, 0.0)
}
func HazardVolumeAddHalfWidth(builder *flatbuffers.Builder, halfWidth float32) {
	builder.PrependFloat32Slot(7, halfWidth, 0.0)
}
func HazardVolumeEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
