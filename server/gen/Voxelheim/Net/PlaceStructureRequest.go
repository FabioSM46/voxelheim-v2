// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package Net

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

// / A player trying to plant a structure in the world. Client -> server, and intent only.
// /
// / The world's counterpart to `BlockEditRequest` for things that are entities rather
// / than voxels. Reach, whether the named slot actually holds that structure, whether the
// / footprint the facing implies is clear and supported, and whether the player is
// / already allowed another one are all resolved server-side and none of it is stated
// / here.
// /
// / **It carries no structure id and no kind.** The kind follows from the item in `slot`,
// / which the server reads from its own inventory, and the id is minted by the server
// / when the structure comes into existence. A client that could name either could name
// / one it does not own.
// /
// / There is no acknowledgement and no rejection payload, and no local placement is
// / entitled to appear: the structure exists when a snapshot says it does.
// /
// / Decoder invariants the server enforces on this untrusted input:
// /   - `anchor` is a struct field, so an absent one decodes as null (Go) / `None`
// /     (Rust). Reject that; the origin is a real location, not a default
// /   - `facing` must be a known non-zero member
// /   - `slot` must be < `ServerWelcome.inventory_slots`
// /   - `anchor` is unbounded on the wire and bounded by the server, under the same reach
// /     rule a block edit obeys
// /   - `client_tick` is ordering and staleness only, never a trusted clock
type PlaceStructureRequest struct {
	_tab flatbuffers.Table
}

func GetRootAsPlaceStructureRequest(buf []byte, offset flatbuffers.UOffsetT) *PlaceStructureRequest {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &PlaceStructureRequest{}
	x.Init(buf, n+offset)
	return x
}

func FinishPlaceStructureRequestBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.Finish(offset)
}

func GetSizePrefixedRootAsPlaceStructureRequest(buf []byte, offset flatbuffers.UOffsetT) *PlaceStructureRequest {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &PlaceStructureRequest{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func FinishSizePrefixedPlaceStructureRequestBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.FinishSizePrefixed(offset)
}

func (rcv *PlaceStructureRequest) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *PlaceStructureRequest) Table() flatbuffers.Table {
	return rcv._tab
}

// / The authoritative inventory slot to spend on this placement.
func (rcv *PlaceStructureRequest) Slot() byte {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		return rcv._tab.GetByte(o + rcv._tab.Pos)
	}
	return 0
}

// / The authoritative inventory slot to spend on this placement.
func (rcv *PlaceStructureRequest) MutateSlot(n byte) bool {
	return rcv._tab.MutateByteSlot(4, n)
}

// / The voxel the structure is anchored at, in world coordinates. Which cells the
// / footprint then occupies is the server's arithmetic, exactly as turning a
// / `BlockEditRequest.pos` into a chunk and an index inside it is.
func (rcv *PlaceStructureRequest) Anchor(obj *BlockCoord) *BlockCoord {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		x := o + rcv._tab.Pos
		if obj == nil {
			obj = new(BlockCoord)
		}
		obj.Init(rcv._tab.Bytes, x)
		return obj
	}
	return nil
}

// / The voxel the structure is anchored at, in world coordinates. Which cells the
// / footprint then occupies is the server's arithmetic, exactly as turning a
// / `BlockEditRequest.pos` into a chunk and an index inside it is.
// / Which way the structure faces. The client quantizes its camera yaw to one of the
// / four members; the server validates the result like it validates the anchor.
func (rcv *PlaceStructureRequest) Facing() Facing {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		return Facing(rcv._tab.GetByte(o + rcv._tab.Pos))
	}
	return 0
}

// / Which way the structure faces. The client quantizes its camera yaw to one of the
// / four members; the server validates the result like it validates the anchor.
func (rcv *PlaceStructureRequest) MutateFacing(n Facing) bool {
	return rcv._tab.MutateByteSlot(8, byte(n))
}

// / The client's own tick counter, for ordering and staleness only.
func (rcv *PlaceStructureRequest) ClientTick() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(10))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

// / The client's own tick counter, for ordering and staleness only.
func (rcv *PlaceStructureRequest) MutateClientTick(n uint32) bool {
	return rcv._tab.MutateUint32Slot(10, n)
}

func PlaceStructureRequestStart(builder *flatbuffers.Builder) {
	builder.StartObject(4)
}
func PlaceStructureRequestAddSlot(builder *flatbuffers.Builder, slot byte) {
	builder.PrependByteSlot(0, slot, 0)
}
func PlaceStructureRequestAddAnchor(builder *flatbuffers.Builder, anchor flatbuffers.UOffsetT) {
	builder.PrependStructSlot(1, flatbuffers.UOffsetT(anchor), 0)
}
func PlaceStructureRequestAddFacing(builder *flatbuffers.Builder, facing Facing) {
	builder.PrependByteSlot(2, byte(facing), 0)
}
func PlaceStructureRequestAddClientTick(builder *flatbuffers.Builder, clientTick uint32) {
	builder.PrependUint32Slot(3, clientTick, 0)
}
func PlaceStructureRequestEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
