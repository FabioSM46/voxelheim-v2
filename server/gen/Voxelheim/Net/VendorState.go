// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package Net

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

// / The complete price list one vendor shows one recipient. Server -> client.
// /
// / **Revisioned like `LootState`, and for the same reason**: the client acts against the
// / list it is looking at, names that revision in its `TradeRequest`, and a request written
// / against a list the server has since replaced is refused rather than applied to a
// / different one. A later state for the same vendor supersedes the earlier revision
// / wholesale — this is never a diff.
// /
// / **It is not `LootState`, though, and the two must not be merged.** A corpse holds
// / stacks with identities and durabilities and one direction of travel; a stall holds
// / prices, two directions, and no identity but the item's. Reusing the loot tables here
// / would have meant an `entry_id` nobody mints, a `count` that is always the whole world,
// / and a price smuggled into a durability field.
// /
// / **Both vectors are the player's view of the trade, not the vendor's.** `sells` is what
// / the player may buy and the price they pay; `buys` is what the vendor accepts and the
// / price it pays. An item may appear in both at different prices, and the spread is the
// / server's business.
// /
// / Decoder invariants:
// /   - `entity_id` is the non-zero `entity_id` of a `MobState` whose kind is
// /     `MobKind.Villager`
// /   - `revision` is non-zero
// /   - `sells` and `buys` are both present. Either may be empty: a vendor that only buys
// /     and one that only sells are both ordinary. Both empty is a vendor with nothing to
// /     say and is `VendorClosed` instead
// /   - every entry satisfies `VendorEntry`, and no item id appears twice within one vector
type VendorState struct {
	_tab flatbuffers.Table
}

func GetRootAsVendorState(buf []byte, offset flatbuffers.UOffsetT) *VendorState {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &VendorState{}
	x.Init(buf, n+offset)
	return x
}

func FinishVendorStateBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.Finish(offset)
}

func GetSizePrefixedRootAsVendorState(buf []byte, offset flatbuffers.UOffsetT) *VendorState {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &VendorState{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func FinishSizePrefixedVendorStateBuffer(builder *flatbuffers.Builder, offset flatbuffers.UOffsetT) {
	builder.FinishSizePrefixed(offset)
}

func (rcv *VendorState) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *VendorState) Table() flatbuffers.Table {
	return rcv._tab
}

func (rcv *VendorState) EntityId() uint64 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		return rcv._tab.GetUint64(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *VendorState) MutateEntityId(n uint64) bool {
	return rcv._tab.MutateUint64Slot(4, n)
}

func (rcv *VendorState) Revision() uint32 {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(6))
	if o != 0 {
		return rcv._tab.GetUint32(o + rcv._tab.Pos)
	}
	return 0
}

func (rcv *VendorState) MutateRevision(n uint32) bool {
	return rcv._tab.MutateUint32Slot(6, n)
}

// / What the player may buy, at the price they pay.
func (rcv *VendorState) Sells(obj *VendorEntry, j int) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		x := rcv._tab.Vector(o)
		x += flatbuffers.UOffsetT(j) * 4
		obj.Init(rcv._tab.Bytes, x)
		return true
	}
	return false
}

func (rcv *VendorState) SellsLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(8))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

// / What the player may buy, at the price they pay.
// / What the vendor buys, at the price it pays.
func (rcv *VendorState) Buys(obj *VendorEntry, j int) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(10))
	if o != 0 {
		x := rcv._tab.Vector(o)
		x += flatbuffers.UOffsetT(j) * 4
		obj.Init(rcv._tab.Bytes, x)
		return true
	}
	return false
}

func (rcv *VendorState) BuysLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(10))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

// / What the vendor buys, at the price it pays.
func VendorStateStart(builder *flatbuffers.Builder) {
	builder.StartObject(4)
}
func VendorStateAddEntityId(builder *flatbuffers.Builder, entityId uint64) {
	builder.PrependUint64Slot(0, entityId, 0)
}
func VendorStateAddRevision(builder *flatbuffers.Builder, revision uint32) {
	builder.PrependUint32Slot(1, revision, 0)
}
func VendorStateAddSells(builder *flatbuffers.Builder, sells flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(2, flatbuffers.UOffsetT(sells), 0)
}
func VendorStateStartSellsVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(4, numElems, 2)
}
func VendorStateAddBuys(builder *flatbuffers.Builder, buys flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(3, flatbuffers.UOffsetT(buys), 0)
}
func VendorStateStartBuysVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(4, numElems, 2)
}
func VendorStateEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
