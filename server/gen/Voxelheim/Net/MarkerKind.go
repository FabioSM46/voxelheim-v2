// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package Net

import "strconv"

// / What a mark on the map means. A vocabulary, not a description.
// /
// / The same shape `HairModel` is: the server stores the member and draws nothing, the
// / client maps it to an icon. That is why every one of these is a category and none of
// / them is a colour or a sprite name — a member is something both sides can carry without
// / agreeing on any asset.
// /
// / `Note` is the member with no subject: a mark that is only its text. It is last rather
// / than first because it is the fallback, and a client that offers it as the default would
// / get a map of unlabelled pins.
// /
// / Zero member for the reason every enum in this contract has one, and it fails closed in
// / both directions: a `MarkerPlaceRequest` that omits `kind` is refused rather than
// / guessed at, and a `Marker` that arrives carrying it is a protocol error rather than a
// / pin nobody can read.
type MarkerKind byte

const (
	/// Never sent deliberately, in either direction. The absent-field zero.
	MarkerKindUnknown  MarkerKind = 0
	MarkerKindResource MarkerKind = 1
	MarkerKindCave     MarkerKind = 2
	MarkerKindMonster  MarkerKind = 3
	MarkerKindBoss     MarkerKind = 4
	MarkerKindCamp     MarkerKind = 5
	MarkerKindVillage  MarkerKind = 6
	/// A mark that is only its note.
	MarkerKindNote MarkerKind = 7
)

var EnumNamesMarkerKind = map[MarkerKind]string{
	MarkerKindUnknown:  "Unknown",
	MarkerKindResource: "Resource",
	MarkerKindCave:     "Cave",
	MarkerKindMonster:  "Monster",
	MarkerKindBoss:     "Boss",
	MarkerKindCamp:     "Camp",
	MarkerKindVillage:  "Village",
	MarkerKindNote:     "Note",
}

var EnumValuesMarkerKind = map[string]MarkerKind{
	"Unknown":  MarkerKindUnknown,
	"Resource": MarkerKindResource,
	"Cave":     MarkerKindCave,
	"Monster":  MarkerKindMonster,
	"Boss":     MarkerKindBoss,
	"Camp":     MarkerKindCamp,
	"Village":  MarkerKindVillage,
	"Note":     MarkerKindNote,
}

func (v MarkerKind) String() string {
	if s, ok := EnumNamesMarkerKind[v]; ok {
		return s
	}
	return "MarkerKind(" + strconv.FormatInt(int64(v), 10) + ")"
}
